# feedaccount 自动发布重构审查报告

**审查日期：** 2026-09-18
**审查范围：** database.js + server.js + auto-publish-scheduler.js + public/app.js + public/index.html
**重构目标：** 自动发布从「按达人」改为「按矩阵」组织

---

## 一、数据一致性

### 1.1 迁移脚本幂等性 ✅ 基本合格

- `matrix_auto_publish_config` 用 `CREATE TABLE IF NOT EXISTS` + `INSERT OR IGNORE`，重启可重复执行。
- 迁移前检查 `exists` 跳过已迁移行，幂等。
- `matrix_profiles` 重建块用 `CREATE TABLE IF NOT EXISTS matrix_profiles_new` + `INSERT OR IGNORE`，幂等。

### 1.2 迁移脚本风险 ⚠️

**【中危】迁移顺序矛盾** — `database.js:619-735`

迁移块 2.4.3（config 迁移，line 651）和 2.4.4（bindings 迁移，line 707）**早于** 2.4.5（matrix_profiles 重建为 1:1，line 738）。

- 2.4.4 的 bindings 迁移用 `INSERT OR IGNORE INTO matrix_profiles`，此时表还是旧约束（`UNIQUE(matrix_id, profile_id)` 组合唯一），不是 1:1。
- 如果一个 creator 的多条 binding 对应同一 matrix_id 但不同 profile_id（旧表允许），2.4.4 会全部插入，之后 2.4.5 重建时 `INSERT OR IGNORE` 到 `UNIQUE(matrix_id)` 新表会丢掉除第一条外的行。

线上数据 `creator_profile_bindings` 仅 1 行，实际触发概率低，但逻辑上不严谨。

**【低危】迁移反查 matrix_id 用 creator_id** — `database.js:668-676, 716-721`

计划文档（2.4.3）原设计是 `creator_auto_publish_config.matrix_id` 直接取，但实际实现改成了通过 `matrix_account_creators` 反查 creator→matrix_account→matrix_id。这与计划不一致，且依赖 `matrix_account_creators` 已有数据。若达人未被任何矩阵账号选中，配置会迁移失败被跳过。线上 `matrix_account_creators` 可能是空表（见附录标注"空表，需开始使用"），则 4 行旧配置**全部迁移失败**。

**【低危】cdp_instance_id 列不在计划建表语句中** — `database.js:646`

`matrix_auto_publish_config` 建表 SQL（line 626-644）不含 `cdp_instance_id` 列，随后 line 646 用 `#ensureColumn` 补。调度器 `triggerRemixTasks` 强依赖 `cfg.cdpInstanceId`（line 419, 424），无此列会报错。补列逻辑正确，但建表 SQL 与计划文档不符，属计划偏离。

### 1.3 新表字段完整性 ✅

`matrix_auto_publish_config` 含：`matrix_id, enabled, preset_id, daily_limit, monitor_interval_hours, last_monitor_at, publish_time_slots, ratio, hashtags_json, privacy_level, cdp_instance_id, created_at, updated_at`。字段完整，覆盖调度器与前端所需。

### 1.4 pipeline 表补列 ⚠️

**【中危】`createPipelineTask` Store 方法未补 source_url/matrix_id** — `database.js:2160-2178`

`store.createPipelineTask()` 的 INSERT 语句（line 2164）只含 11 列，**不含 `source_url` 和 `matrix_id`**。该方法被 `server.js:4454` 的 `POST /api/remix/pipeline/tasks` 路由调用。通过该 API 手动创建的 pipeline 任务 `source_url`/`matrix_id` 永远为 NULL，排期时走 `_getMatrixConfig` 回退逻辑补 `matrix_id`，但 `source_url` 永久缺失。计划文档未提及改此方法。

### 1.5 matrix_profiles 1:1 约束重建 ⚠️

**【中危】重建检测条件脆弱** — `database.js:743`

检测旧约束用 `mpSchema.sql.includes("matrix_id, profile_id")`。如果建表 SQL 文本恰好包含该子串但语义不同（如注释），会误判。且重建后再次启动时，新表 SQL 含 `UNIQUE (matrix_id)` 不含旧子串，检测正常跳过——这点 OK。但子串匹配脆弱。

**【低危】`bindMatrixProfile` 未显式强制 1:1** — `database.js:2364-2368`

DB 层已加 `UNIQUE(matrix_id)`，`INSERT OR IGNORE` 会静默忽略重复。server.js:4610-4614 的 POST 路由有显式 `existing.length > 0` 拦截，双保险，合格。但若绕过 server 直接调 store 方法，重复绑定会静默失败无提示。

---

## 二、API 一致性

### 2.1 前端调用与后端路由匹配 ✅

| 前端调用 | 后端路由 | 状态 |
|---|---|---|
| `GET /api/auto-publish/matrices` | `server.js:4184` | ✅ |
| `PUT /api/auto-publish/matrix-config/:matrixId` | `server.js:4206` | ✅ |
| `GET /api/auto-publish/matrix-config/:matrixId` | `server.js:4209` | ✅ |
| `GET /api/auto-publish/pipeline?matrixId=` | `server.js:4235` | ✅ |
| `POST /api/auto-publish/pipeline/retry/:id` | `server.js:4245` | ✅ |
| `POST /api/auto-publish/monitor/:matrixId` | `server.js:4254` | ✅ |
| `GET /api/auto-publish/logs` | `server.js:4263` | ✅ |

### 2.2 ⚠️ 遗留旧路由未删除

**【中危】旧达人级 API 路由仍存在** — `server.js:4332-4397`

计划 3.2 明确要求删除以下路由，但代码中仍保留：
- `GET/PUT /api/remix/creators/:creatorId/auto-publish-config`（line 4333）→ 仍调用已废弃的 `store.getAutoPublishConfig`/`upsertAutoPublishConfig`
- `GET /api/remix/auto-publish/creators`（line 4358）→ 仍调用 `store.listAutoPublishCreators()`
- `GET/POST /api/remix/creators/:creatorId/profile-bindings`（line 4364）→ 仍调用 `store.listProfileBindings`/`addProfileBinding`
- `PATCH/DELETE /api/remix/profile-bindings/:id`（line 4380）→ 仍调用 `updateProfileBinding`/`removeProfileBinding`

这些旧路由读写废弃表，与新架构并存，有数据不一致风险（用户可能误调旧 API 写旧表）。计划 Task 10 要求删除但未执行。

### 2.3 ⚠️ `/api/auto-publish/matrices` 返回的 autoPublishConfig 格式不一致

**【中危】enabled 字段类型与 GET 单条配置不一致** — `server.js:4187 vs 4210`

- 列表接口 line 4187：`store.getMatrixAutoPublishConfig(m.id) || { enabled: 0 }` — 回退时 `enabled` 是 `0`（数字）
- Store 方法 `getMatrixAutoPublishConfig` 返回 `enabled: row.enabled !== 0`（**布尔**，line 1862）
- 单条 GET 接口 line 4210 回退：`enabled: false`（布尔）
- 前端 `renderMatrices` 判断 `cfg.enabled === 1 || cfg.enabled === true`（line 8004）兼容了两种，合格
- 前端 `toggleMatrixAutoPublish` 提交 `enabled: enabled ? 1 : 0`（数字，line 7913）
- `upsertMatrixAutoPublishConfig` 接收 `enabled` 参数，内部 `enabled !== null` 才处理，`enabled ? 1 : 0` 转换

链路能跑通但类型不统一（列表给布尔，开关给数字），属隐式约定，易在后续维护中踩坑。

### 2.4 ⚠️ analytics 接口 matrixId 前缀检测

**【低危】** — `server.js:4291`

`profileId.startsWith("mx_")` 判断传入的是 matrixId。矩阵 ID 前缀是 `mx_`（`createMatrix` line 2331），profile ID 不以 `mx_` 开头，检测逻辑正确。但如果未来 profile ID 格式变化，此启发式可能失效。建议显式传参数标识。

---

## 三、调度器逻辑

### 3.1 监控循环 ✅

`monitorMatrixVideos()`（line 96）遍历 `_listEnabledMatrixConfigs()` → `matrix_auto_publish_config` 表，逻辑正确。`last_monitor_at` 更新用原始 SQL（line 113-115），正确。

### 3.2 profile_id 查询 ✅

`_checkMatrixNewVideos`（line 128）从 `matrix_profiles` 查 `profile_id`，`LIMIT 1`，符合 1:1 设计。

### 3.3 daily_limit 查询 ✅

`_scheduleOnePipeline`（line 549）`_getMatrixConfig(pipeline.matrix_id)` → `cfg.dailyLimit`，从矩阵配置取，不再查 `creator_profile_bindings`。合格。

### 3.4 监控遍历矩阵→达人 ✅

`_checkMatrixNewVideos`（line 138-144）SQL：`matrix_account_creators → matrix_accounts → remix_creators`，按 `matrix_id` 查所有关联达人，逻辑正确。

### 3.5 ⚠️ cdp_instance_id 强依赖

**【高危】cdp_instance_id 缺失直接标记 failed** — `auto-publish-scheduler.js:419,424-431,897,902-909`

`triggerRemixTasks` 和 `retryFailedTasks` 中，`cdpInstanceId = effectiveCfg?.cdpInstanceId || null`，为 null 则直接标 failed。但：
1. 新表 `cdp_instance_id` 是后补的列（line 646），迁移旧数据时从 `creator_auto_publish_config.cdp_instance_id` 迁移（line 696），但旧表此列可能也为 NULL（计划文档说 `cdp_instance_id` 已废弃，实例从 `matrix_profiles` 查）。
2. 计划文档 5.1.4 明确说"实例从 matrix_profiles 查"，但调度器仍从 `cfg.cdpInstanceId` 查，而非 `matrix_profiles.profile_id`。这是**计划与实现不一致**。
3. 若矩阵已绑 `matrix_profiles`（有 profile_id）但配置中 `cdp_instance_id` 为空，混剪会因缺 cdp_instance_id 失败，即使 profile_id 可用。

### 3.6 ⚠️ pipeline 补全逻辑的回退

**【低危】matrix_id 回退查询不幂等** — `auto-publish-scheduler.js:382-393, 561-573, 859-871`

三处（triggerRemix、schedulePublish、retryFailed）都有"若 `pipeline.matrix_id` 为空则反查 `matrix_account_creators` 补全"的逻辑。反查用的是 `LIMIT 1`（line 386-387），如果一个达人被多个矩阵选中，会取到不确定的第一个 matrix_id，补到错误的矩阵配置上。风险较低（依赖数据形态），但逻辑不严谨。

### 3.7 ⚠️ `_createPipelineForExistingVideos` 可能重复创建

**【低危】** — `auto-publish-scheduler.js:306-310`

去重检查 `WHERE source_video_id = ? AND profile_id = ?`（line 308），但**不含 `matrix_id`**。如果一个达人视频被两个不同矩阵（不同 profile_id）选中，因为 profile_id 不同，不会误判重复。但如果两个矩阵绑了**同一个 profile_id**（违反 1:1 但旧数据可能存在），会漏检。低概率。

### 3.8 ⚠️ pipeline 状态 "scheduled" 但 tk_publish_job 用 "success" 判定

**【中危】状态名不一致** — `auto-publish-scheduler.js:807` vs `tk_publish_jobs` 实际状态

`checkPublishResults` 检查 `job.status === "success"`（line 807），但 `tk_publish_jobs` 表的 status 默认是 `'pending'`（`database.js:203`），发布执行后状态应为 `'success'`/`'failed'`。需确认发布执行器（不在此审查范围）确实写入 `"success"` 而非 `"completed"` 或 `"published"`。若发布器用的状态字符串不同，pipeline 会永远卡在 `scheduled`。

---

## 四、端到端流程

### 4.1 混剪→排期→发布 链路 ⚠️

**【高危】混剪 API 返回 tasks 格式假设** — `auto-publish-scheduler.js:434-454`

调度器调 `POST /api/remix/ai-remix-task`，断言 `remixData.tasks?.length`（line 454）。需确认 server.js 该路由（line 2548 区域）返回 `{ tasks: [...] }` 格式。若格式不符，混剪触发会静默失败标 failed。

**【中危】排期后发布执行不在调度器内** — `auto-publish-scheduler.js:627-633`

`_scheduleOnePipeline` 创建 `tk_publish_job`（status=pending），pipeline 标 `scheduled`。但 `checkPublishResults`（line 789）只检查 `job.status === "success"`，**没有"执行发布"步骤**——即谁把 pending job 变成 success？需有独立的发布执行器（CDP 操作）轮询 pending job 执行发布。此审查范围内未见该执行器被调度器调用。如果发布执行器是独立进程/定时任务，链路可通；否则 job 永远 pending。

### 4.2 播放量记录 ⚠️

**【中危】播放量采集不在调度器** — `auto-publish-scheduler.js` 无 analytics 写入

`tk_video_analytics` 表的写入不在调度器主循环中。`/api/auto-publish/analytics/:id`（server.js:4287）只**读取** analytics。需有独立的播放量采集器写入 `tk_video_analytics`。若无，则 analytics 接口永远返回空。

### 4.3 监控→下载→pipeline 创建 ✅

`_checkCreatorNewVideosForMatrix`（line 161）→ `_downloadAndCreatePipelineForMatrix`（line 233），下载用 `/api/tiktok/download`，pipeline INSERT 含 `source_url` 和 `matrix_id`，链路完整。

---

## 五、前端渲染

### 5.1 `renderMatrices` ✅ 基本合格

矩阵列表渲染、开关绑定、点击打开弹窗，逻辑完整。

### 5.2 ⚠️ `openMatrixAccountsModal` 中 creatorCount 引用错误

**【高危】前端引用不存在的 `a.creatorCount`** — `app.js:8088`

`_renderMatrixAccountsHtml` 用 `a.creatorCount ?? 0`（line 8088），但后端 `/api/auto-publish/matrices` 返回的 account 对象（`store.listMatrixAccounts` 返回，`database.js:2394-2397`）**没有 `creatorCount` 字段**。后端只在矩阵顶层算 `creatorCount`（line 4198），account 级别没算。结果：弹窗中每个账号显示"0 达人"，即使实际有绑定。

### 5.3 ⚠️ `renderMonitor` 引用不存在的字段

**【高危】`m.lastMonitorAt` 和 `m.newVideoCount` 路径错误** — `app.js:8333-8334`

`renderMonitor` 用 `m.lastMonitorAt`（line 8333）和 `m.newVideoCount`（line 8334）。但后端返回的矩阵对象中，`lastMonitorAt` 在 `m.autoPublishConfig.lastMonitorAt` 内（嵌套），不在顶层。`newVideoCount` 字段后端**完全不返回**。结果：监控列表永远显示"从未监控"和"无新视频"。

### 5.4 ⚠️ `renderConfigPage` 拉取后覆盖 autoPublishConfig

**【低危】** — `app.js:8107-8109`

`cfg = await request(...)` 后 `m.autoPublishConfig = cfg`。GET 单条接口在配置不存在时返回 `{ matrixId, enabled: false, presetId: null, ... }`（server.js:4210），字段比列表接口返回的 Store 对象少（缺 `createdAt`/`updatedAt`/`cdpInstanceId`）。覆盖后 `m.autoPublishConfig` 丢失部分字段。影响有限（渲染用到的字段都有），但数据完整性受损。

### 5.5 ⚠️ `renderPipeline` 表头与数据列数

**【低危】colspan 不匹配** — `index.html:1604` vs `app.js:8227,8260`

表头 7 列（矩阵/源视频/状态/目标实例/尝试/创建时间/操作），空状态 `colspan="7"`（line 8227）匹配。日志展开行 `colspan="7"`（line 8260）也匹配。合格。但计划文档要求表头有"平台账号"列（4.3.8），实际 index.html 只有 7 列无"平台账号"列，`listPipelineTasks` 返回了 `platform`/`accountName` 但前端未渲染。计划偏离。

### 5.6 ⚠️ 事件绑定重复

**【低危】** — `app.js:8027-8041` vs `app.js:7814-7836`

`renderMatrices` 每次调用都 `addEventListener`（line 8028, 8037），而 `fetchMatrices` 每次都调 `renderMatrices`。刷新多次后同一元素上会累积多个 click 监听器（虽然 `innerHTML` 重写会清除旧元素，所以实际 OK）。`_bindEvents`（line 7814）中的 `refreshMatrices` 等按钮只绑一次，OK。总体无重复绑定问题，但代码组织可优化。

### 5.7 ⚠️ 模态框关闭方式不一致

**【低危】** — `index.html:1640` vs `app.js:8055,8067-8069`

index.html 的关闭按钮用内联 `onclick`（line 1640）设 `display='none'`。app.js 的 `_closeMatrixModal`（line 8067）也设 `display='none'` 并清 `selectedMatrixId`。`_bindEvents`（line 7827-7832）绑了点击遮罩和 `.modal-close` 关闭。但 index.html 的 `onclick` 不会清 `selectedMatrixId`，导致用 × 按钮关闭后 `selectedMatrixId` 残留。后续若依赖该状态可能出错。

---

## 六、Payload 一致性（falsy 值）

### 6.1 ⚠️ dailyLimit = 0 被 filter 掉

**【高危】`dailyLimit: 0` 无法保存** — `app.js:8154`

`dailyLimit: parseInt(dailyInp?.value, 10) || 0`（line 8154）—— 当输入为 0 时 `parseInt("0") || 0` = 0，payload 中 `dailyLimit: 0`。传到后端 `upsertMatrixAutoPublishConfig`（database.js:1892）`if (dailyLimit !== null)` → `0 !== null` 为 true，会执行 `sets.push("daily_limit = ?"); params.push(0)`。**Insert 路径**（line 1916）用 `dailyLimit || 3` → `0 || 3` = 3，**UPDATE 路径 OK，INSERT 路径会把 0 变成 3**。

不一致：已有配置设 dailyLimit=0 可存；新建配置设 0 会变 3。若用户想"暂停发布"（dailyLimit=0），首次保存无效。

### 6.2 ⚠️ enabled = 0 / false

**【低危】** — `app.js:7913,8172`

`toggleMatrixAutoPublish` 提交 `enabled: enabled ? 1 : 0`（line 7913），关闭时传 0。`upsertMatrixAutoPublishConfig`：`enabled !== null` → `0 !== null` true → `enabled ? 1 : 0` = 0。正确，0 能存。

`saveMatrixConfig` 的 `payload.enabled = (cfg.enabled === 1 || cfg.enabled === true) ? 1 : 0`（line 8172），也能正确传 0。合格。

### 6.3 ⚠️ publishTimeSlots 清空

**【低危】** — `app.js:8169,8187`

清空时间段传 `publishTimeSlots: null`（line 8169, 8187）。`upsertMatrixAutoPublishConfig`：`publishTimeSlots !== null` → `null !== null` false → 不更新。**无法清空已设置的时间段**（UPDATE 路径跳过，值保持不变）。

### 6.4 ⚠️ cdpInstanceId / presetId 清空

**【低危】** — `app.js:8156,8201,8210`

清空 select 传 `cdpInstanceId: null` / `presetId: null`。同 6.3，`!== null` 为 false，无法清空。

---

## 七、遗留代码

### 7.1 ⚠️ 旧 Store 方法未删除

**【中危】** — `database.js:1952-2099`

以下计划要求删除的方法仍保留且仍被旧路由调用：
- `getAutoPublishConfig`（line 1952）← 被 `server.js:4337` 调用
- `upsertAutoPublishConfig`（line 1974）← 被 `server.js:4342` 调用
- `listAutoPublishCreators`（line 2019）← 被 `server.js:4359` 调用
- `listProfileBindings`（line 2043）← 被 `server.js:4368` 调用
- `addProfileBinding`（line 2056）← 被 `server.js:4374` 调用
- `removeProfileBinding`（line 2075）← 被 `server.js:4394` 调用
- `updateProfileBinding`（line 2079）← 被 `server.js:4385` 调用

### 7.2 ⚠️ `createPipelineTask` 未适配新列

**【中危】** — `database.js:2160-2178`

如 1.4 所述，`createPipelineTask` 的 INSERT 不含 `source_url`/`matrix_id`，手动 API 创建的 pipeline 缺这两列。

### 7.3 ⚠️ 调度器注释仍提旧表

**【低危】** — `auto-publish-scheduler.js:13-15`

文件头注释仍写 `creator_auto_publish_config` / `creator_profile_bindings`，未更新为 `matrix_auto_publish_config` / `matrix_profiles`。

### 7.4 ⚠️ `_ensureSchema` 重复建表

**【低危】** — `auto-publish-scheduler.js:985-1008`

调度器 `_ensureSchema` 又建了一次 `matrix_auto_publish_config`（line 991-1008），与 `database.js` 的建表重复。双保险，无功能问题，但代码冗余且两处定义需保持同步（调度器版多了 `cdp_instance_id` 列在建表 SQL 中，database.js 版是后补）。

---

## 八、错误处理

### 8.1 迁移失败处理 ⚠️

**【中危】迁移无 try-catch** — `database.js:651-761`

迁移块（2.4.3-2.4.5）无 try-catch 包裹。若迁移 SQL 报错（如旧表 schema 异常），会抛出异常导致 `#migrate()` 失败 → 构造函数失败 → 服务无法启动。无降级路径。

### 8.2 监控失败处理 ✅

`monitorMatrixVideos`（line 110-120）每个矩阵 try-catch，失败记日志不中断循环。合格。

### 8.3 混剪/排期失败处理 ✅

`triggerRemixTasks`（line 471-478）、`schedulePublishJobs`（line 537-542）、`retryFailedTasks`（line 948-954）都有 try-catch，失败标 failed + 记日志。合格。

### 8.4 API 报错降级 ⚠️

**【低危】GET /api/auto-publish/matrices 无错误处理** — `server.js:4184-4202`

该路由无 try-catch。若 `store.listMatrices()` 或子查询报错，会抛到顶层 HTTP handler 返回 500。前端 `fetchMatrices` 有 catch（line 7856），能显示"加载失败"。降级合格但后端无结构化错误响应。

### 8.5 ⚠️ `readJson` 未处理异常

**【低危】** — `server.js:4217`

PUT matrix-config 路由 `const body = await readJson(request)` 无 try-catch。若请求体非 JSON，`readJson` 抛异常 → 500。前端能 catch 显示错误，但后端无 400 友好响应。

---

## 九、问题汇总（按严重程度）

### 🔴 高危
1. **前端 `creatorCount` 字段不存在**（5.2）— 弹窗永远显示"0 达人"
2. **前端 `lastMonitorAt`/`newVideoCount` 路径错误**（5.3）— 监控列表永远显示错误信息
3. **cdp_instance_id 从配置查而非 matrix_profiles 查**（3.5）— 与计划不符，配置缺失即失败
4. **混剪 API 返回格式假设**（4.1）— 需验证 `tasks` 字段
5. **dailyLimit=0 在 INSERT 路径变 3**（6.1）— 无法设 0 暂停发布

### 🟡 中危
6. **旧 API 路由未删除**（2.2）— 读写废弃表，数据不一致风险
7. **旧 Store 方法未删除**（7.1）— 同上
8. **`createPipelineTask` 未含 source_url/matrix_id**（1.4, 7.2）
9. **迁移无 try-catch**（8.1）— 迁移失败致服务无法启动
10. **发布执行步骤不在调度器**（4.1）— job 可能永远 pending
11. **播放量采集不在调度器**（4.2）— analytics 接口可能永远空
12. **pipeline 状态 "scheduled" vs job "success" 需确认**（3.8）
13. **迁移顺序矛盾**（1.2）— 2.4.4 早于 2.4.5 重建
14. **enabled 类型不统一（布尔 vs 数字）**（2.3）
15. **publishTimeSlots/cdpInstanceId/presetId 无法清空**（6.3, 6.4）— `!== null` 判断导致 null 值被跳过

### 🟢 低危
16. 迁移反查 matrix_id 依赖 matrix_account_creators 有数据（1.2）
17. cdp_instance_id 列位置与计划不符（1.2）
18. 重建检测用子串匹配脆弱（1.5）
19. matrix_id 回退查询用 LIMIT 1 取不确定值（3.6）
20. pipeline 补全去重不含 matrix_id（3.7）
21. analytics 前缀检测启发式（2.4）
22. renderConfigPage 覆盖丢失字段（5.4）
23. 表头缺"平台账号"列（5.5）
24. 模态框 × 按钮不清 selectedMatrixId（5.7）
25. 调度器注释未更新（7.3）
26. _ensureSchema 重复建表（7.4）
27. GET matrices 路由无 try-catch（8.4）
28. readJson 无异常处理（8.5）

---

## 十、建议优先修复顺序

1. **立即修复**（阻断核心功能）：
   - 前端 `creatorCount` / `lastMonitorAt` / `newVideoCount` 字段路径（#1, #2）
   - `dailyLimit=0` 在 INSERT 路径的处理（#5）
   - `publishTimeSlots=null` 等清空逻辑（#15）

2. **尽快修复**（数据完整性/架构一致性）：
   - 删除旧 API 路由 + 旧 Store 方法（#6, #7）
   - `createPipelineTask` 补 source_url/matrix_id（#8）
   - cdp_instance_id 改从 matrix_profiles 查或明确配置来源（#3）
   - 迁移块加 try-catch（#9）

3. **验证后修复**（依赖外部组件）：
   - 确认混剪 API 返回格式（#4）
   - 确认发布执行器存在并写入 job.status="success"（#11, #12）
   - 确认播放量采集器存在（#13）

4. **后续优化**（低优先）：
   - enabled 类型统一（#14）
   - 迁移顺序调整（#10）
   - 各低危项

---

**审查结论：** 重构主体逻辑正确，矩阵化改造方向符合计划。但前端有 3 个高危字段引用错误会导致核心 UI 功能失效；payload 中 falsy 值（0/null）的保存逻辑有缺陷；旧代码未清理；发布执行和播放量采集环节不在调度器内需确认外部组件。建议按优先级修复后再上线。
