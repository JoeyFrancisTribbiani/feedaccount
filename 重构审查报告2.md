# feedaccount 自动发布重构第二轮审查报告

**审查日期：** 2026-09-18
**审查范围：** database.js + server.js + auto-publish-scheduler.js + public/app.js + public/index.html + tiktok/tiktok-publish-manager.js + tiktok/tiktok-publisher.js
**审查目标：** 验证上一轮修复 + 检查新问题 + 端到端流程 + 前后端匹配

---

## 一、上一轮修复验证（逐个核对）

### 1.1 ✅ 已修复：迁移脚本 try-catch

- `database.js:621-780`：整个迁移块已用 `try { ... } catch (migrateErr) { ... }` 包裹（line 621 `try`，line 770 `catch`）。
- 失败时记录 cdp_logs（line 773-775），降级 `console.error` 继续启动，不阻止服务启动。
- `CREATE TABLE IF NOT EXISTS` + `INSERT OR IGNORE` 保持幂等。

### 1.2 ✅ 已修复：迁移顺序矛盾

- `database.js:650-674`：2.4.5（matrix_profiles 重建为 1:1）**已移到** 2.4.3/2.4.4（config/bindings 迁移，line 676/730）**之前**执行。
- 注释明确标注（line 651）"必须在数据迁移之前执行，确保新表已是 1:1 约束"。
- 逻辑正确：重建后新表已有 `UNIQUE(matrix_id)`（line 665），后续 2.4.4 的 `INSERT OR IGNORE`（line 759）会正确去重。

### 1.3 ✅ 已修复：cdp_instance_id 从 matrix_profiles 查

- `auto-publish-scheduler.js:419-425`（triggerRemixTasks）和 `903-909`（retryFailedTasks）：
  - 先取 `effectiveCfg?.cdpInstanceId`（line 419/903），为空时从 `matrix_profiles` 查 `profile_id`（line 422-424 / 906-908）作为 CDP 实例。
  - 两处都有相同的回退逻辑，一致。
- 与计划文档 5.1.4 一致：实例从 `matrix_profiles` 查。

### 1.4 ✅ 已修复：混剪 API 返回格式

- `server.js:2677`：`sendJson(response, 200, { tasks, count: tasks.length })` — 返回 `{ tasks: [...] }` 格式。
- `auto-publish-scheduler.js:460`：`remixData.tasks?.length` 断言正确，格式匹配。

### 1.5 ✅ 已修复：dailyLimit=0 在 INSERT 路径

- `database.js:1892-1942`：`upsertMatrixAutoPublishConfig` 全部参数默认值改为 `undefined`（line 1893-1896），判断条件从 `!== null` 改为 `!== undefined`。
- INSERT 路径（line 1932）：`dailyLimit !== undefined ? dailyLimit : 3` — 传 0 时 `0 !== undefined` 为 true，使用 0。**正确**。
- UPDATE 路径（line 1908）：`if (dailyLimit !== undefined)` — 传 0 时进入更新，push 0。**正确**。

### 1.6 ✅ 已修复：publishTimeSlots / cdpInstanceId / presetId 清空

- 同 1.5，全部改用 `!== undefined` 判断（line 1907-1915）。
- 传 `null` 时：`null !== undefined` 为 true → 进入更新 → push null。**可清空**。

### 1.7 ✅ 已修复：前端 creatorCount 字段

- `server.js:4190-4193`：列表接口给每个 account 加 `creatorCount: store.getMatrixAccountCreators(a.id).length`（line 4192）。
- `app.js:8088`：`const creatorCount = a.creatorCount ?? 0` — 现在后端确实返回此字段。**匹配**。

### 1.8 ✅ 已修复：前端 lastMonitorAt 路径

- `app.js:8333-8334`：`m.autoPublishConfig?.lastMonitorAt` — 从嵌套对象取，注释说明"在 m.autoPublishConfig 内"。
- 后端 `server.js:4200`：`autoPublishConfig: cfg` — cfg 含 `lastMonitorAt`（`database.js:1881`）。**路径匹配**。

### 1.9 ✅ 已修复：前端 newVideoCount

- 上一轮报告指出 `m.newVideoCount` 不存在。本轮检查 `app.js` 中**已无 `newVideoCount` 引用**。
- `renderMonitor`（line 8332-8341）现在只显示矩阵名 + 最后监控时间，不显示新视频数。**问题已消除**。

### 1.10 ✅ 已修复：旧 API 路由已删除

- `server.js:4337`：注释"旧达人级自动发布 API 已删除（迁移到矩阵级 /api/auto-publish/*）"。
- 旧路由 `GET/PUT /api/remix/creators/:creatorId/auto-publish-config` 等已不存在。
- 全文搜索 `creator_auto_publish_config` 在 server.js 中 0 次出现（仅注释提及）。

### 1.11 ✅ 已修复：旧 Store 方法已删除

- `database.js` 中 `getAutoPublishConfig`、`upsertAutoPublishConfig`、`listAutoPublishCreators`、`listProfileBindings`、`addProfileBinding`、`removeProfileBinding`、`updateProfileBinding` **全部已删除**（0 次出现）。

### 1.12 ✅ 已修复：createPipelineTask 补 source_url/matrix_id

- `database.js:2029-2044`：`createPipelineTask` 的 INSERT 语句现已包含 `source_url` 和 `matrix_id` 列（13 列，line 2030）。
- `getPipelineTask` 仍不返回这两个字段（见 3.4），但 `listPipelineTasks` 返回了（line 2096-2101）。

### 1.13 ✅ 已修复：发布执行器存在

- `server.js:403-404`：`TiktokPublishManager` 实例化（line 403）并 `startScheduler()` 启动（line 404，用默认 30 秒轮询）。
- `server.js:2220-2225`：`POST /api/tiktok/publish/jobs/:id/execute` 路由存在，调用 `tiktokPublisherManager.executeJob(jobId)`（line 2223）。
- 调度器 `checkPublishResults`（line 813）检查 `job.status === "success"`，与 `executeJob` 写入的 `status: "success"`（`tiktok-publish-manager.js:101`）匹配。

### 1.14 ✅ 已修复：播放量采集在发布后

- `tiktok-publish-manager.js:99-128`：`result.ok` → 更新 `status: "success"`（line 100-104）→ `publisher.recordAnalytics()`（line 110）→ INSERT `tk_video_analytics`（line 116-124）。
- 采集在 `executeJob` 内部，不在调度器主循环中，但逻辑正确：发布成功 → 采集。
- 采集失败不阻止发布结果（catch 记 warning，line 126-128）。

### 1.15 ✅ 已修复：pipeline 状态 "scheduled" vs job "success"

- `auto-publish-scheduler.js:813`：`job.status === "success"` — 与 `tiktok-publish-manager.js:101` 写入的 `status: "success"` 一致。
- `tk_publish_jobs` 表 status 列（`database.js:203`）默认 `'pending'`，无枚举约束，`"success"` / `"failed"` / `"pending"` / `"running"` 均为字符串，无冲突。

### 1.16 ✅ 已修复：模态框 × 按钮不清 selectedMatrixId

- `index.html:1640`：内联 `onclick="document.getElementById('ap-matrix-modal').style.display='none'"`。
- `app.js:7832`：`_bindEvents` 绑定 `#ap-matrix-modal .modal-close` 的 click → 调用 `_closeMatrixModal()`（line 8067-8070 清 `selectedMatrixId`）。
- **两个事件处理器都会触发**（内联 onclick 先执行，addEventListener 后执行），`_closeMatrixModal` 清除了 `selectedMatrixId`。**已修复**。

---

## 二、上一轮低危问题跟踪

### 2.1 ⚠️ 仍存在：调度器文件头注释未更新

**【低危】** — `auto-publish-scheduler.js:13-14`

文件头注释仍写：
```
 *   - creator_auto_publish_config  — 达人自动发布配置
 *   - creator_profile_bindings     — 达人绑定指纹浏览器 profile
```
应改为 `matrix_auto_publish_config` / `matrix_profiles`。不影响功能，但维护时易误导。

### 2.2 ⚠️ 仍存在：_ensureSchema 重复建表

**【低危】** — `auto-publish-scheduler.js:997-1021`

调度器 `_ensureSchema` 仍建了一次 `matrix_auto_publish_config`（line 1003-1020，含 `cdp_instance_id` 在建表 SQL 中），与 `database.js:628-646` 的建表重复。两处定义需保持同步。实际上 `database.js` 版用 `#ensureColumn` 补 `cdp_instance_id`（line 648），调度器版在建表 SQL 中直接包含，两者最终结果一致，但维护时需改两处。

### 2.3 ⚠️ 仍存在：matrix_id 回退查询用 LIMIT 1

**【低危】** — `auto-publish-scheduler.js:383-392, 568-578, 866-877`

三处回退逻辑仍用 `LIMIT 1` 反查 `matrix_account_creators → matrix_accounts → matrix_id`。如果一个达人被多个矩阵选中，会取不确定的第一个。低概率但逻辑不严谨。

### 2.4 ✅ 维持原状：analytics 前缀检测

- `server.js:4296`：`profileId.startsWith("mx_")` 仍存在，矩阵 ID 前缀确实是 `mx_`（`createMatrix` `database.js:2230` 区域），逻辑正确。低危风险未恶化。

### 2.5 ⚠️ 仍存在（未恶化）：renderConfigPage 覆盖丢失字段

**【低危】** — `app.js:8107-8109`

`renderConfigPage` 仍用 GET 单条接口覆盖 `m.autoPublishConfig`（line 8109 `m.autoPublishConfig = cfg`）。
GET 单条回退（`server.js:4215-4217`）在配置不存在时返回 `{ matrixId, enabled: false, presetId: null, dailyLimit: 3, monitorIntervalHours: 6 }`，**缺少** `lastMonitorAt`/`publishTimeSlots`/`ratio`/`cdpInstanceId`/`createdAt`/`updatedAt` 等字段。
- 首次保存后 `upsertMatrixAutoPublishConfig` 会创建完整行，后续 GET 返回完整对象。影响有限，但配置不存在时前端配置页下拉框会因 `cfg.cdpInstanceId === undefined` 无法选中正确项。

### 2.6 ✅ 维持原状：表头缺"平台账号"列

- `index.html:1597-1605`：表头仍为 7 列（矩阵/源视频/状态/目标实例/尝试/创建时间/操作），无"平台账号"列。
- `listPipelineTasks`（`database.js:2083`）返回了 `platform`/`accountName`，前端 `renderPipeline`（`app.js:8230-8266`）未渲染这两列。数据有但未展示，不影响功能。

---

## 三、新发现问题

### 3.1 🔴 高危：listPipelineTasks JOIN matrix_accounts 产生重复行

**【高危】** — `database.js:2087`

```sql
LEFT JOIN matrix_accounts ma ON ma.matrix_id = p.matrix_id
```

`listPipelineTasks` 的 SQL（line 2081-2091）用 `matrix_accounts` 左连接获取 `platform`/`account_name`。但如果一个矩阵有**多个平台账号**（如 TikTok 账号 + Instagram 账号），这个 JOIN 会为每个 pipeline 任务产生 **N 行**（N = 矩阵中账号数）。

- 前端 `renderPipeline`（`app.js:8230`）遍历 `this.pipelineTasks`，会显示重复的任务行。
- `LIMIT 100`（line 2090）会被重复行占用，实际返回的唯一任务数少于预期。
- 这会误导用户认为有多个相同任务。

**修复建议：** 用子查询取 `LIMIT 1` 的账号，或用 `GROUP_CONCAT` 聚合，或在应用层按 `p.id` 去重。

### 3.2 🟡 中危：recordAnalytics 记录全部主页视频，非仅发布的视频

**【中危】** — `tiktok-publish-manager.js:110-124` + `tiktok-publisher.js:501-542`

`recordAnalytics()`（publisher line 501-542）抓取账号主页上**所有视频**（滚动最多 30 次，line 503），对每个视频都 push 一条记录（line 534-539）。

`executeJob` 中（manager line 120-124）对每个视频都 INSERT 一行 `tk_video_analytics`，且**全部关联到同一个 jobId**（line 123 `insertStmt.run(jobId, views, likes, 0, 0, nowIso)`）。

问题：
1. 如果账号主页有 50 个视频，会插入 50 行，全部关联到当前 jobId。
2. analytics API（`server.js:4306-4332`）按 `publish_job_id` 查询，会把 50 个视频的播放量**全部算到这一个发布任务上**。
3. `totalViews`（line 4319）= 50 个视频播放量总和，`maxViews`/`minViews`（line 4320-4321）也跨视频，数据语义错误。

**修复建议：** 在 `recordAnalytics` 中过滤只保留 `publishedVideoId` 对应的视频（即 `job.publishedVideoId`，发布成功后已写入），或将 `videoId` 存入 analytics 表并按视频聚合。

### 3.3 🟡 中危：likes/comments/shares 始终为 0

**【中危】** — `tiktok-publisher.js:534-539` + `tiktok-publish-manager.js:122-123`

`recordAnalytics()` 只抓取 `data-e2e="video-views"` 元素的播放量（publisher line 527-528），**不抓取**点赞数、评论数、分享数。

```javascript
items.push({
  videoId: match[2],
  videoUrl: href,
  views: viewsText,     // ← 有值
  title: title.substring(0, 100),
  // 无 likes, comments, shares 字段
});
```

`executeJob` 中：
```javascript
const likes = this._parseCount(v.likes);  // v.likes = undefined → _parseCount(undefined) → 0
insertStmt.run(jobId, views, likes, 0, 0, nowIso);  // comments=0, shares=0 硬编码
```

结果：`tk_video_analytics` 表中 `likes_count`、`comments_count`、`shares_count` **永远为 0**。

**修复建议：** 在 `recordAnalytics` 中增加抓取点赞/评论/分享的元素（TikTok 主页卡片通常有 `data-e2e="video-likes"` 等属性），或明确标注这些指标不采集。

### 3.4 🟢 低危：getPipelineTask 缺 matrixId/sourceUrl 字段

**【低危】** — `database.js:2049-2071`

`getPipelineTask` 返回的对象（line 2057-2070）**不含** `matrixId`、`sourceUrl`、`matrixName`、`platform`、`accountName`，而 `listPipelineTasks`（line 2092-2110）返回这些字段。

如果其他代码通过 `getPipelineTask` 获取单个任务并期望 `matrixId`，会得到 `undefined`。目前 `getPipelineTask` 主要被 `createPipelineTask`（line 2046）和 `updatePipelineTask`（line 2124）的返回值使用，影响有限，但字段不一致易踩坑。

### 3.5 🟢 低危：状态过滤器缺少 'scheduled' 选项

**【低危】** — `index.html:1581-1590`

状态过滤下拉菜单选项（line 1583-1589）：`pending, remixing, remixed, publishing, published, failed, retry`

**缺少 `scheduled`**：pipeline 的 `scheduled` 状态（排期已创建，等待发布执行，`auto-publish-scheduler.js:643`）无法被过滤查看。

**多余 `publishing`**：调度器从不设置 pipeline 状态为 `publishing`（流程是 `remixed → scheduled → published`），此选项永远不会匹配任何任务。

### 3.6 🟢 低危：statusClass 缺少 'scheduled' 样式

**【低危】** — `app.js:7751-7762`

`statusClass` 映射表（line 7752-7760）**无 `scheduled`** 键。当 pipeline 状态为 `scheduled` 时，`statusClass(status)` 返回默认值 `'ap-status-pending'`（line 7761）。

用户看到 `scheduled` 状态的任务显示为 `pending` 的样式，视觉上无法区分"待混剪"和"已排期待发布"。

### 3.7 🟢 低危：executeJob 中 username 变量为死代码

**【低危】** — `tiktok-publish-manager.js:87-97`

```javascript
let username = null;
if (this.persistence) {
  const pipeline = this.persistence.db.prepare(
    "SELECT creator_id FROM auto_remix_publish_pipeline WHERE publish_job_id = ?"
  ).get(jobId);
  if (pipeline) {
    const creator = this.persistence.db.prepare(
      "SELECT name FROM remix_creators WHERE id = ?"
    ).get(pipeline.creator_id);
    if (creator) {
      username = creator.name.replace(/^@/, "");
    }
  }
}
```

`username` 变量被赋值后**从未使用**（后续 line 110 `publisher.recordAnalytics()` 内部自行通过 TikTok API 获取当前登录账号用户名，publisher line 474-486）。这是一段无效的数据库查询代码，浪费资源且易误导维护者。

### 3.8 🟢 低危：index.html 模态框内联 onclick 与 addEventListener 并存

**【低危】** — `index.html:1640`

关闭按钮同时有内联 `onclick` 和 `addEventListener` 绑定（`app.js:7832`）。两者都会触发，功能正确（`selectedMatrixId` 被清除），但双重关闭逻辑冗余。建议移除内联 `onclick`，统一用 `addEventListener`。

### 3.9 🟢 低危：fetchPipelineTasks 回退解析 data.tasks 但后端返回数组

**【低危】** — `app.js:7873`

```javascript
this.pipelineTasks = Array.isArray(data) ? data : (data?.tasks || []);
```

后端 `server.js:4245` 返回 `store.listPipelineTasks(...)` 的结果，是**数组**（`database.js:2092` 返回 `rows.map(...)`）。所以 `Array.isArray(data)` 为 true，`data?.tasks` 回退分支永不执行。代码无害但冗余——这是兼容旧格式 `data.tasks` 的遗留。

### 3.10 🟢 低危：updatePipelineTask 用 `!== null` 判断，无法清空字段

**【低危】** — `database.js:2113-2121`

`updatePipelineTask` 的参数默认值是 `null`，判断用 `!== null`：
```javascript
if (status !== null) { sets.push("status = ?"); params.push(status); }
if (remixTaskId !== null) { sets.push("remix_task_id = ?"); params.push(remixTaskId); }
```

如果调用方想用此方法**清空** `remix_task_id` 或 `publish_job_id`（传 null），会被跳过。目前调度器清空用的是 `failReason: null`（`auto-publish-scheduler.js:816`）——但 `failReason` 判断也是 `!== null`，传 null 时**不会清空 fail_reason**，只是不更新。

实际影响：`checkPublishResults` 成功时传 `failReason: null`（line 816）想清空失败原因，但 `!== null` 为 false → **不更新 → fail_reason 保持原值**。成功发布的 pipeline 仍显示旧的失败原因文本。这是一个实际触发的 bug，但因前端只在 `isFailed` 时显示 failReason（`app.js:8248`），`published` 状态不显示，所以用户不可见。

**建议：** 与 `upsertMatrixAutoPublishConfig` 一致，改用 `!== undefined` 判断。

---

## 四、端到端流程完整性

### 4.1 监控 → 下载 → pipeline 创建 ✅

```
monitorMatrixVideos() (scheduler:96)
  → _listEnabledMatrixConfigs() [查 matrix_auto_publish_config WHERE enabled=1]
  → _checkMatrixNewVideos(cfg) [查 matrix_profiles 取 profile_id (line 129), 查 matrix_account_creators 取达人 (line 138-144)]
  → _checkCreatorNewVideosForMatrix() [调 /api/tiktok/parse-profile 获取新视频 (line 172)]
  → _downloadAndCreatePipelineForMatrix() [调 /api/tiktok/download (line 234), INSERT pipeline 含 source_url + matrix_id (line 257-269)]
```

链路完整。pipeline INSERT 含 13 列（含 `source_url` line 265、`matrix_id` line 266），数据完整。

另有 `_createPipelineForExistingVideos()`（line 280）为已下载视频补建 pipeline，去重检查 `source_video_id + profile_id`（line 308），逻辑正确。

### 4.2 混剪触发 ✅

```
triggerRemixTasks() (scheduler:345)
  → 取 pending pipeline (line 346)
  → _getMatrixConfig(pipeline.matrix_id) [查配置 (line 369)]
  → cdpInstanceId 从 cfg 或 matrix_profiles 取 (line 419-425)
  → POST /api/remix/ai-remix-task → 返回 { tasks: [...] } (server:2677)
  → pipeline.status = "remixing" (line 465)
```

链路完整。cdp_instance_id 有双保险（配置 → matrix_profiles）。`effectiveMatrixId` 在传给 `matrixIds: [effectiveMatrixId]`（line 444）前已确保非 null（line 380-403 的 cfg 检查会拦截无配置的情况）。

### 4.3 混剪完成检测 ✅

```
checkRemixComplete() (scheduler:489)
  → 取 remixing pipelines (line 490)
  → getRemixTask(pipeline.remix_task_id) [查 remix_tasks 表 (line 497)]
  → status === "DONE" → pipeline.status = "remixed" (line 509)
  → status === "FAILED" → pipeline.status = "failed" (line 519)
```

remix_tasks 的状态值 `"DONE"` / `"FAILED"`（大写）与 `#mapRemixTask` 的 `r.status` 直接传递一致。

### 4.4 排期发布 ✅

```
schedulePublishJobs() (scheduler:537)
  → 取 remixed pipelines (line 538)
  → _scheduleOnePipeline() (line 553)
    → _getMatrixConfig() 取 dailyLimit (line 594)
    → _countTodayPublishedByProfile() [查今日 success 状态的 jobs (line 783-790)]
    → todayPublished >= dailyLimit → 跳过 (line 596-598)
    → createTkMaterial() [创建素材 (line 624)]
    → createTkPublishJob() [创建 tk_publish_job, status=pending (line 633-639)]
    → pipeline.status = "scheduled" (line 643)
```

链路完整。`dailyLimit=0` 时 `0 >= 0` 永远为 true → 跳过 = 暂停发布，语义正确。但注意 `_countTodayPublishedByProfile` 用 `j.status !== "success"` 过滤（line 787），只统计成功发布的，逻辑正确。

### 4.5 发布执行 ✅

```
TiktokPublishManager.checkAndExecutePendingJobs() [30秒轮询 (manager:12-21)]
  → listTkPublishJobs({ status: "pending", limit: 20 }) (manager:33)
  → 并发限制：runningJobIds.size > 0 则跳过 (manager:36)
  → job.scheduledAt <= now → executeJob(jobId) (manager:39-40)
    → bitBrowserApi.openProfile(job.profileId) [开浏览器 (manager:62)]
    → publisher.connect(wsUrl) [连 CDP (manager:68)]
    → publisher.uploadVideo() [上传+发布 (manager:79)]
    → updateTkPublishJobStatus(jobId, { status: "success" }) [成功 (manager:100-104)]
    → publisher.recordAnalytics() [采集播放量 (manager:110)]
    → INSERT tk_video_analytics (manager:116-124)
```

链路完整。发布执行器与调度器分离运行，通过 `tk_publish_jobs.status` 通信。

### 4.6 发布结果检测 ✅

```
checkPublishResults() [调度器主循环 (scheduler:795)]
  → 取 scheduled pipelines (line 796)
  → getTkPublishJob(pipeline.publish_job_id) (line 803)
  → job.status === "success" → pipeline.status = "published" (line 814-817)
  → job.status === "failed" → pipeline.status = "retry" (attempt < 3) 或 "failed" (line 824-838)
```

链路完整。状态字符串匹配（`"success"` / `"failed"`）。

### 4.7 播放量记录 ⚠️

流程存在但有数据质量问题（见 3.2、3.3）：
- 播放量采集**确实在 executeJob 成功后执行**（非调度器内，但时序正确）。
- 但采集的是**整个主页所有视频**的播放量，全部关联到同一个 jobId，数据语义错误。
- likes/comments/shares 始终为 0。

### 4.8 失败重试 ✅

```
retryFailedTasks() (scheduler:853)
  → 取 retry pipelines (line 854)
  → 重新触发混剪（POST /api/remix/ai-remix-task）(line 853-948)
  → pipeline.status = "remixing" (attempt_count 保留，line 825-836 中 attempt 在发布失败时递增)
```

链路完整。重试是重新混剪（非重新发布），逻辑合理。

---

## 五、前端后端匹配验证

### 5.1 API 路由匹配 ✅

| 前端调用 | 后端路由 | 状态 |
|---|---|---|
| `GET /api/auto-publish/matrices` | `server.js:4184` | ✅ |
| `PUT /api/auto-publish/matrix-config/:matrixId` | `server.js:4221` | ✅ |
| `GET /api/auto-publish/matrix-config/:matrixId` | `server.js:4214` | ✅ |
| `GET /api/auto-publish/pipeline?matrixId=&status=&limit=` | `server.js:4240` | ✅ |
| `POST /api/auto-publish/pipeline/retry/:id` | `server.js:4250` | ✅ |
| `POST /api/auto-publish/monitor/:matrixId` | `server.js:4259` | ✅ |
| `GET /api/auto-publish/logs?level=&limit=` | `server.js:4268` | ✅ |
| `GET /api/auto-publish/analytics/:id` | `server.js:4292` | ✅ |
| `POST /api/tiktok/publish/jobs/:id/execute` | `server.js:2220` | ✅ |

### 5.2 前端渲染与后端返回格式匹配

| 字段 | 后端返回 | 前端使用 | 状态 |
|---|---|---|---|
| `m.autoPublishConfig.enabled` | `getMatrixAutoPublishConfig`: `boolean`（`db:1877`）；列表回退：`0`（数字，`server:4187`）；GET 单条回退：`false`（布尔，`server:4216`） | `cfg.enabled === 1 \|\| cfg.enabled === true`（`app:8004`） | ✅ 兼容 |
| `a.creatorCount` | `server.js:4192`: `number` | `app.js:8088`: `a.creatorCount ?? 0` | ✅ 匹配 |
| `m.profileId` | `server.js:4202`: `string \| null` | `app.js:8006`: `!!m.profileId` | ✅ 匹配 |
| `m.autoPublishConfig.lastMonitorAt` | `getMatrixAutoPublishConfig`: `string \| null`（`db:1881`） | `app.js:8334`: `m.autoPublishConfig?.lastMonitorAt` | ✅ 匹配 |
| `t.matrixName` | `listPipelineTasks`: `string \| null`（`db:2097`） | `app.js:8231`: `t.matrixName \|\| t.matrixId \|\| '—'` | ✅ 匹配 |
| `t.profileId` | `listPipelineTasks`: `string`（`db:2103`） | `app.js:8233`: `t.profileId \|\| '—'` | ✅ 匹配 |
| `t.publishJobId` | `listPipelineTasks`: `string \| null`（`db:2104`） | `app.js:8240`: `t.publishJobId` | ✅ 匹配 |
| `t.sourceVideoId` | `listPipelineTasks`: `string`（`db:2100`） | `app.js:8245`: `t.sourceVideoId \|\| '—'` | ✅ 匹配 |
| `t.attemptCount` | `listPipelineTasks`: `number`（`db:2107`） | `app.js:8234`: `t.attemptCount ?? 0` | ✅ 匹配 |

### 5.3 ⚠️ 前端事件绑定完整性

| 事件 | 绑定位置 | 状态 |
|---|---|---|
| 矩阵卡片点击 → 打开弹窗 | `app.js:8027-8033` | ✅ |
| 自动发布开关 change | `app.js:8036-8041` | ✅ |
| 弹窗遮罩点击关闭 | `app.js:7827-7831` | ✅ |
| 弹窗 × 按钮关闭 | `app.js:7832` + `index.html:1640` 内联 | ✅ 双触发 |
| 配置按钮 → 配置页 | `app.js:8058-8060` | ✅ |
| 立即监控按钮 | `app.js:8062-8064` | ✅ |
| 保存配置按钮 | `app.js:8142-8175` | ✅ |
| 返回账号列表按钮 | `app.js:8177-8179` | ✅ |
| 时间段保存按钮 | `app.js:8181-8198` | ✅ |
| 单字段自动保存（preset/daily/interval/cdp） | `app.js:8200-8211` | ✅ |
| 流水线重试按钮 | `app.js:8270-8272` | ✅ |
| 立即发布按钮 | `app.js:8275-8277` | ✅ |
| 日志按钮 | `app.js:8280-8282` | ✅ |
| 刷新矩阵/流水线/监控/日志按钮 | `app.js:7815-7835` | ✅ |
| 矩阵过滤器 change | `app.js:7818-7821` | ✅ |
| 状态过滤器 change | `app.js:7822-7825` | ✅ |
| 日志过滤器 change | `app.js:7835` | ✅ |

事件绑定完整，无遗漏。

---

## 六、Payload 一致性（falsy 值）

### 6.1 ✅ dailyLimit = 0

- 前端 `app.js:8154`：`dailyLimit: parseInt(dailyInp?.value, 10) || 0` — 输入 0 时 payload 为 `0`。
- 前端 `app.js:8204`：单字段自动保存 `parseInt(inp.value, 10) || 0` — 输入 0 时 payload 为 `0`。
- 后端 `upsertMatrixAutoPublishConfig`：`dailyLimit !== undefined`（`db:1908`）→ `0 !== undefined` → true → 存 0。
- INSERT 路径（`db:1932`）：`dailyLimit !== undefined ? dailyLimit : 3` → 传 0 用 0。**已修复**。

### 6.2 ✅ enabled = 0 / false

- 前端 `app.js:7913`：`enabled: enabled ? 1 : 0` — 关闭时传 `0`。
- 后端：`enabled !== undefined`（`db:1906`）→ `0 !== undefined` → true → `enabled ? 1 : 0` = 0。**正确**。

### 6.3 ✅ publishTimeSlots = null（清空）

- 前端 `app.js:8169, 8187`：清空时传 `null`。
- 后端：`publishTimeSlots !== undefined`（`db:1911`）→ `null !== undefined` → true → 存 null。**已修复**。

### 6.4 ✅ cdpInstanceId / presetId = null（清空）

- 前端 `app.js:8153, 8156, 8201, 8210`：清空 select 时传 `null`。
- 后端：`!== undefined`（`db:1907, 1915`）→ true → 存 null。**已修复**。

### 6.5 ⚠️ monitorIntervalHours = 0 被 `|| 1` 吞掉

**【低危】** — `app.js:8155, 8207`

- `monitorIntervalHours: parseInt(intervalInp?.value, 10) || 1` — 输入 0 时 `parseInt("0") || 1` = 1。
- 用户无法设置监控间隔为 0（虽然 0 间隔无意义，但 payload 一致性上 falsy 值被吞）。
- 实际影响极小（0 间隔会导致每分钟都监控，不合理），但与其他字段的 `|| 0` 处理不一致。

---

## 七、迁移脚本幂等性

### 7.1 ✅ 重启安全

- `CREATE TABLE IF NOT EXISTS`（`db:628`）：重启不报错。
- `INSERT OR IGNORE`（`db:687, 759`）：重复执行不重复插入。
- `exists` 检查（`db:708-711, 752-755`）：跳过已迁移行。
- `#ensureColumn`（`db:783-786`）：检查列是否存在，已存在则跳过。
- try-catch 包裹（`db:621, 770`）：迁移失败不阻止启动。

### 7.2 ✅ 重建检测幂等

- `database.js:656`：检测旧约束用 `mpSchema.sql.includes("matrix_id, profile_id")`。
- 重建后新表 SQL 含 `UNIQUE (matrix_id)`（`db:665`）不含旧子串 `matrix_id, profile_id` → 再次启动时跳过重建。**幂等**。

### 7.3 ✅ 补建 pipeline 幂等

- `_createPipelineForExistingVideos`（`scheduler:280`）去重检查 `source_video_id + profile_id`（`db:308`），重启不会重复创建。

---

## 八、调度器与 TiktokPublishManager 交互

### 8.1 ✅ 交互正确

- **AutoPublishScheduler**（60 秒主循环，`scheduler:82-88`）：
  - 监控 → 下载 → 创建 pipeline → 触发混剪 → 检测完成 → 排期（创建 `tk_publish_job` status=pending）→ 检测发布结果
  
- **TiktokPublishManager**（30 秒轮询，`manager:12`）：
  - 查 pending jobs → `executeJob` → 开浏览器 → 上传发布 → 写 `status=success/failed` → 采集播放量

- **通信机制**：通过 `tk_publish_jobs.status` 字段。调度器写 `pending`（`scheduler:638`），PublishManager 写 `success/failed`（`manager:101, 134`），调度器读 `success/failed`（`scheduler:813, 824`）。

- **无竞争**：两者操作不同的表/行，调度器只读 `tk_publish_jobs.status`，PublishManager 只写 `tk_publish_jobs.status`。

### 8.2 ✅ 发布执行器 30 秒轮询 pending job

- `tiktok-publish-manager.js:12`：`startScheduler(intervalMs = 30000)` — 默认 30 秒。
- `server.js:404`：`tiktokPublisherManager.startScheduler()` — 用默认 30 秒。
- `checkAndExecutePendingJobs`（`manager:30`）：查 `status: "pending"` 的 jobs（`manager:33`），检查 `scheduledAt <= now`（`manager:39`），执行第一个（`manager:43` break）。
- 并发限制：`runningJobIds.size > 0` 则跳过（`manager:36`），同时只执行一个发布任务。

### 8.3 ✅ 播放量在 executeJob 成功后采集

- `tiktok-publish-manager.js:99-128`：`result.ok` → 更新 `status: "success"`（line 100-104）→ `recordAnalytics()`（line 110）→ INSERT `tk_video_analytics`（line 116-124）。
- 采集失败不阻止发布结果（catch 记 warning，line 126-128）。

### 8.4 ⚠️ 手动触发发布与自动执行可能并发

**【低危】** — `server.js:2223` vs `manager:39-40`

前端"立即发布"按钮（`app.js:7971`）调 `POST /api/tiktok/publish/jobs/:id/execute`（`server:2220`），直接调 `tiktokPublisherManager.executeJob(jobId)`（`server:2223`）。
同时 PublishManager 的 30 秒轮询也可能选中同一个 pending job 执行。
但 `executeJob` 开头有 `if (this.runningJobIds.has(jobId)) throw new Error("该任务正在执行中")`（`manager:49`）防重入。**有保护**，但手动触发后 30 秒轮询会报错记日志（无害但产生噪音日志）。

---

## 九、问题汇总（按严重程度）

### 🔴 高危（新发现）

1. **listPipelineTasks JOIN 产生重复行**（3.1）— 矩阵多账号时 pipeline 任务重复显示

### 🟡 中危（新发现）

2. **recordAnalytics 记录全部主页视频到同一 jobId**（3.2）— 播放量数据语义错误
3. **likes/comments/shares 始终为 0**（3.3）— 只抓播放量，不抓其他指标

### 🟡 中危（上一轮遗留，未恶化）

4. **GET 单条配置回退字段不完整**（2.5）— 缺 lastMonitorAt/ratio/cdpInstanceId 等
5. **enabled 类型不统一**（列表回退给数字 0，Store 给布尔，GET 单条回退给布尔 false）

### 🟢 低危（新发现）

6. **getPipelineTask 缺 matrixId/sourceUrl 字段**（3.4）— 与 listPipelineTasks 字段不一致
7. **状态过滤器缺 'scheduled' 选项**（3.5）— 用户无法过滤已排期任务
8. **statusClass 缺 'scheduled' 样式**（3.6）— scheduled 状态显示为 pending 样式
9. **executeJob 中 username 为死代码**（3.7）— 无效数据库查询
10. **模态框内联 onclick 与 addEventListener 并存**（3.8）— 冗余但功能正确
11. **fetchPipelineTasks 回退解析 data.tasks**（3.9）— 冗余但无害
12. **updatePipelineTask 用 `!== null` 无法清空 failReason**（3.10）— 成功 pipeline 保留旧失败原因（用户不可见）
13. **monitorIntervalHours = 0 被 `|| 1` 吞掉**（6.5）— 无法设 0（影响极小）

### 🟢 低危（上一轮遗留，未恶化）

14. **调度器文件头注释未更新**（2.1）— 仍引用旧表名
15. **_ensureSchema 重复建表**（2.2）— 与 database.js 重复
16. **matrix_id 回退查询用 LIMIT 1**（2.3）— 多矩阵时取不确定值
17. **表头缺"平台账号"列**（2.6）— 数据有但未展示
18. **手动触发发布与自动执行可能并发**（8.4）— 有防重入保护但产生噪音日志

### ✅ 已修复（上一轮 5 个高危 + 6 个中危，全部确认）

19. 迁移脚本 try-catch（1.1）
20. 迁移顺序矛盾（1.2）
21. cdp_instance_id 从 matrix_profiles 查（1.3）
22. 混剪 API 返回格式（1.4）
23. dailyLimit=0 INSERT 路径（1.5）
24. publishTimeSlots/cdpInstanceId/presetId 清空（1.6）
25. 前端 creatorCount 字段（1.7）
26. 前端 lastMonitorAt 路径（1.8）
27. 前端 newVideoCount（1.9）
28. 旧 API 路由删除（1.10）
29. 旧 Store 方法删除（1.11）
30. createPipelineTask 补列（1.12）
31. 发布执行器存在（1.13）
32. 播放量采集（1.14）
33. pipeline 状态匹配（1.15）
34. 模态框 selectedMatrixId 清除（1.16）

---

## 十、建议修复优先级

### 1. 立即修复（影响数据正确性）

- **listPipelineTasks JOIN 重复行**（#1）：改为子查询取 `LIMIT 1` 的账号，或应用层按 `p.id` 去重，否则用户看到重复任务
- **recordAnalytics 全部视频关联同一 jobId**（#2）：过滤只记录 `publishedVideoId` 对应的视频

### 2. 尽快修复（数据完整性）

- **likes/comments/shares 采集**（#3）：增加抓取或明确标注不采集
- **GET 单条配置回退补全字段**（#4）：回退对象补 lastMonitorAt/ratio/cdpInstanceId 等
- **状态过滤器补 scheduled**（#7）+ **statusClass 补 scheduled**（#8）
- **updatePipelineTask 改用 `!== undefined`**（#12）：使 failReason 可清空

### 3. 后续优化

- **getPipelineTask 补字段**（#6）
- **删除 executeJob 死代码**（#9）
- **移除内联 onclick**（#10）
- **更新调度器注释**（#14）
- **enabled 类型统一**（#5）

---

**审查结论：** 上一轮的 5 个高危和 6 个中危问题**全部已修复并验证生效**。端到端流程（监控→下载→混剪→排期→发布→记录播放量）链路完整，调度器与 TiktokPublishManager 的交互正确（30 秒轮询 pending job，播放量在 executeJob 成功后采集）。迁移脚本幂等性合格，重启安全。前端事件绑定完整，前后端 API 路由和字段格式匹配。但本轮发现 3 个新问题：listPipelineTasks 的 JOIN 重复行（高危）、播放量采集范围错误（中危）、likes/comments/shares 未采集（中危）。建议优先修复 #1 和 #2 后再上线。
