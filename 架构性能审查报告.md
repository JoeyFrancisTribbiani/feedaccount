# 架构性能审查报告

> **项目**: feedaccount (D:/WILLLUXE/yix-repo/feedaccount)
> **审查日期**: 2026-09-19
> **审查范围**: 高负载（大量下载/混剪/发布任务）下的性能瓶颈与扩展性
> **审查文件**: `src/server.js`、`src/database.js`、`src/auto-publish-scheduler.js`、`src/tiktok/tiktok-publish-manager.js`、`src/video-remix.js`
> **说明**: 本报告仅作问题分析，不修改代码。影响程度分三级：🔴 严重 / 🟡 中等 / 🟢 轻微

---

## 一、总体架构概览

| 组件 | 实现 | 并发模型 |
|---|---|---|
| HTTP 服务 | 原生 `node:http` `createServer`，单线程事件循环 | 所有请求串行进入同一个 event loop |
| 数据库 | `node:sqlite` `DatabaseSync`（同步 API），WAL 模式 | 同步阻塞，单连接，单写入者 |
| 自动发布调度器 | `AutoPublishScheduler`，`setInterval(60s)` 顺序 6 步 | `running` 互斥锁，单实例单循环 |
| TikTok 发布管理器 | `TiktokPublishManager`，`setInterval(30s)` | `runningJobIds.size > 0` 时直接 return，**严格串行单任务** |
| AI 混剪队列 | `aiRemixQueue`，`AI_REMIX_MAX_CONCURRENT = 1` | 串行单任务，启动间隔 60s |
| 普通混剪队列 | `remixQueue`，`remixProcessing` 布尔锁 | 串行单任务 |
| 下载 | `processSingleTiktokDownload` → TikWM/yt-dlp | 无显式并发限制（batch-download 串行 +2s 间隔） |
| FFmpeg | `execFile("ffmpeg")` 子进程，30 分钟超时 | 单进程，`-threads 0`（用满所有 CPU 核心） |
| 发布执行器 | Playwright `connectOverCDP` + BitBrowser | 串行单任务，无并发 |

**核心结论**: 整个系统是一个**单进程、单线程、单数据库连接、严格串行执行**的架构。在低负载（个位数任务）下运行良好，但高负载（100+ 下载、10+ 混剪、多矩阵并发发布）下会出现严重的吞吐量瓶颈和事件循环阻塞。

---

## 二、瓶颈详细分析

### 🔴 问题 1: SQLite 同步 API 阻塞事件循环

**位置**: `src/database.js` 全文（123 个同步方法，0 个异步方法）

**分析**:
`node:sqlite` 的 `DatabaseSync` 是**同步 API**——每次 `.prepare().all()` / `.get()` / `.run()` / `.exec()` 调用都会**阻塞 Node.js 事件循环**，直到 SQLite 返回结果。这意味着：

1. **查询期间，HTTP 服务无法处理任何其他请求**（包括 SSE 心跳、前端轮询、下载 API）
2. **查询期间，所有 `setInterval` 定时器无法触发**（调度器、发布管理器的轮询会被推迟）

**具体瓶颈点**:

| 查询场景 | 代码位置 | 阻塞风险 |
|---|---|---|
| `getStats()` — 全表 `SUM/COALESCE` 聚合 `task_runs` | `database.js:1131-1172` | 🟡 中等（前端每 10s 轮询 `/api/stats` 触发） |
| `listPipelineTasks()` — 6 表 LEFT JOIN + 子查询 | `database.js:2107-2129` | 🔴 严重（pipeline 行数多时 JOIN 很慢，且前端轮询频繁） |
| `listTkPublishJobs()` — 4 表 LEFT JOIN | `database.js:1790-1800` | 🟡 中等 |
| `_createPipelineForExistingVideos()` — N×M 循环查询 | `auto-publish-scheduler.js:292-353` | 🔴 严重（每个矩阵 × 每个达人 × 每个视频 = 3 层嵌套同步查询） |
| `_calcNextPublishTimeWithSlots()` — `listTkPublishJobs({limit:500})` | `auto-publish-scheduler.js:786` | 🟡 中等（每次排期都全量查 500 条） |
| 迁移期 `#migrate()` 的 `ALTER TABLE` 循环 | `database.js:463-617` | 🟢 轻微（仅启动时一次） |

**最严重场景**: 下载 1000 个视频后，`_createPipelineForExistingVideos()` 会执行：
```
矩阵数 × 达人数 × 视频数 次同步 SELECT + INSERT
```
假设 5 个矩阵 × 10 个达人 × 100 个视频 = **5000 次同步数据库操作**，期间事件循环完全冻结。

**建议**:
- 将 `DatabaseSync` 替换为 `node:sqlite` 的异步 wrapper（worker_threads），或迁移到 `better-sqlite3` + worker pool
- 短期方案：对高频查询（`getStats`、`listPipelineTasks`）增加内存缓存（TTL 5-10s），减少同步查询频率
- `_createPipelineForExistingVideos()` 改为单条批量 SQL（`INSERT ... SELECT ... WHERE NOT EXISTS`），消除 N×M 循环

---

### 🔴 问题 2: HTTP 服务并发能力——单线程串行路由

**位置**: `src/server.js:1346` `createServer(async (request, response) => { ... })`

**分析**:
原生 `node:http` 的 `createServer` 回调是**同步串行匹配路由**的——5152 行代码中，路由通过一长串 `if (pathname === "/api/...")` 逐一匹配。这意味着：

1. **每个请求都要从头遍历所有路由 if 链**（约 60+ 个路由分支），匹配越靠后的路由延迟越高
2. **路由处理中的同步 DB 查询会阻塞所有其他请求**（见问题 1）
3. **`readJson(request)` 使用 `for await (const chunk of request)` 收集 body**（`server.js:284`），这是异步的，但 body 累积完成前的同步 DB 调用仍会阻塞

**高负载场景**:
- 前端 10s 轮询 × 多个页面同时打开（统计页 + 混剪页 + 发布页 + pipeline 页）
- SSE 长连接持有事件循环（`server.js:1934-1948`），`heartbeat` 每 15s 写一次
- 下载 API（`/api/tiktok/download`）单次请求可能耗时 30s-3min（TikWM + fetch 下载）
- 混剪 API（`/api/remix/ai-remix-task`）提交后立即返回，但后台 `processSingleAiRemixTask` 会长时间运行

**具体瓶颈**: 
- SSE 客户端通过 `sseClients.set(response, heartbeat)` 持有（`server.js:1943`），**每个 SSE 连接的 `response.write` 是同步的**，如果客户端 TCP 窗口满，`write` 会背压阻塞事件循环
- `broadcast` 函数（`server.js:1328-1340`）遍历所有 SSE 客户端同步写入，N 个客户端 = N 次同步 write

**建议**:
- 短期：将路由匹配改为 `Map` 查找（O(1)）而非线性 `if` 链（O(n)）
- 中期：考虑迁移到 Fastify（内置路由树 + 异步非阻塞）
- SSE broadcast 改为异步 `response.write` + 背压处理（检查 `response.writableNeedDrain`）

---

### 🔴 问题 3: 调度器 60s 主循环——6 步顺序执行导致延迟累积

**位置**: `src/auto-publish-scheduler.js:78-92`

**分析**:
```javascript
async checkAndProcess() {
    if (this.running) return;  // 互斥锁
    this.running = true;
    try {
      await this.monitorMatrixVideos();        // 步骤1: 网络请求 TikWM + CDP 解析，可能 30s-3min
      await this._createPipelineForExistingVideos(); // 步骤1.5: N×M 同步 DB 循环
      await this.triggerRemixTasks();           // 步骤2: 触发混剪（fetch 到 server 自身）
      await this.checkRemixComplete();          // 步骤3: 查 remix_tasks 状态
      await this.schedulePublishJobs();         // 步骤4: 创建 publish jobs
      await this.checkPublishResults();          // 步骤5: 查 publish jobs 状态
      await this.retryFailedTasks();            // 步骤6: 重试混剪
    } finally {
      this.running = false;
    }
}
```

**瓶颈**:
1. **步骤 1（`monitorMatrixVideos`）耗时最长**：每个矩阵 × 每个达人调用 `fetch('/api/tiktok/parse-profile')`，该 API 内部通过 CDP 打开 TikTok 主页、滚动加载 50 次（每次等 4s），单次解析 3-5 分钟。如果 5 个矩阵 × 10 个达人 = **50 次 CDP 解析，总耗时可达 2.5-4 小时**
2. **步骤 1 阻塞步骤 2-6**：因为 `running` 互斥锁，步骤 1 没完成，后续步骤不会执行。意味着：混剪触发、排期发布、失败重试全部被延迟
3. **`this.running` 锁在 `monitorMatrixVideos` 网络请求期间一直持有**，如果该步异常卡住（如 CDP 连接超时 180s），整个调度器冻结

**具体瓶颈点**:
- `_checkCreatorNewVideosForMatrix()` → `fetch('/api/tiktok/parse-profile')`，`AbortSignal.timeout(180000)` = 3 分钟超时（`auto-publish-scheduler.js:188`）
- `_downloadAndCreatePipelineForMatrix()` → `fetch('/api/tiktok/download')`，`AbortSignal.timeout(180000)` = 3 分钟超时（`auto-publish-scheduler.js:250`）
- 下载间隔 `await new Promise(r => setTimeout(r, 2000))`（`auto-publish-scheduler.js:241`），100 个新视频 = 200s 纯等待

**建议**:
- 将 6 步拆分为独立的 `setInterval`（各自有自己的频率），而非串行在一个 60s 循环里
- `monitorMatrixVideos` 改为异步并发（`Promise.all` 并行多个矩阵的解析），而非 `for` 循环串行
- `triggerRemixTasks` / `checkRemixComplete` / `schedulePublishJobs` / `checkPublishResults` / `retryFailedTasks` 可以独立 30s 轮询，不依赖监控步骤完成

---

### 🟡 问题 4: yt-dlp 子进程与下载并发

**位置**: `src/server.js:3284` `processSingleTiktokDownload` + `3566` `/api/tiktok/batch-download`

**分析**:
- `processSingleTiktokDownload` 调用 `getTiktokVideoData(tiktokUrl)`（TikWM API 或 yt-dlp 子进程）
- `batch-download` 是**串行 for 循环** + 2s 间隔（`server.js:3572-3590`），无并发
- 单次下载通过 `fetch(tkData.play)` 下载视频流，**无内存限制**——大视频（100MB+）会通过 `fetch().arrayBuffer()` 全部加载到内存

**瓶颈**:
1. **批量下载串行**：100 个视频 × (下载 5-30s + 间隔 2s) = 700s-3200s（12-53 分钟）
2. **`downloadTiktokVideo` 函数将整个视频读入内存**（`Buffer.from(await res.arrayBuffer())`），并发下载 10 个 50MB 视频 = 500MB 内存
3. 自动发布调度器中 `_downloadAndCreatePipelineForMatrix` 也调用 `/api/tiktok/download`，与手动批量下载**争抢同一事件循环**

**建议**:
- 批量下载改用 `Promise.all` + 并发限制（如 `p-limit`，并发 3-5）
- 大文件下载改为流式写入（`response.body.pipeTo(writeStream)`），避免全量读入内存

---

### 🟡 问题 5: FFmpeg 混剪——CPU/磁盘 IO 瓶颈

**位置**: `src/video-remix.js`

**分析**:
- `runFfmpeg` 使用 `execFile("ffmpeg", ["-threads", "0", ...args])`（`video-remix.js:56`），`-threads 0` = **使用所有 CPU 核心**
- 混剪并发 = 1（`AI_REMIX_MAX_CONCURRENT = 1`，`server.js:481`），但**单次混剪会占满所有 CPU 核心**
- 单次 AI 混剪流程：
  1. `processSingleVideo`（去重）→ 1 次 ffmpeg
  2. `normalizeSegment`（intro/outro 归一化）→ 最多 2 次 ffmpeg
  3. `concatVideos`（拼接）→ 1 次 ffmpeg + 可能 1 次重编码 fallback
  4. `mixBackgroundMusic`（背景音乐）→ 1 次 ffmpeg
  5. `overlayImagesOnVideo`（图片覆盖）→ 1 次 ffmpeg
  6. `concatWithTransition`（转场）→ N 次 ffmpeg 归一化 + 1 次 xfade
- **单次混剪 = 5-10 次 ffmpeg 子进程调用**，每次 10s-2min，总计 1-10 分钟

**瓶颈**:
1. **混剪期间 CPU 满载**，影响 HTTP 服务响应（事件循环虽不被 ffmpeg 阻塞，但 CPU 竞争导致 Node.js 调度延迟）
2. **磁盘 IO 瓶颈**：混剪中间文件（`TEMP_DIR`）+ 下载文件 + 成品文件同时读写，单 SATA SSD 随机 IO 带宽有限
3. `runFfmpeg` 的 `maxBuffer: 20 * 1024 * 1024`（20MB）可能在 stderr 输出过大时截断
4. **临时文件清理依赖 `finally` 块**（`video-remix.js:417-420`），如果进程异常退出，`TEMP_DIR` 会积累垃圾文件

**建议**:
- 限制 ffmpeg 线程数：`-threads 2` 或 `-threads 4`，留出 CPU 给 Node.js
- 混剪任务从并发 1 提升到 2-3（如果 CPU 核心数足够），用 `p-limit` 控制
- 临时文件清理增加定时任务（每小时清理 `TEMP_DIR` 中超过 1 小时的文件）

---

### 🟡 问题 6: 发布执行器——Playwright 内存与连接管理

**位置**: `src/tiktok/tiktok-publish-manager.js` + `src/tiktok/tiktok-publisher.js`

**分析**:
- 发布管理器 `startScheduler(30000)` 每 30s 轮询一次 `listTkPublishJobs({status:"pending", limit:20})`（`tiktok-publish-manager.js:37`）
- `runningJobIds.size > 0` 时直接 return（`tiktok-publish-manager.js:40`），**严格串行单任务**
- 每次发布：
  1. `bitBrowserApi.openProfile(profileId)` → 打开 BitBrowser 实例
  2. `new TiktokPublisher()` → `chromium.connectOverCDP(httpUrl)` 连接
  3. `publisher.connect(wsUrl)` → 导航到 TikTok Studio，等待弹窗，创建 CDP session
  4. `publisher.uploadVideo()` → 上传视频、输入标题、选隐私、点发布
  5. `publisher.recordAnalytics()` → 访问账号主页抓取播放量
  6. `publisher.close()` → `this.browser.close()`（**注释说"不应该关闭浏览器"但代码调用了 close**）

**瓶颈与风险**:
1. **`tiktok-publisher.js:571` 的 `close()` 调用 `this.browser.close()`**，但注释（`L569`）说"connectOverCDP 不应该关闭浏览器，只断开连接"——**代码与注释矛盾**。`browser.close()` 在 connectOverCDP 模式下会关闭 Playwright 连接但可能不关闭 BitBrowser 实例，但如果 Playwright 内部创建了额外 context/page，这些不会被清理
2. **CDP session 未显式 disconnect**：`this.cdpSession = await this.context.newCDPSession(page)`（`L65`）创建的 CDP session 在 `close()` 中**没有 `cdpSession.detach()` 调用**，可能导致 CDP 连接泄漏
3. **`recordAnalytics` 是额外的浏览器导航**（`L500`: `page.goto(profileUrl)`），延长了发布任务的持有时长，增加内存占用
4. **iOS Farm 模式的轮询循环**：`_executeViaIosFarm` 中有两个 `while` 轮询（`L220-234` 等待 execution + `L247-271` 轮询状态），每个 `setTimeout(2000)` 间隔，最长等 10 分钟。期间 `runningJobIds` 持有 jobId，其他任务全部排队

**内存泄漏风险**: 🟡 中等
- `chromium.connectOverCDP` 返回的 browser 对象在 `close()` 后理论上会被 GC，但如果 BitBrowser 端的 WebSocket 连接没有正确关闭，Playwright 内部可能保留引用
- 多次发布（10+ 次/天）后，如果 Playwright 连接未完全清理，可能累积内存

**建议**:
- `close()` 方法应先 `cdpSession.detach()`，再 `browser.close()`
- 发布后 `recordAnalytics` 改为异步后台执行，不阻塞发布任务完成
- iOS Farm 轮询改为基于事件的回调（如果 Farm 支持 webhook），而非 `while + setTimeout` 占用 `runningJobIds`

---

### 🟡 问题 7: 前端 10s 轮询——数据库查询放大

**位置**: 前端轮询调用多个 API（推断自路由表）

**分析**:
前端每 10s 轮询一次，典型页面会调用：

| 页面 | 调用的 API | 对应 DB 查询 |
|---|---|---|
| 统计面板 | `GET /api/stats` | `getStats()` — 1 次全表聚合 + 2 次 COUNT |
| 历史记录 | `GET /api/history?limit=100` | `listRuns()` — 1 次 SELECT * + 100 次 `#publicRun` 映射 |
| 日志 | `GET /api/logs?limit=200` | `listEvents()` — 1 次 JOIN SELECT + 200 次映射 |
| 混剪任务 | `GET /api/remix/tasks` | `listRemixTasks()` — 1 次 SELECT *（**无 LIMIT！**）|
| 自动发布 | `GET /api/auto-publish/pipeline` | `listPipelineTasks()` — 6 表 JOIN |
| 自动发布矩阵 | `GET /api/auto-publish/matrices` | `listMatrices()` + 每个矩阵 N 次子查询 |
| 发布任务 | `GET /api/tiktok/publish/jobs` | `listTkPublishJobs()` — 4 表 JOIN |

**瓶颈**:
1. **`listRemixTasks()` 无 LIMIT**（`database.js:2640`）——如果积累了 1000+ 混剪任务，每次轮询都全量返回，JSON 序列化 + 网络传输耗时
2. **`/api/auto-publish/matrices` 有 N+1 查询问题**（`server.js:4267-4286`）：`listMatrices()` → 每个矩阵 `getMatrixAutoPublishConfig` + `listMatrixAccounts` + 每个账号 `getMatrixAccountCreators`，M 个矩阵 × A 个账号 = M×A 次同步查询
3. 假设 5 个页面同时轮询 × 每 10s 一次 × 每次 5-7 个 API = **每 10s 触发 25-35 次同步 DB 查询**

**建议**:
- `listRemixTasks()` 增加 `LIMIT` 参数（默认 100，最多 500）
- `/api/auto-publish/matrices` 改为单条 SQL 一次性 JOIN 出所有数据
- 前端轮询改为 SSE 推送（已有 `/api/events` SSE 基础设施），仅初始加载时全量查询

---

### 🔴 问题 8: SQLite 单写入者锁——大量写入导致锁竞争

**位置**: `src/database.js`（全局单 `DatabaseSync` 实例）

**分析**:
SQLite 在 WAL 模式下支持**多读单写**——多个读操作可以并发，但**同一时刻只能有一个写操作**。所有写入操作通过 `db.prepare().run()` 同步执行，如果两个写入同时到达：

1. SQLite 返回 `SQLITE_BUSY` 错误
2. `DatabaseSync` 没有 `busy_timeout` 设置（代码中未看到 `PRAGMA busy_timeout`），默认 0 = 立即报错

**高负载写入来源**:

| 写入场景 | 频率 | 表 |
|---|---|---|
| 下载视频记录 | 每个视频 1-3 次 UPDATE/INSERT | `remix_videos` |
| 混剪任务状态更新 | 每次状态变更 1 次 UPDATE | `remix_tasks` |
| 混剪中间日志 | 每个 ffmpeg 步骤可能写日志 | `cdp_logs` |
| 发布任务状态 | 每次状态变更 1 次 UPDATE | `tk_publish_jobs` |
| pipeline 状态 | 每次状态变更 1 次 UPDATE | `auto_remix_publish_pipeline` |
| 调度器日志 | 每个步骤写日志 | `cdp_logs` |
| 发布器日志 | 每个操作写日志 | `cdp_logs` |
| 事件日志 | 每个滚动/操作写事件 | `task_events`, `tiktok_events` |
| 播放量记录 | 每次发布后 N 条 INSERT | `tk_video_analytics` |

**瓶颈**:
- 自动发布调度器 60s 循环 + 发布管理器 30s 循环 + 前端 10s 轮询的写入可能同时到达
- `cdp_logs` 表是最大的写入热点——调度器、发布器、混剪器都往这里写日志，且没有清理策略
- **`upsertProfiles`（`database.js:819-851`）使用 `BEGIN/COMMIT` 事务**，如果 20 个 BitBrowser 实例同时上报心跳，事务持有写锁期间其他写入全部阻塞

**建议**:
- 启动时设置 `PRAGMA busy_timeout = 5000`（5 秒等待重试）
- `cdp_logs` 增加定时清理（保留最近 7 天或最近 10000 条）
- 考虑将日志写入从 SQLite 迁移到文件（`pino` + `rotating-file`）

---

### 🟡 问题 9: 内存——大量视频文件信息累积

**位置**: 多处

**分析**:
1. **`listRemixTasks()` 无 LIMIT 全量返回**（`database.js:2640`）——每次调用都把所有 remix_tasks 行加载到内存，140+ 任务 × 每行 20+ 字段（含 JSON）≈ 数 MB。如果积累到 1000+ 任务，每次 API 调用都分配/释放数 MB 内存，增加 GC 压力
2. **`listRemixVideos(creatorId)` 也无 LIMIT**（`database.js:2598`）——某达人下 500+ 视频时全量返回
3. **`_calcNextPublishTimeWithSlots` 调用 `listTkPublishJobs({limit: 500})`**（`auto-publish-scheduler.js:786`）——每次排期都加载 500 条 publish jobs 到内存做时间比较，可以用 `SELECT MAX(scheduled_at)` 替代
4. **`listTkMaterials` / `listTkPublishJobs` 等列表查询无缓存**
5. **SSE `broadcast` 将 JSON 序列化后的字符串写入所有客户端**（`server.js:1328-1340`），如果有 10 个 SSE 客户端，每次状态变更都 10 次 `JSON.stringify` + 10 次 `response.write`

**内存占用估算**（140 任务场景）:
- remix_tasks 全量：~2-5 MB
- pipeline_tasks 全量（6 表 JOIN）：~5-10 MB
- SSE broadcast 帧序列化：~100KB × 客户端数
- 下载视频 Buffer（单个 50MB）：并发 5 个 = 250 MB

**建议**:
- 所有 `list*` 方法增加 `limit/offset` 分页
- `_calcNextPublishTimeWithSlots` 改用 `SELECT MAX(scheduled_at) FROM tk_publish_jobs WHERE profile_id = ?` 替代全量加载
- SSE broadcast 先序列化一次，再写入所有客户端（当前是每次 `broadcast` 都 `JSON.stringify`）

---

### 🟡 问题 10: 磁盘 IO——混剪中间文件 + 下载 + 成品同时读写

**位置**: `src/video-remix.js`（TEMP_DIR, OUTPUT_DIR）+ 下载目录

**分析**:
单次 AI 混剪的磁盘 IO 模式：

| 步骤 | 操作 | 文件 |
|---|---|---|
| 去重 | 读原视频 → 写 `remix_deduped_*.mp4` | 1 读 + 1 写 |
| intro 归一化 | 读 intro → 写 `remix_intro_*.mp4` | 1 读 + 1 写 |
| outro 归一化 | 读 outro → 写 `remix_outro_*.mp4` | 1 读 + 1 写 |
| 拼接 | 读 3 个片段 → 写 `remix_concat_*.mp4` | 3 读 + 1 写 |
| 背景音乐 | 读 concat + 读 music → 写输出 | 2 读 + 1 写 |
| 图片覆盖 | 读视频 + 读 N 张图 → 写 overlay | 1+N 读 + 1 写 |
| 转场归一化 | 读 N 片段 → 写 N 个 norm | N 读 + N 写 |
| 转场拼接 | 读 N norm → 写 concat | N 读 + 1 写 |
| 最终输出 | 复制到 OUTPUT_DIR | 1 读 + 1 写 |

**单次混剪 = 约 15-25 次文件读写**，每个文件 10-100MB。

**并发场景**:
- 自动发布调度器触发下载（写 `remix-videos/`）同时
- AI 混剪在 `remix-tmp/` 读写中间文件同时
- 前端请求 `/data/remix-output/` 读取成品视频

如果这三个目录在**同一块磁盘**上，磁头频繁寻道，IO 吞吐量急剧下降。

**建议**:
- 将 `TEMP_DIR`（中间文件）放在独立磁盘（如 SSD）
- `OUTPUT_DIR`（成品）和 `UPLOAD_DIR`（下载）分开到不同磁盘
- 混剪完成后立即清理中间文件（当前 `finally` 块有清理，但 `concatWithTransition` 的 norm 文件清理在 `L974` 独立于 `tempPaths`）

---

## 三、瓶颈优先级排序与修复建议

| 优先级 | 问题 | 影响 | 建议修复方式 | 工作量 |
|---|---|---|---|---|
| P0 | SQLite 同步阻塞事件循环 | 所有请求和定时器被阻塞 | 迁移到 worker_threads 异步 SQLite 或 better-sqlite3 + worker pool | 大 |
| P0 | 调度器 6 步串行导致延迟累积 | 混剪/发布/重试延迟数小时 | 拆分为独立 setInterval | 中 |
| P1 | `listRemixTasks()` 等无 LIMIT 全量返回 | 内存/GC/网络压力 | 增加 limit/offset 分页 | 小 |
| P1 | `/api/auto-publish/matrices` N+1 查询 | 每次轮询 M×A 次同步 DB 查询 | 改为单条 JOIN SQL | 中 |
| P1 | `listPipelineTasks()` 6 表 JOIN 无缓存 | 前端轮询频繁时 DB 阻塞 | 增加内存缓存（TTL 5s） | 小 |
| P1 | SQLite 无 `busy_timeout` | 并发写入时报 SQLITE_BUSY | 启动时 `PRAGMA busy_timeout = 5000` | 极小 |
| P2 | 批量下载串行无并发 | 100 视频 = 12-53 分钟 | `Promise.all` + `p-limit(3)` | 小 |
| P2 | 下载大文件全量读入内存 | 并发下载内存爆炸 | 改为流式 `pipe(writeStream)` | 中 |
| P2 | FFmpeg `-threads 0` 占满 CPU | 影响 Node.js 调度 | 改为 `-threads 4` | 极小 |
| P2 | Playwright CDP session 未 detach | 内存泄漏风险 | `close()` 增加 `cdpSession.detach()` | 小 |
| P2 | `cdp_logs` 无清理策略 | 表无限增长 | 定时清理 >7 天日志 | 小 |
| P3 | SSE broadcast 每客户端序列化一次 | 客户端多时 CPU 浪费 | 先序列化一次再广播 | 极小 |
| P3 | 路由 if 链线性匹配 O(n) | 靠后路由延迟高 | 改为 Map 查找 | 中 |
| P3 | `_calcNextPublishTime` 全量加载 500 条 | 排期时内存/DB 压力 | 改用 `SELECT MAX(scheduled_at)` | 小 |

---

## 四、扩展性评估

### 当前架构的吞吐量上限（估算）

| 操作 | 单次耗时 | 并发 | 理论吞吐/小时 |
|---|---|---|---|
| 视频下载 | 5-30s | 串行 | 120-720 个 |
| AI 混剪 | 1-10 min | 1（串行） | 6-60 个 |
| 视频发布 | 2-5 min | 1（串行） | 12-30 个 |
| 主页解析 | 3-5 min | 串行（调度器内） | 12-20 个达人 |
| pipeline 状态查询 | 50-200ms | 受 DB 阻塞影响 | - |

### 扩展瓶颈

1. **垂直扩展受限**：单线程 Node.js 无法利用多核 CPU；SQLite 单写入者无法通过增加连接数提升写入吞吐
2. **水平扩展不支持**：所有状态在单进程内存（`remixQueue`、`aiRemixQueue`、`runningJobIds`、`activeDaemonTasks`），无法多实例部署
3. **Playwright/BitBrowser 强耦合**：发布任务依赖本地 BitBrowser + CDP 端口，无法分布式部署
4. **CDP daemon 单实例**：`chrome-cdp-daemon` 是本地子进程（`server.js:130` spawn），无法跨机器

### 推荐的扩展路径

**短期（1-2 周）**:
- 增加 `PRAGMA busy_timeout`
- `listRemixTasks` 等增加 LIMIT
- 调度器 6 步拆分为独立定时器
- FFmpeg 线程限制
- `cdp_logs` 定时清理

**中期（1-2 月）**:
- SQLite 迁移到 worker_threads 异步封装
- 下载/混剪/发布改为任务队列（如 BullMQ + Redis）
- 前端轮询改为 SSE 推送
- N+1 查询合并为 JOIN

**长期（3-6 月）**:
- 数据库迁移到 PostgreSQL（支持并发写入）
- 发布执行器拆分为独立微服务（支持多机器部署）
- CDP daemon 改为远程服务（支持多 Chrome 实例池）

---

## 五、结论

当前架构在**低负载**（< 50 任务/天、< 5 并发请求）下运行稳定，设计简洁有效。但在**高负载**（100+ 下载/混剪/发布任务、多矩阵并发、前端多页面轮询）下存在**三个严重瓶颈**：

1. **SQLite 同步 API 阻塞事件循环**——所有 DB 查询阻塞所有 HTTP 请求和定时器
2. **调度器 6 步串行**——监控步骤耗时数小时，阻塞后续所有步骤
3. **无 LIMIT 的全量查询**——内存/GC/网络压力随数据增长线性放大

这三个问题中，**SQLite 同步阻塞是根因**——它是单点瓶颈，影响所有其他组件。建议优先解决数据库层的异步化或缓存，再处理调度器拆分和查询优化。
