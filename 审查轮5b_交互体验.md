# 第二轮审查报告：前端交互完整性和用户体验验证

**项目**: feedaccount (D:/WILLLUXE/yix-repo/feedaccount)
**审查文件**: `public/app.js` + `public/index.html` + `public/styles.css`
**审查日期**: 2026-09-19
**审查范围**: 前端交互完整性、用户体验、用户流程、布局、状态显示、响应式、错误提示、加载状态、监控状态、旧CSS清理

---

## 严重程度说明

| 等级 | 含义 |
|------|------|
| 🔴 严重 | 功能不可用或流程断裂，用户无法完成操作 |
| 🟡 中等 | 功能可用但体验明显不佳，或存在易触发的边界问题 |
| 🟢 轻微 | 不影响核心功能，属于代码质量/可维护性问题 |
| ✅ 通过 | 该项检查通过 |

---

## 1. 用户流程完整性

**流程**: 打开页面 → 看到 auto-publish tab(发布历史) → 查看全部发布历史 → 点击矩阵 → 选账号 → 看该账号发布历史 → 查看日志 → 重试/重新发布

### 评估结果：✅ 基本畅通（有轻微问题）

**实现路径验证**:

1. **打开页面默认显示 auto-publish tab** ✅
   - `index.html` 第 83 行: `<button class="platform-tab active" data-platform="auto-publish">`
   - `app.js` 第 7871-7873 行: `setTimeout(() => document.querySelector('.platform-tab[data-platform="auto-publish"]')?.click(), 0)` 确保初始化

2. **查看全部发布历史** ✅
   - `#ap-pipeline-tbody` 加载流水线任务，含矩阵/平台账号/源视频/标题/状态/播放量/发布时间/尝试/操作 9 列
   - 筛选器：全部矩阵 + 全部账号 + 全部状态

3. **点击矩阵 → 选账号 → 看该账号发布历史** ✅
   - `renderAccountTree()` 渲染矩阵→账号树形结构
   - 点击账号 → `fetchPublishHistory(accountId)` → `renderPublishHistory()` 显示该账号历史
   - 历史项含标题/状态/播放量/点赞/评论/分享/发布时间/视频链接/失败原因/日志按钮/重新发布按钮

4. **查看日志** ✅
   - 流水线表格的"日志"按钮 → `togglePipelineLogs()` 行内展开
   - 历史项的"查看日志"按钮 → `_showHistoryLog()` 用 `alert()` 展示

5. **重试/重新发布** ✅
   - 流水线: `canRetry`(failed/retry) 时显示"重试"按钮 → `retryPipelineTask()`
   - 历史项: `canRetry && h.taskId` 时显示"重新发布"按钮 → `retryPipelineTask()`

### 发现的问题

#### 🟡 1.1 历史项日志用 alert() 展示，体验不佳
- **位置**: `app.js` 第 8397-8413 行 `_showHistoryLog(taskId)`
- **问题**: 使用 `alert(text)` 展示日志，长文本阅读体验差，且阻塞页面。流水线表格的日志用的是行内展开(`togglePipelineLogs`)，两处日志展示方式不一致。
- **影响**: 用户在历史项查看日志时需面对一个长 alert 弹窗，无法滚动/复制/对比。

#### 🟢 1.2 流水线表格刷新时不显示加载中
- **位置**: `app.js` 第 8018-8035 行 `fetchPipelineTasks`
- **问题**: 首次加载时 `<tbody>` 初始内容为"加载中…"(HTML 静态)，但后续刷新(如切换筛选器)时不显示加载状态，旧数据保持显示直到新数据返回。`quiet: true` 轮询时保留旧数据是合理的，但用户主动点"刷新"按钮(`quiet: false`)时也不显示加载提示。
- **影响**: 用户点刷新后无明显反馈，可能以为没响应。

---

## 2. 配置流程

**流程**: 切换到 matrix tab → 选矩阵 → 看到自动发布配置 → 开关/选方案/设上限/设时间段 → 自动保存 → 切回 auto-publish tab 看效果

### 评估结果：✅ 畅通（有轻微问题）

**实现路径验证**:

1. **切换到 matrix tab** ✅
   - `app.js` 第 2645 行: `if (platform === "matrix") { fetchMatrices(); }`

2. **选矩阵 → 看到自动发布配置** ✅
   - `renderMatrices()` 第 5562 行: 选中矩阵后调用 `fetchMatrixAutoPublishConfig(mxState.selectedId)`
   - `renderMatrixAutoPublishConfig(cfg)` 渲染: 开关/混剪方案/每日上限/监控间隔/发布时间段/CDP实例

3. **自动保存** ✅
   - `app.js` 第 5917-5925 行: `document.addEventListener('change', ...)` 监听 `#mx-ap-enabled`、`#mx-ap-preset`、`#mx-ap-daily`、`#mx-ap-interval` 的 change 事件，自动触发 `saveMatrixAutoPublishConfig()`
   - 也有"保存配置"按钮(第 5915 行)供手动保存

4. **切回 auto-publish tab 看效果** ✅
   - 切回时 `autoPublish.init()` 重新调用 `fetchMatrices()` → `fetchMonitorData()` → `renderAccountTree()` → `_updateFilterOptions()`
   - 轮询(10秒)也会静默刷新 matrices 数据

### 发现的问题

#### 🟢 2.1 发布时间段(publishTimeSlots)不自动保存
- **位置**: `app.js` 第 5917-5925 行
- **问题**: 自动保存的 change 监听只覆盖 `#mx-ap-enabled`、`#mx-ap-preset`、`#mx-ap-daily`、`#mx-ap-interval`，**不包含 `#mx-ap-slots`**(发布时间段输入框)。用户修改时间段后必须手动点"保存配置"按钮。
- **影响**: 用户可能以为时间段也会自动保存，修改后直接切走导致丢失。

#### 🟢 2.2 切换矩阵 tab 不自动恢复上次选中的矩阵配置
- **位置**: `app.js` 第 2645 行 `fetchMatrices()`
- **问题**: `fetchMatrices()` 只加载列表并 `renderMatrices()`，不自动重新选中之前选中的矩阵(`mxState.selectedId` 虽然保留但 `renderMatrices` 不会自动触发 click 事件来加载详情)。用户切走再切回 matrix tab 后，左侧列表刷新但右侧详情区不会自动恢复。
- **影响**: 用户需要重新点击左侧矩阵才能看到配置，略显繁琐。

---

## 3. auto-publish tab 布局

**要求**: 上半部分(全部发布历史表格) + 下半部分(矩阵×账号树形选择器+详情)

### 评估结果：✅ 布局合理（结构清晰）

**实际布局**（`index.html` 第 1558-1659 行）:

```
auto-publish-panel
├── 视频监控状态栏 (ap-monitor-section)
├── 发布历史看板 (ap-pipeline-section) ← 上半部分：全部发布历史表格
│   ├── 筛选器：矩阵/账号/状态
│   └── 流水线表格 (9列)
├── 矩阵×账号分栏 (ap-history-split) ← 下半部分：树形选择器+详情
│   ├── 左：ap-tree-col (矩阵→账号树)
│   └── 右：ap-history-detail-col (选中账号的发布历史)
└── 发布日志 (ap-pipeline-section)
```

**评价**:
- 布局逻辑清晰：监控 → 全部历史 → 按账号钻取 → 日志，自上而下递进
- 每个区块有独立的 `ap-section-head` 标题和操作按钮
- 表格有 `max-height: 500px; overflow-y: auto`，树有 `max-height: 520px`，不会无限撑高页面
- ✅ 布局合理

---

## 4. 表格列

**要求**: 发布历史表格的列是否完整(矩阵/平台账号/源视频/标题/状态/播放量/发布时间/尝试/操作)？colspan是否匹配？

### 评估结果：✅ 完整匹配

**验证**:

| 表头(HTML 1599-1608) | 渲染(app.js 8167-8184) |
|----------------------|----------------------|
| 矩阵 | `t.matrixName \|\| t.matrixId` |
| 平台账号 | `platform / accountName` |
| 源视频 | `t.sourceVideoId` (截取前12位) |
| 标题 | `t.materialTitle` (清洗后截取40字) |
| 状态 | `status` + statusClass 样式 |
| 播放量 | `t.viewsCount` |
| 发布时间 | `t.jobExecutedAt \|\| t.createdAt` |
| 尝试 | `${attempts}/${maxAttempts}` (3次上限) |
| 操作 | 重试/立即发布/日志 按钮 |

- **colspan 匹配**: 空状态行 `colspan="9"`(第 1611 行) = 9 列 ✅
- 行内日志展开行 `colspan="9"`(第 8187 行) = 9 列 ✅

---

## 5. 树形选择器

**要求**: 矩阵→账号的树形结构是否清晰？点击交互是否正确？

### 评估结果：✅ 清晰正确（有轻微问题）

**实现**(`app.js` 8275-8317 行 `renderAccountTree`):

```
矩阵A
  ▸ [展开/折叠]
    ├── tiktok 账号1 (高亮 active)
    ├── instagram 账号2
    └── youtube 账号3
矩阵B
  ▸
    └── 暂无账号
```

- 矩阵名可点击折叠/展开(`ap-tree-matrix-name` click → toggle display)
- 账号可点击选中(`ap-tree-account` click → `fetchPublishHistory`)
- 选中账号高亮(`.active` → 蓝底白字)
- 平台标签有颜色区分(`platform-tiktok` 黑底 / `platform-instagram` 橙底 / `platform-youtube` 红底)

### 发现的问题

#### 🟢 5.1 矩阵折叠用 inline style 而非 class
- **位置**: `app.js` 第 8313-8314 行
- **问题**: 折叠通过 `next.style.display = 'none'/'block'` 实现，而非 toggle class。首次渲染时所有矩阵默认展开(无初始折叠状态)，用户需逐个点击折叠。
- **影响**: 矩阵多时页面较长，但功能正常。

#### 🟢 5.2 折叠箭头不随状态变化
- **位置**: `app.js` 第 8294 行 `<span>▸</span>`
- **问题**: 折叠/展开时箭头始终是 `▸`，不切换为 `▾`，用户无法从箭头判断当前是展开还是折叠状态。
- **影响**: 可用性轻微降低。

---

## 6. 状态显示

**要求**: 各种状态(pending/remixing/remixed/scheduled/published/failed/retry)是否都有对应的样式？

### 评估结果：✅ 全部覆盖

**CSS 样式**(`styles.css` 4831-4837 行):

| 状态 | CSS 类 | 背景色 | 文字色 |
|------|--------|--------|--------|
| pending | `.ap-status-pending` | `#f1f5f9` (灰) | `#64748b` (灰) |
| remixing | `.ap-status-remixing` | `rgba(59,130,246,0.12)` (蓝) | `#2563eb` (蓝) |
| remixed | `.ap-status-remixed` | `rgba(16,185,129,0.12)` (绿) | `#059669` (绿) |
| scheduled | `.ap-status-scheduled` | `rgba(139,92,246,0.12)` (紫) | `#7c3aed` (紫) |
| published | `.ap-status-published` | `rgba(22,163,74,0.12)` (深绿) | `#15803d` (深绿) |
| failed | `.ap-status-failed` | `rgba(239,68,68,0.12)` (红) | `#dc2626` (红) |
| retry | `.ap-status-retry` | `rgba(245,158,11,0.12)` (橙) | `#d97706` (橙) |

- `statusClass()` 方法(`app.js` 7897-7908 行)映射完整 ✅
- 失败行有 `.failed-row` 红色背景(`styles.css` 4809-4813 行) ✅
- 失败原因有 `.ap-fail-reason` 红色小字(`styles.css` 4839-4847 行) ✅

---

## 7. 按钮显示逻辑

**要求**: 重试/立即发布/日志按钮在不同状态下是否正确显示/隐藏？

### 评估结果：✅ 逻辑正确

**流水线表格按钮**(`app.js` 8159-8183 行):

| 按钮 | 显示条件 | 评估 |
|------|----------|------|
| 重试 | `canRetry = status === 'failed' \|\| status === 'retry'` | ✅ 仅失败/重试时显示 |
| 立即发布 | `canPublish = (status === 'scheduled' \|\| status === 'retry') && t.publishJobId` | ✅ 仅已排期/重试且有发布任务ID时显示 |
| 日志 | 始终显示 | ✅ 所有状态都可查看日志 |

**历史项按钮**(`app.js` 8380-8383 行):

| 按钮 | 显示条件 | 评估 |
|------|----------|------|
| 查看日志 | `h.taskId` 存在 | ✅ |
| 重新发布 | `canRetry && h.taskId` | ✅ 仅失败/重试且有任务ID时显示 |

---

## 8. 响应式

**要求**: 页面在不同宽度下是否可用？

### 评估结果：🟡 基本可用，matrix tab 响应式缺失

**auto-publish tab 响应式** ✅:
- `@media (max-width: 900px)`: `.ap-history-split` → 单列(`styles.css` 5080-5082 行)
- `@media (max-width: 640px)`: `.ap-table` 字号缩小、padding 缩小、`.ap-table-wrap` 横向滚动(`styles.css` 5398-5415 行)
- `.ap-layout` 也有响应式(`styles.css` 5074-5076 行)，但 `.ap-layout` 实际未在 HTML 中使用(见第12项)

#### 🟡 8.1 matrix-layout 无响应式规则
- **位置**: `styles.css` 第 3817-3821 行 `.matrix-layout { grid-template-columns: 260px 1fr; }`
- **问题**: `matrix-layout` 在所有媒体查询中都没有出现。在窄屏(≤640px)下，260px 侧栏 + 1fr 主区仍保持双列，侧栏会挤压主区内容。
- **影响**: 手机端 matrix tab 布局可能拥挤，但 `.matrix-sidebar` 有 `max-height` + `overflow-y` 可滚动，勉强可用。

#### 🟢 8.2 auto-publish 面板内联样式较多
- **问题**: `#ap-logs-list`(第 1654 行)等元素使用大量内联 `style` 而非 class，响应式调整困难。
- **影响**: 维护性降低，但功能正常。

---

## 9. 错误提示

**要求**: API失败时是否有toast或错误信息？

### 评估结果：✅ 基本覆盖（有遗漏）

**toast 机制** ✅:
- `showToast(message, isError)` 函数(`app.js` 188-194 行)
- CSS: `.toast` 固定右下角，`.toast.error` 红色背景(`styles.css` 2260-2285 行)
- HTML: `<div id="toast" class="toast" role="status" aria-live="polite">`(`index.html` 1849 行)

**错误提示覆盖情况**:

| 操作 | 错误处理 | 评估 |
|------|----------|------|
| 重试 `retryPipelineTask` | `showToast('重试失败: ...', true)` | ✅ |
| 立即发布 `triggerPublishTask` | `showToast('触发发布失败: ...', true)` | ✅ |
| 触发监控 `triggerMonitor` | `showToast('监控触发失败: ...', true)` | ✅ |
| 加载发布历史 `fetchPublishHistory` | 列表区显示红色错误信息 | ✅ |
| 加载日志 `fetchPublishLogs` | `showToast('加载日志失败: ...', true)` (非 quiet 时) | ✅ |
| 加载配置 `fetchMatrixAutoPublishConfig` | 配置区显示红色错误信息 | ✅ |
| 保存配置 `saveMatrixAutoPublishConfig` | `showToast('保存失败: ...', true)` | ✅ |

#### 🟡 9.1 流水线加载失败静默无提示
- **位置**: `app.js` 第 8029-8034 行 `fetchPipelineTasks` catch 块
- **问题**: `quiet: false` 时清空表格并渲染"暂无流水线任务"，但**不弹 toast**。用户主动点"刷新"按钮后若 API 失败，只看到"暂无流水线任务"，无法区分"真的没数据"还是"加载出错"。
- **影响**: 用户可能误以为系统正常但无数据。

#### 🟢 9.2 矩阵加载失败静默
- **位置**: `app.js` 第 8013-8015 行 `fetchMatrices` catch 块
- **问题**: `this.matrices = []` 后无任何提示，树形选择器显示"暂无矩阵"，无法区分加载失败还是真的无矩阵。
- **影响**: 轻微，因为轮询会持续重试。

---

## 10. 加载状态

**要求**: 数据加载时是否显示加载中提示？

### 评估结果：🟡 部分覆盖

| 区域 | 初始加载 | 后续刷新 | 评估 |
|------|----------|----------|------|
| 流水线表格 | "加载中…"(HTML 静态) | 无加载提示 | 🟡 |
| 树形选择器 | "加载中…"(HTML 静态) | 无加载提示 | 🟡 |
| 账号发布历史 | "加载中…"(`fetchPublishHistory` 设置) | 无加载提示 | 🟡 |
| 监控列表 | "暂无监控数据"(HTML 静态) | 无加载提示 | 🟢 |
| 发布日志 | "暂无日志"(HTML 静态) | 无加载提示 | 🟢 |

#### 🟡 10.1 首次加载后刷新无加载反馈
- **问题**: 仅首次加载(HTML 静态内容)有"加载中…"提示。用户点"刷新"按钮时，旧数据保持显示直到新数据返回，无加载中过渡状态。
- **影响**: 用户对刷新操作缺乏即时反馈。

#### 🟢 10.2 监控列表初始文案不准确
- **位置**: `index.html` 第 1567 行
- **问题**: 初始文案为"暂无监控数据"，但实际是尚未加载。首次进入页面时看到"暂无监控数据"可能让用户误以为没有启用监控的矩阵。
- **影响**: 轻微误导。

---

## 11. 监控状态

**要求**: 是否有地方显示矩阵的最后监控时间？

### 评估结果：✅ 已实现

**实现**(`app.js` 8257-8267 行 `renderMonitor`):

- 监控列表每项显示: 矩阵名 + 最后监控时间 + "立即监控"按钮
- `lastMonitorAt` 从 `m.autoPublishConfig.lastMonitorAt` 读取
- 无记录时显示"从未监控"
- 有"立即监控"按钮 → `triggerMonitor(matrixId)` 手动触发

#### 🟢 11.1 监控项缺少新视频数量展示
- **位置**: `app.js` 8260-8266 行 `renderMonitor`
- **问题**: CSS 中定义了 `.ap-monitor-count`(新视频数量徽章) 和 `.ap-monitor-count.has-new`(有新视频高亮)样式(`styles.css` 4755-4767 行)，但 `renderMonitor` **未渲染** count 字段，仅有名称和时间。
- **影响**: 用户无法在监控列表直接看到新发现了多少视频，需进入流水线查看。CSS 样式成了死代码。

---

## 12. 旧CSS清理

**要求**: 是否有大量死CSS规则影响维护？

### 评估结果：🔴 存在大量死CSS（约100行+）

经逐一验证，以下 CSS 类**在 HTML 和 JS 中均无使用**，属于旧版自动发布布局的遗留代码：

#### 🔴 12.1 旧版达人卡片布局死CSS（约100行）
- **位置**: `styles.css` 第 4532-4730 行
- **死类清单**:
  - `.ap-layout` (第 4532-4537 行) — 旧版双栏布局，当前 HTML 用 `.ap-main-col` 单栏
  - `.ap-creators-col` (第 4538-4544 行) — 旧版左栏容器
  - `.ap-creators-list` (第 4570-4575 行)
  - `.ap-creator-card` (第 4578-4582 行) — 旧版达人卡片
  - `.ap-creator-header` (第 4583-4593 行)
  - `.ap-creator-toggle` (第 4594-4599 行)
  - `.ap-creator-info` (第 4600-4611 行)
  - `.ap-creator-platform` (第 4612-4615 行)
  - `.ap-creator-enabled` (第 4616-4618 行)
  - `.ap-creator-body` (第 4621-4627 行)
  - `.ap-config-row` (第 4628-4633 行)
  - `.ap-config-label` (第 4634-4639 行)
  - `.ap-config-value` (第 4640-4654 行)
  - `.ap-bindings-area` (第 4657-4661 行)
  - `.ap-bindings-head` (第 4662-4669 行)
  - `.ap-binding-form` (第 4670-4686 行)
  - `.ap-binding-item` (第 4687-4714 行) + 子元素 `.ap-binding-seq`/`.ap-binding-name`/`.ap-binding-limit`
  - `.ap-binding-del` (第 4715-4729 行)
- **影响**: 约100行死CSS，增加文件体积，干扰维护者理解当前布局结构。

#### 🟢 12.2 响应式中引用死类
- **位置**: `styles.css` 第 5074-5082 行 `@media (max-width: 900px)` 和 第 5356-5396 行 `@media (max-width: 640px)`
- **问题**: 响应式规则中引用了 `.ap-layout`、`.ap-creators-col`、`.ap-creator-header`、`.ap-creator-info`、`.ap-creator-platform`、`.ap-creator-body`、`.ap-config-row`、`.ap-config-label`、`.ap-config-value`、`.ap-binding-item` 等死类，这些规则永远不会生效。
- **影响**: 约30行死响应式规则。

#### 🟢 12.3 .ap-switch 仅JS内联引用
- **位置**: `styles.css` 第 4850-4886 行 `.ap-switch`
- **问题**: `.ap-switch` 在 HTML 中无使用(class 属性)，但 `app.js` 中有2处引用(`mx-ap-switch` 用的是独立的 `.mx-ap-switch` 类)。`.ap-switch` 类可能被 JS 动态生成但实际未使用。
- **影响**: 约35行可能死CSS(需进一步确认JS动态生成场景)。

#### 🟢 12.4 autoPublish 对象死属性
- **位置**: `app.js` 第 7889 行 `selectedMatrixId: null`
- **问题**: `selectedMatrixId` 声明后从未被赋值或读取，树形选择器用的是 `selectedAccountId`。属于死代码。
- **影响**: 轻微，不影响功能。

#### 🟢 12.5 filterLogLevel 死赋值
- **位置**: `app.js` 第 7985 行 `this.filterLogLevel = e.target.value;`
- **问题**: `filterLogLevel` 在 `_bindEvents` 中被赋值，但 `fetchPublishLogs` 直接从 DOM(`#ap-log-filter`)读取 level 值，从不读取 `this.filterLogLevel`。该属性赋值无效。
- **影响**: 轻微死代码。

#### 🟢 12.6 CSS变量引用不一致
- **位置**: `index.html` 第 1654 行 `#ap-logs-list` 内联样式 `border:1px solid var(--border,#e2e8f0)`
- **问题**: 引用了 `--border` 变量，但 CSS 中定义的变量名是 `--line`(第 18 行 `--line: #e2e8f0`)，`--border` 从未定义。因此始终回退到 `#e2e8f0`。
- **影响**: 功能正常(回退值正确)，但变量名不一致，若未来调整 `--line` 不会同步影响此处。

---

## 汇总表

| # | 检查项 | 严重程度 | 状态 |
|---|--------|----------|------|
| 1 | 用户流程完整性 | 🟢 | 基本畅通 |
| 1.1 | 历史项日志用alert() | 🟡 中等 | 需优化 |
| 1.2 | 流水线刷新无加载提示 | 🟢 轻微 | 可优化 |
| 2 | 配置流程 | 🟢 | 畅通 |
| 2.1 | 发布时间段不自动保存 | 🟢 轻微 | 可优化 |
| 2.2 | 切换tab不恢复选中矩阵 | 🟢 轻微 | 可优化 |
| 3 | auto-publish tab布局 | ✅ | 合理 |
| 4 | 表格列 | ✅ | 完整匹配 |
| 5 | 树形选择器 | 🟢 | 清晰正确 |
| 5.1 | 折叠用inline style | 🟢 轻微 | 可优化 |
| 5.2 | 折叠箭头不变 | 🟢 轻微 | 可优化 |
| 6 | 状态显示 | ✅ | 全部覆盖 |
| 7 | 按钮显示逻辑 | ✅ | 逻辑正确 |
| 8 | 响应式 | 🟡 | 基本可用 |
| 8.1 | matrix-layout无响应式 | 🟡 中等 | 需补充 |
| 8.2 | 内联样式较多 | 🟢 轻微 | 可优化 |
| 9 | 错误提示 | 🟡 | 基本覆盖 |
| 9.1 | 流水线加载失败静默 | 🟡 中等 | 需补充toast |
| 9.2 | 矩阵加载失败静默 | 🟢 轻微 | 可优化 |
| 10 | 加载状态 | 🟡 | 部分覆盖 |
| 10.1 | 刷新无加载反馈 | 🟡 中等 | 需优化 |
| 10.2 | 监控列表初始文案 | 🟢 轻微 | 可优化 |
| 11 | 监控状态 | ✅ | 已实现 |
| 11.1 | 缺少新视频数量 | 🟢 轻微 | CSS已就绪待启用 |
| 12 | 旧CSS清理 | 🔴 | 存在大量死CSS |
| 12.1 | 旧版达人卡片布局死CSS | 🔴 严重 | ~100行需清理 |
| 12.2 | 响应式引用死类 | 🟢 轻微 | ~30行需清理 |
| 12.3 | .ap-switch可能死CSS | 🟢 轻微 | ~35行需确认 |
| 12.4 | selectedMatrixId死属性 | 🟢 轻微 | 需清理 |
| 12.5 | filterLogLevel死赋值 | 🟢 轻微 | 需清理 |
| 12.6 | CSS变量引用不一致 | 🟢 轻微 | 需统一 |

---

## 优先修复建议

### 🔴 高优先级
1. **清理旧版达人卡片布局死CSS**(第 4532-4730 行约100行 + 响应式中引用约30行) — 提升可维护性

### 🟡 中优先级
2. **流水线加载失败时弹toast**(`fetchPipelineTasks` catch 块补充 `showToast`) — 让用户区分"无数据"和"加载失败"
3. **刷新操作显示加载中状态**(流水线表格、树形选择器刷新时插入临时"加载中…") — 提供即时反馈
4. **matrix-layout 补充响应式规则**(≤640px 时侧栏改为上下布局) — 改善手机端体验
5. **历史项日志改用弹窗/行内展开替代alert()** — 与流水线日志展示方式统一

### 🟢 低优先级
6. 发布时间段(`#mx-ap-slots`)纳入自动保存监听
7. 切换 matrix tab 时自动恢复上次选中的矩阵
8. 树形折叠改用 class toggle + 箭头图标切换
9. 监控列表渲染新视频数量(启用已就绪的 `.ap-monitor-count` CSS)
10. 清理 `selectedMatrixId`、`filterLogLevel` 死代码
11. 统一 `--border` → `--line` 变量引用

---

**审查结论**: 前端交互流程基本完整可用，核心用户路径(查看发布历史→选账号→查看日志→重试)畅通。主要问题集中在(1)旧版布局遗留的大量死CSS影响可维护性，(2)加载/错误状态的即时反馈不足，(3)matrix tab 响应式缺失。无功能性阻断问题。
