请直接处理我上传的【1个原始包包视频】，完整分析其中的真实包包、真实达人和真实场景，并在本次任务中一次性生成【10张彼此独立的9:16高清图片】。

本任务只做图片生成，不做视频、音频、MP4、GIF、ZIP。

【最高优先级】

1. 本次任务最终必须返回【10个独立图片文件】。
2. 严禁把10张图做成1张拼图、九宫格、contact sheet、collage、storyboard、多面板图片。
3. 每个输出文件内部只能有【1张完整9:16照片】。
4. 用户只提交一次任务；系统内部可逐张生成、逐张QC，但最终必须一次性返回10张独立图片。

【核心原则】
产品身份 > 产品真实性 > 达人身份 > 场景身份 > 物理合理性 > 摄影质感 > 多样性

所有新增图片都必须让人感觉：
【原视频中的同一个达人、同一个包，在同一天、同一个真实空间继续补拍。】

【生成原则】

1. 必须先完整分析原视频，并提取真实参考帧。
2. 后续生成必须主要依赖这些真实参考帧，不能只凭文字重新想象。
3. 必须分别建立：产品参考、达人参考、场景参考。
4. 每张目标图只选择4–8张最相关参考帧。
5. 产品结构分为：

   * CONFIRMED：可正常生成
   * UNCERTAIN：只能保守表现已确认部分
   * UNKNOWN：禁止生成
6. 如果内部结构不够清楚，禁止脑补内部，自动改生成其他已确认细节。
7. 如果Logo不够清楚，禁止生成乱码、错字母、错品牌或额外Logo。
8. 纯产品图禁止人物手持；包必须自然放在原场景中真实存在的平台上。
9. 所有图片必须符合真实物理：

   * 包不能悬浮
   * 包底必须真实接触平台
   * 链条/肩带必须自然下垂
   * 人物与包不能穿模
   * 抓握、受力、肩带绕身关系必须合理
10. 所有真人图必须是原视频中的同一个达人本人，禁止换脸、换人或明显漂移。
11. 所有图片必须保持同一个真实房间，不得换场景、换装修、加专柜、加摄影棚、加新家具。
12. 画面风格必须是【真实TikTok UGC + 高质量手机补拍】，可以增强纹理、缝线、五金反光和材质层次，但不能改变真实颜色、材质和结构。

【10张图片任务结构】
图1：产品标准正面，无字
图2：产品45°角，无字
图3：产品侧面 / 顶部 / 包口，无字
图4：内部（仅在CONFIRMED时）或其他CONFIRMED细节，无字
图5：原达人在原场景背起同一个包，无字
图6：原达人以明显不同姿势再次背起同一个包，无字
图7：产品正面 / 材质 / 纹理 / 五金 + 英文讲解
图8：产品侧面 / 厚度 / 底部 / 支撑结构 + 英文讲解
图9：顶部 / 包口 / 内部；如内部不可靠则改为其他CONFIRMED细节 + 英文讲解
图10：原达人展示同一个包 + 英文讲解

【图7–图10文字规则】

1. 每张都必须有一段独立英文讲解。
2. 每段英文必须 >35个英文单词，建议36–60词。
3. 语气必须像真实美国女性TikTok达人自然分享，不要像官网，不要硬广。
4. 禁止CTA，例如：BUY NOW、SHOP NOW、LINK IN BIO、SALE、DISCOUNT、ORDER NOW。
5. 不要让生图模型直接生成复杂长英文；应先生成无字底图，QC通过后，再叠加正确英文。
6. 文字优先放在画面中上区域，必须可读、完整，不遮挡包主体、Logo、关键五金和人物脸。

【QC规则】
必须执行：
【生成1张 → 检查1张 → 合格保存 → 不合格自动重生 → 再进入下一张】

每张都要检查：

1. 是否仍然是原视频中的同一个包
2. 产品颜色、比例、轮廓、材质、纹理、五金、Logo、肩带/链条是否正确
3. 真人图是否仍然是原达人本人
4. 场景是否仍然是同一个真实空间
5. 物理关系是否合理
6. 图7–图10的英文是否 >35词、无乱码、无明显语法错误、与画面内容对应、无遮挡核心主体

【失败自动降级】
如果生成失败或不稳定，自动按以下原则修复，不要询问用户：

* 加强正确参考
* 删除无关参考
* 使用更接近原视频的角度
* 简化构图
* 简化背景
* 减少人物变化
* 内部不稳就停止生成内部，改生成其他CONFIRMED细节
* 场景漂移就回到最清晰、最确定的平台与背景

【最终交付】
最终只返回【10张独立的9:16高清成品图片】。

再次强调：

* 是10个独立图片文件
* 不是1张拼图
* 不是九宫格
* 不是contact sheet
* 不是collage
* 不是多画面合成图