# 第二轮审查：第一轮修复验证 + 新引入Bug检查

> **审查对象**：`public/app.js`、`public/index.html`、`src/server.js`
> **审查范围**：验证第一轮标记为已修复的 P0/P1 项是否真正生效 + 检查修复方式是否引入新Bug
> **审查方式**：只读代码审查，不改代码
> **审查日期**：2026-09-19

---

## 一、总体结论

第一轮标记修复的 6 项问题中：
- **P0 TDZ崩溃** → ✅ 已真正修复（`setTimeout(0)` 方案有效）
- **P1 自动发布开关change事件** → ✅ 已修复（document委托绑定正确）
- **P1 日志筛选器refetch** → ✅ 已修复（change→fetchPublishLogs）
- **P1 publish-history的accountId查找逻辑** → ✅ 部分修复（去掉死代码`matrixId||''`，但全表搜路径仍保留，性能问题未根除）
- **P1 JOIN行膨胀** → ❌ 修复方式错误，引入新的致命数据不一致（详见问题1）
- **P1 limit NaN** → ✅ 已修复（`Number()||100`与`parseInt||"50"`均有兜底）

**新发现严重Bug 1个**：JOIN条件 `ma.account_name = COALESCE(j.account_id, '')` 因字段语义不匹配（`j.account_id` 存的是 profileId/`tk_accounts.id`，而 `ma.account_name` 是 TikTok 用户名），导致**按账号查询发布历史永远返回空结果**，且**未按账号筛选时 accountName/platform 字段恒为 null**。

---

## 二、问题清单（按严重程度排序）

| # | 严重程度 | 位置 | 问题 | 第一轮状态 |
|---|---------|------|------|-----------|
| 1 | 🔴 P0 致命（新引入） | server.js:4336 | JOIN条件字段语义不匹配，导致按账号查发布历史返回空 | 第一轮"修复"反而破坏功能 |
| 2 | 🟠 P1 中（遗留） | server.js:4413-4415 | logs API 无 taskId 时硬编码 `message LIKE '%发布任务%'`，"全部日志"视图只显示含此关键字的日志 | 第一轮标记但未修复 |
| 3 | 🟡 P2 低（遗留） | app.js:8048 | `fetchMonitorData` 空数组判断仍用 `this.matrices.length`，矩阵全空时轮询周期内冗余请求 | 第一轮P3，未修复 |
| 4 | 🟡 P2 低（遗留） | app.js:8320-8343 | `fetchPublishHistory` 无请求序号防竞态，快速切换账号可能渲染错位 | 第一轮P3，未修复 |
| 5 | 🟢 P3 提示 | app.js:7985 | `this.filterLogLevel` 被赋值但从未被读取（fetchPublishLogs 直接从DOM读 `#ap-log-filter` 值） | 死代码，无害 |

---

## 三、详细问题分析

### 问题1 🔴 P0 致命（新引入）：JOIN条件字段语义不匹配，按账号查发布历史恒返回空

**位置**：`server.js:4335-4336`

**修复后的代码**：
```sql
LEFT JOIN matrix_accounts ma ON ma.matrix_id = p.matrix_id
  AND ma.account_name = COALESCE(j.account_id, '')
```

**问题分析**：

1. **字段语义核对**（关键）：
   - `ma.account_name`（`matrix_accounts.account_name`）：存的是社媒账号名，即 TikTok 用户名（如 `@user_a`）。表定义见 `database.js:300`。
   - `j.account_id`（`tk_publish_jobs.account_id`）：存的是 `data.accountId || data.profileId`（`database.js:1778`）。
   - 自动发布路径下，调度器调用 `createTkPublishJob({ accountId: profileId, ... })`（`auto-publish-scheduler.js:645-647`），即 **`j.account_id` 实际存的是 profileId**（`profiles.id`，形如 `prof_xxx`），**不是 TikTok 用户名**。
   - 手动发布路径下，前端传 `accountId: acc ? acc.id : profileId`（`app.js:3223`），其中 `acc.id` 是 `tk_accounts.id`（另一个表的主键），**同样不是 `matrix_accounts.account_name`**。

2. **结论**：`ma.account_name = COALESCE(j.account_id, '')` 的等值匹配**几乎永远不成立**（两字段属于不同值域）。

3. **影响链**：
   - JOIN 失败 → `LEFT JOIN` 退化为 `ma.*` 全 NULL（行仍返回，但 accountName/platform/matrix_account_id 字段为 NULL）。
   - WHERE 子句 `ma.account_name = ? OR j.account_id = ?`（`server.js:4287`）：
     - `ma.account_name = accountName` → `NULL = accountName` → false
     - `j.account_id = accountId` → `profileId` 或 `tk_accounts.id` ≠ `matrix_account.id`（`ma_xxx`）→ false
     - **整行被过滤掉**
   - **结果**：`fetchPublishHistory(accountId)` 调用 `/api/auto-publish/publish-history?accountId=ma_xxx` 时，SQL 查询返回空数组，前端显示"该账号暂无发布历史"——即使该账号实际有发布记录。

4. **未按账号筛选时也有问题**：仅传 `matrixId` 不传 `accountId` 时，WHERE 只有 `p.matrix_id = ?`，行能返回，但 `ma.account_name`/`ma.platform`/`ma.accountId` 字段全为 NULL。当前 `renderPublishHistory` 恰好不渲染 accountName/platform，所以 UI 上不可见，但底层数据错误，后续如显示账号列就会暴露。

5. **与第一轮原 JOIN 的对比**：
   - 第一轮原 JOIN：`ma.account_name = j.account_id OR ma.platform = 'tiktok'`
   - 原 JOIN 的 `ma.platform = 'tiktok'` 至少能匹配到该矩阵的 TikTok 账号（虽然可能不是真正发布的那个账号），行不膨胀为全 NULL。
   - 新 JOIN 去掉了 `platform='tiktok'` 兜底，改为纯 `account_name = account_id` 等值——**修复了行膨胀，但引入了全不匹配**。属于"治表不治里"。

**验证依据**：
- `auto-publish-scheduler.js:646` `accountId: profileId`
- `database.js:1778` `data.accountId || data.profileId`
- `database.js:300` `matrix_accounts.account_name TEXT NOT NULL`（社媒账号名）
- `app.js:3223` 手动发布 `accountId: acc ? acc.id : profileId`（`acc.id` 是 `tk_accounts.id`）
- `app.js:8305` `this.selectedAccountId = el.dataset.accId`（`data-acc-id` 绑的是 `a.id` 即 `matrix_account.id`）
- `app.js:8334` `params.set('accountId', accountId)` 传给后端

**影响**：发布历史页的核心功能（按账号查看发布历史）完全不可用，用户点任何账号都看到"暂无发布历史"。这是回归性Bug。

**修复方向**（不改代码，仅建议）：
- 方案A：JOIN 改为通过 `matrix_profiles` 链路匹配——`tk_publish_jobs.profile_id` JOIN `matrix_profiles.profile_id` 拿到 `matrix_id`，再 JOIN `matrix_accounts`。即 `LEFT JOIN matrix_profiles mp ON mp.profile_id = j.profile_id LEFT JOIN matrix_accounts ma ON ma.matrix_id = COALESCE(mp.matrix_id, p.matrix_id)`。
- 方案B：WHERE 的账号筛选改为子查询 `p.matrix_id IN (SELECT matrix_id FROM matrix_accounts WHERE id = ?)`，不依赖 JOIN 的 `ma` 字段。
- 方案C：统一 `tk_publish_jobs.account_id` 的存值语义（全部存 `matrix_account.id` 或 `tk_accounts.id`），并让 JOIN 条件与之对齐。需改 `auto-publish-scheduler.js:646` 与 `app.js:3223`。

---

### 问题2 🟠 P1 中（遗留）：logs API 无 taskId 时硬编码过滤"发布任务"

**位置**：`server.js:4408-4416`

**代码**：
```js
if (taskId) {
  query += " AND (task_id = ? OR message LIKE ?)";
  params.push(taskId, `%${taskId}%`);
} else {
  query += " AND message LIKE ?";
  params.push("%发布任务%");
}
```

**问题**：前端 `fetchPublishLogs()`（`app.js:8418-8425`）不传 taskId，走 else 分支，只返回 message 含"发布任务"的日志。但调度器的日志 message 形如 `自动发布-监控矩阵失败`、`自动发布-下载视频失败`、`自动发布-Pipeline创建`、`自动发布-排期`、`自动发布-触发混剪失败` 等（`auto-publish-scheduler.js` 多处），**多数不含"发布任务"字样**。

**影响**：发布日志面板（`#ap-logs-list`）显示的日志严重不全，用户看不到监控失败、下载失败、混剪失败等关键日志。第一轮已标记为 P1 中，但本次复查确认**未修复**。

**修复方向**：else 分支应去掉 `message LIKE '%发布任务%'` 硬编码，改为按 `instance_id` 或时间范围过滤；或加一个 `source='auto-publish'` 字段区分日志来源。

---

### 问题3 🟡 P2 低（遗留）：fetchMonitorData 空数组判断仍用 length

**位置**：`app.js:8048`

**代码**：
```js
if (this.matrices && this.matrices.length) {
  this.monitorData = this.matrices.filter(...);
  ...
  return;
}
// 空数组时会走到下面再次 request('/api/auto-publish/matrices')
```

**问题**：当 `this.matrices` 为空数组（length=0，例如系统无任何矩阵）时，条件不命中，走到下方再次 `request('/api/auto-publish/matrices')`。而轮询回调（`app.js:7998`）已经内联请求过一次 matrices 接口并赋值 `this.matrices = []`，紧接着调用 `fetchMonitorData()`，于是同一次轮询周期内 matrices 接口被请求两次。

**影响**：仅当矩阵列表为空时多一次冗余请求，性能影响极小。第一轮 P3，未修复。

**修复方向**：条件改为 `if (this.matrices)`（判断数组对象存在而非长度）。

---

### 问题4 🟡 P2 低（遗留）：fetchPublishHistory 无请求序号防竞态

**位置**：`app.js:8320-8343`

**问题**：用户快速连续点击不同账号时，多个 `fetchPublishHistory` 并发，后发请求可能先返回，`this.publishHistory` 被旧请求覆盖，`renderPublishHistory` 渲染的是旧账号数据，而 `this.selectedAccountId` 已是新值，导致左侧高亮与右侧列表短暂不一致。

**影响**：低概率、快速点击场景下短暂错位。第一轮 P3，未修复。

**修复方向**：引入自增请求 token，渲染前校验 token 是否为最新。

---

### 问题5 🟢 P3 提示：filterLogLevel 被赋值但从未读取

**位置**：`app.js:7985`

**代码**：
```js
document.querySelector('#ap-log-filter')?.addEventListener('change', (e) => {
  this.filterLogLevel = e.target.value;  // 赋值
  this.fetchPublishLogs();               // 但 fetchPublishLogs 直接从 DOM 读 #ap-log-filter.value
});
```

**问题**：`this.filterLogLevel` 被赋值但全文件无任何读取处（`fetchPublishLogs` 在 `app.js:8420-8421` 直接 `document.querySelector('#ap-log-filter')?.value`）。属死代码，无功能影响，但可读性误导。

**修复方向**：要么删除 `this.filterLogLevel` 赋值，要么让 `fetchPublishLogs` 优先读 `this.filterLogLevel`。

---

## 四、第一轮修复逐项验证

### ✅ P0 TDZ崩溃 — 已真正修复

**修复方式**：`app.js:7871-7873`
```js
setTimeout(() => {
  document.querySelector('.platform-tab[data-platform="auto-publish"]')?.click();
}, 0);
```

**验证**：
1. `setTimeout(0)` 回调在模块脚本顶层执行完毕后才触发（JS 事件循环机制）。
2. `const autoPublish = {...}` 在 `app.js:7878` 声明，位于 `setTimeout` 注册点之后，但在回调执行点之前（模块脚本已完整执行到 `app.js:8453`）。
3. 回调触发 `.click()` → 进入 `app.js:2630` 的 tab 切换处理器 → 调用 `autoPublish.init()`（`app.js:2647`）→ 此时 `autoPublish` 已初始化，无 TDZ。
4. `autoPublish.init()` 用 `_initialized` 标志保证事件只绑定一次（`app.js:7927-7929`），多次切 tab 不会重复绑定。

**结论**：修复有效，不再崩溃。✅

---

### ✅ P1 自动发布开关change事件 — 已修复

**修复方式**：`app.js:5917-5925`
```js
document.addEventListener('change', (e) => {
  if (e.target?.id === 'mx-ap-enabled') {
    saveMatrixAutoPublishConfig();
  }
  if (e.target?.id === 'mx-ap-preset' || e.target?.id === 'mx-ap-daily' || e.target?.id === 'mx-ap-interval') {
    saveMatrixAutoPublishConfig();
  }
});
```

**验证**：
1. 事件委托绑定在 `document` 上，页面加载时绑定一次（`app.js:5918`）。
2. `renderMatrixAutoPublishConfig`（`app.js:5807`）每次重新渲染 innerHTML，但事件监听器在 `document` 上而非内部元素，**不会重复绑定**。✅
3. `#mx-ap-enabled`（checkbox）change 触发时 `e.target.id === 'mx-ap-enabled'` 命中，调用 `saveMatrixAutoPublishConfig()`。✅
4. `#mx-ap-preset`/`#mx-ap-daily`/`#mx-ap-interval` change 也触发保存（额外覆盖，合理）。✅
5. `#mx-ap-slots` 未纳入自动保存（需点保存按钮）——合理设计，因 slots 需格式校验（`app.js:5886-5893`）。

**潜在副作用（非Bug）**：`saveMatrixAutoPublishConfig` 成功后会 `await fetchMatrixAutoPublishConfig(matrixId)`（`app.js:5908`）重新渲染配置区，会重建 DOM。对 checkbox 的 change（失焦触发）无影响；对 select/number 的 change（失焦触发）也无影响。但若用户在 `mx-ap-daily` 改值失焦后立即改 `mx-ap-interval`，第一次保存的重新渲染可能重置 `mx-ap-interval` 的值，导致用户输入丢失。属低概率竞态，不构成Bug。

**结论**：修复有效，开关即时保存工作正常。✅

---

### ✅ P1 日志筛选器change refetch — 已修复

**修复方式**：`app.js:7984-7987`
```js
document.querySelector('#ap-log-filter')?.addEventListener('change', (e) => {
  this.filterLogLevel = e.target.value;
  this.fetchPublishLogs();  // 改为重新拉取，不再仅本地渲染
});
```

**验证**：
1. change 事件调用 `this.fetchPublishLogs()`（`app.js:8418`），该方法从 DOM 读 `#ap-log-filter.value` 作为 level，拼到查询参数（`app.js:8420-8423`），重新请求后端。✅
2. 不再依赖本地缓存的客户端过滤，避免了"先选具体级别再切回全部"时数据不一致的问题。✅
3. `renderPublishLogs`（`app.js:8433-8452`）仍有客户端二次过滤（`app.js:8441` `levelFilter ? this.publishLogs.filter(...) : ...`），但因 fetch 已带 level 服务端过滤，二次过滤结果与一次一致，无矛盾。✅

**遗留小问题**：`this.filterLogLevel` 赋值后从未被读取（见问题5），属死代码，无功能影响。

**结论**：修复有效，日志筛选器切换会重新拉取。✅

---

### ⚠️ P1 publish-history accountId查找逻辑 — 部分修复

**修复方式**：`server.js:4271-4281`
```js
if (accountId) {
  let acc = null;
  if (matrixId) {
    acc = store.listMatrixAccounts(matrixId).find(a => a.id === accountId);
  }
  if (!acc) {
    const matrices = store.listMatrices();
    for (const m of matrices) {
      const found = store.listMatrixAccounts(m.id).find(a => a.id === accountId);
      if (found) { acc = found; break; }
    }
  }
  ...
}
```

**验证**：
1. 原代码 `store.listMatrixAccounts(matrixId || '')` 当 matrixId 为 null 时传空字符串，`WHERE matrix_id = ''` 永远返回空数组（死代码）。✅ 已修复为 `if (matrixId)` 判断，不传时直接走全表搜降级。
2. 全表搜降级路径（`server.js:4276-4280`）正确：遍历所有矩阵，对每个矩阵调 `listMatrixAccounts(m.id)`，找到 `a.id === accountId` 即停。✅
3. 前端 `fetchPublishHistory(accountId)`（`app.js:8334`）只传 accountId 不传 matrixId，所以每次走全表搜路径——O(矩阵数) 次 DB 查询。性能差但功能可用。

**遗留问题**：
- 全表搜路径的性能问题（O(N) 次 DB 查询）未根除，矩阵多时退化。建议 `matrix_accounts` 表加 `id` 索引（已有 PRIMARY KEY）并直接 `SELECT * FROM matrix_accounts WHERE id = ?` 单次查询，无需遍历矩阵。

**结论**：死代码已去除，查找逻辑正确，但性能未优化。⚠️ 部分修复。

---

### ❌ P1 JOIN行膨胀 — 修复方式错误，引入新致命Bug

详见**问题1**。修复去掉了 `ma.platform = 'tiktok'` 的宽匹配（解决了行膨胀），但新条件 `ma.account_name = COALESCE(j.account_id, '')` 因字段语义不匹配（`ma.account_name` 是社媒用户名，`j.account_id` 是 profileId/`tk_accounts.id`），导致 JOIN 恒失败，按账号筛选时 WHERE 过滤掉所有行，返回空结果。

**结论**：修复引入回归，按账号查发布历史完全失效。❌

---

### ✅ P1 limit NaN — 已修复

**验证**：
1. `server.js:4260` `const limit = Math.min(Number(url.searchParams.get("limit")) || 100, 500);`
   - `url.searchParams.get("limit")` 为 null 时，`Number(null) = 0`，`0 || 100 = 100`，`Math.min(100, 500) = 100`。✅
2. `server.js:4407` `const limit = parseInt(url.searchParams.get("limit") || "50", 10);`
   - 为 null 时 `null || "50" = "50"`，`parseInt("50", 10) = 50`。✅

**结论**：NaN 防御有效。✅

---

## 五、新Bug检查（逐项）

### 6. setTimeout(0) 是否导致页面闪烁？

**结论**：不会闪烁。✅

**依据**：
- HTML 中 auto-publish 面板初始**无 `hidden` 类**（`index.html:1558` `<section class="auto-publish-panel auto-publish-tab">`），而其他面板（remix/matrix/scheduler/reddit/tiktok/cdp）初始均有 `hidden` 类。
- auto-publish tab 按钮初始即为 `active`（`index.html:83` `class="platform-tab active"`）。
- 页面首次 HTML 渲染时 auto-publish 面板就可见，`setTimeout(0)` 触发的 `.click()` 只是重复设置已存在的 active/可见状态（幂等），并调用 `autoPublish.init()` 拉取数据。
- 不会出现"先显示 remix tab 再切到 auto-publish"的闪烁。唯一可见变化是面板内从"暂无监控数据"空状态变为有数据，属正常加载。

---

### 7. change 事件委托是否重复触发？

**结论**：不会重复触发。✅

**依据**：
- `document.addEventListener('change', ...)`（`app.js:5918`）在页面加载时绑定一次，监听器在 `document` 上。
- `renderMatrixAutoPublishConfig`（`app.js:5807`）每次重新渲染 `mxEl.autopublishConfig.innerHTML`，但内部 input 元素的事件不直接绑定在它们上，而是靠 document 委托。
- 重新渲染会移除旧 input 的 DOM 节点（连同其上的直接监听器，如果有），但 document 上的委托监听器始终只有一个。
- 因此不会因重新渲染而重复绑定或重复触发。✅

---

### 8. renderPublishHistory 是否正确渲染所有字段？空数据防护？

**结论**：字段渲染正确，空数据防护充分。✅

**依据**（`app.js:8346-8394`）：
- 空数据：`if (!this.publishHistory.length)` 显示"该账号暂无发布历史"（`app.js:8349-8351`）。✅
- `materialTitle`：`h.materialTitle ? ...trim() : '(未命名视频)'`（`app.js:8354`）。✅
- `status`：`h.status || 'pending'`（`app.js:8355`）。✅
- `viewsCount`/`likesCount`/`commentsCount`/`sharesCount`：`h.viewsCount || 0` 等（`app.js:8357-8360`）。✅
- `publishedAt`：`h.executedAt ? formatDateTime(...) : (h.createdAt ? formatDateTime(...) : '—')`（`app.js:8361`）。✅
- `failReason`：`h.failReason || ''`（`app.js:8362`），非空才渲染失败原因块。✅
- `publishedVideoUrl`：`h.publishedVideoUrl || ''`，非空才显示"查看视频"链接（`app.js:8364,8377`）。✅
- 重试按钮：`canRetry && h.taskId` 才显示（`app.js:8363,8382`）。✅
- 日志按钮：`h.taskId` 才显示（`app.js:8381`）。✅

**唯一隐患**：由于问题1（JOIN失败），`h.accountName`/`h.platform` 恒为 null，但 `renderPublishHistory` 当前不渲染这两个字段，所以 UI 上不可见。若后续加账号列会暴露。

---

### 9. 矩阵账号树形选择器：点击账号是否正确加载发布历史？

**结论**：点击绑定正确，但因问题1导致数据恒空。⚠️

**依据**（`app.js:8303-8308`）：
```js
container.querySelectorAll('.ap-tree-account').forEach((el) => {
  el.addEventListener('click', () => {
    this.selectedAccountId = el.dataset.accId;
    this.renderAccountTree(); // 更新高亮
    this.fetchPublishHistory(el.dataset.accId);
  });
});
```
- `data-acc-id` 绑的是 `a.id`（`matrix_account.id`，`app.js:8286`）。✅
- 点击后调用 `fetchPublishHistory(accountId)`（`app.js:8307`）。✅
- `fetchPublishHistory` 调 `/api/auto-publish/publish-history?accountId=...`（`app.js:8334,8336`）。✅
- **但后端 SQL 因问题1返回空**，所以用户点任何账号都看到"该账号暂无发布历史"。

**矩阵名点击**：仅折叠/展开（`app.js:8311-8316`），不加载历史。属合理设计（矩阵下多账号，无法只看一个矩阵的历史）。✅

---

### 10. matrix tab 配置：选中矩阵时加载/保存是否正确？

**结论**：加载与保存流程正确。✅

**依据**：
- 选中矩阵时（`app.js:5550-5563`）调用 `fetchMatrixAutoPublishConfig(mxState.selectedId)`（`app.js:5562`）。✅
- `fetchMatrixAutoPublishConfig`（`app.js:5794-5805`）GET `/api/auto-publish/matrix-config/${matrixId}`，存 `mxState.autoPublishConfig`，调 `renderMatrixAutoPublishConfig(cfg)`。✅
- `saveMatrixAutoPublishConfig`（`app.js:5871-5912`）：
  - 从 DOM 读 enabled/presetId/dailyLimit/monitorIntervalHours/publishTimeSlots。✅
  - payload 含 `enabled`/`presetId`/`dailyLimit`/`monitorIntervalHours`/`publishTimeSlots`/`cdpInstanceId`（只读保留）。✅
  - PUT `/api/auto-publish/matrix-config/${matrixId}`。✅
  - 成功后 `await fetchMatrixAutoPublishConfig(matrixId)` 重新渲染。✅
- 时间段校验（`app.js:5886-5893`）：支持 `HH:MM-HH:MM` 与 `HH:MM` 格式，逗号分隔，校验失败不保存。✅

**潜在问题（非Bug）**：`renderMatrixAutoPublishConfig` 引用 `autoPublish.presets`/`autoPublish.cdpInstances`（`app.js:5811,5817`）。若用户先访问 matrix tab 再访问 auto-publish tab，`autoPublish` 可能尚未 `fetchPresets`/`fetchCdpInstances`，导致配置区下拉为空。`typeof autoPublish !== 'undefined'` 防御了未定义，但未防御"已定义但 presets 为空"。建议 matrix tab 选中矩阵时也触发 autoPublish 的 presets/cdp 预加载。

---

## 六、修复优先级建议

| 优先级 | 问题 | 建议动作 |
|--------|------|----------|
| 🔴 P0 | JOIN 字段语义不匹配（问题1） | 必须修复，否则按账号查发布历史完全不可用 |
| 🟠 P1 | logs API 硬编码"发布任务"过滤（问题2） | 去掉硬编码，改为按来源/时间范围过滤 |
| 🟡 P2 | fetchMonitorData 空数组判断（问题3） | 条件改为 `if (this.matrices)` |
| 🟡 P2 | fetchPublishHistory 竞态（问题4） | 引入请求序号 token |
| 🟢 P3 | filterLogLevel 死代码（问题5） | 删除赋值或让 fetchPublishLogs 读取它 |

---

## 七、附：未在本轮范围但观察到的其他问题

1. **`listPipelineTasks` 的账号筛选子查询同样依赖 `j.account_id = acc.accountName`**（`database.js:2101`），与问题1同源——`j.account_id`（profileId）≠ `acc.accountName`（TikTok用户名），子查询不匹配。流水线按账号筛选（`fetchPipelineTasks` 传 `accountId`，`app.js:8024`）可能也返回空。需一并修复。
2. **`listPipelineTasks` 的 `platform`/`account_name` 子查询用 `LIMIT 1`**（`database.js:2109-2110`），取的是矩阵下第一个账号，不一定是任务实际发布的账号。精度问题，非Bug。
3. **`autoPublish.init()` 每次切 tab 都重新拉取全部数据**（fetchPresets/fetchProfiles/fetchCdpInstances/fetchPipelineTasks/fetchPublishLogs/fetchMatrices），无节流。频繁切 tab 产生冗余请求。可加最小间隔节流。

---

*审查完成。以上结论均基于 `app.js`（8453行）、`index.html`（1893行）、`server.js`（5066行）、`database.js`（2743行）、`auto-publish-scheduler.js`（1155行）的实际代码核对，未做任何代码修改。*
