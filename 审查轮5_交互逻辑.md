# 审查轮5：前端交互逻辑 + 事件绑定正确性

> 审查对象：`public/app.js`（autoPublish 对象新方法 + matrix tab 配置交互）、`public/index.html`（新按钮/输入框事件绑定）
> 审查范围：第1轮重构后（auto-publish 设为默认 tab，配置移到 matrix tab，auto-publish 改为发布历史页）
> 审查方式：只读代码审查，不改代码

---

## 一、总体结论

重构的整体结构是清晰的：`autoPublish` 对象承担发布历史页的展示职责，matrix tab 通过 `fetchMatrixAutoPublishConfig` / `saveMatrixAutoPublishConfig` 承担配置职责，两者职责分离合理。但存在 **1 个会导致页面加载即崩溃的致命 Bug**、**2 个会导致功能失效的严重问题**，以及若干中低风险问题。建议优先修复致命与严重项后再合并。

---

## 二、问题清单（按严重程度排序）

### 🔴 P0 致命：初始化即崩溃（`autoPublish` 在声明前被引用，Temporal Dead Zone）

- **位置**：`app.js:7860` 触发，`app.js:7865` 声明
- **现象**：脚本末尾 `document.querySelector('.platform-tab[data-platform="auto-publish"]')?.click();`（第7860行）会触发 tab 切换逻辑，进而调用 `autoPublish.init()`（第2647行）。但 `const autoPublish = {...}` 的声明在第7865行，**位于 `.click()` 调用之后**。由于 `autoPublish` 用 `const` 声明，在执行到第7860行时它处于"暂时性死区（TDZ）"，访问会抛出 `ReferenceError: Cannot access 'autoPublish' before initialization`。
- **影响**：页面首次加载时，自动发布 tab 作为默认 tab 会被程序化点击，直接抛错。该错误在 `app.js` 作为 ES module 顶层执行时会导致**整个模块脚本中断**，autoPublish 后续所有方法（轮询、渲染、绑定）都无法注册到任何事件，发布历史页完全不可用。
- **验证依据**：`const autoPublish` 无函数/`var` 提升；`typeof autoPublish !== 'undefined'` 的防御（第5811行）在 TDZ 下同样会抛错，不能救场。
- **修复方向**：将 `.click()` 模拟移到 `autoPublish` 声明之后；或改为在 `autoPublish` 定义后显式调用 `autoPublish.init()`；或将 `autoPublish` 改为 `var`/函数声明形式（不推荐，治标不治本）。

---

### 🟠 P1 严重：自动发布开关 change 事件未绑定，切换不自动保存

- **位置**：`app.js:5807-5868`（`renderMatrixAutoPublishConfig`）、`app.js:7970-7971`（事件绑定区）
- **现象**：审查要求第7点明确要求"自动发布开关 change 事件是否正确触发保存"。代码中 `#mx-ap-enabled`（自动发布开关，第5827行）、`#mx-ap-preset`、`#mx-ap-daily`、`#mx-ap-interval`、`#mx-ap-slots` 这些配置输入框**均未绑定任何 `change` 事件**。只有 `#mx-save-autopublish` 按钮绑定了 click→`saveMatrixAutoPublishConfig`（第5915行）。
- **影响**：用户切换自动发布开关后，必须手动点"保存配置"按钮才生效。这与审查期望（"自动发布开关 change 事件正确触发保存"）不符。若产品意图是"开关即时保存、其他字段走按钮保存"，则当前完全缺失开关的即时保存逻辑；若意图是"全部走按钮"，则功能可用但体验上开关无即时反馈。需确认产品意图——但当前代码与审查要求第7点直接冲突。
- **修复方向**：为 `#mx-ap-enabled` 单独绑定 `change` → 调用 `saveMatrixAutoPublishConfig()`（仅提交 `enabled` 字段或全量提交）；或在 `renderMatrixAutoPublishConfig` 渲染后为开关绑定即时保存。

---

### 🟠 P1 严重：发布日志级别筛选器 change 只重渲染本地数据，不重新拉取

- **位置**：`app.js:7971`（绑定）、`app.js:8402-8415`（fetch）、`app.js:8417-8436`（render）
- **现象**：`#ap-log-filter` 的 change 事件绑定的是 `() => this.renderPublishLogs()`（第7971行），即**只重新渲染本地已缓存的 `publishLogs`**。但 `fetchPublishLogs`（第8402行）在拉取时已经把 `level` 作为查询参数传给后端（第8407行），意味着后端做了服务端过滤。当用户切换 level 时：
  - 若本地缓存里没有该级别的日志（因为首次拉取时 `level=''` 拿了全部 50 条，切换到 `error` 时本地有数据可过滤）——**当前恰好能工作**，因为首次拉取 level 为空拿到了全量。
  - 但 `renderPublishLogs`（第8424-8425行）又对本地数据做了一次客户端过滤 `this.publishLogs.filter(l => l.level === levelFilter)`，形成**双重过滤**：服务端已过滤 + 客户端再过滤。
  - 更隐蔽的问题：如果用户先选 `error`（此时 fetch 带 level=error 只拿 error 日志），再切回 `""`（全部），change 只触发 render 不触发 fetch，于是本地只有 error 日志，"全部"视图却只显示 error 日志——**数据不一致**。
- **影响**：在"先选具体级别再切回全部"的交互路径下，日志列表显示不完整，用户误以为没有其他级别日志。
- **修复方向**：change 事件应调用 `this.fetchPublishLogs()`（重新拉取）而非 `this.renderPublishLogs()`（仅本地过滤）；或移除 fetch 中的服务端过滤、统一用客户端过滤。

---

### 🟡 P2 中：流水线"立即发布"按钮在 `retry` 状态下可能误触发无 publishJobId 的任务

- **位置**：`app.js:8144`、`app.js:8069-8084`
- **现象**：`canPublish = (status === 'scheduled' || status === 'retry') && t.publishJobId`（第8144行）判定是否显示"立即发布"按钮。`triggerPublishTask`（第8069行）内部又做了一次 `if (!task || !task.publishJobId)` 防御。逻辑本身正确，但 `retry` 状态的任务通常意味着上次发布失败、可能需要重新混剪而非直接重发。将 `retry` 与 `scheduled` 并列允许"立即发布"，可能绕过混剪重试流程。
- **影响**：功能上不会崩溃（有 publishJobId 防御），但业务语义上 `retry` 状态直接发可能反复失败。属业务策略问题，非纯 Bug。
- **建议**：确认 `retry` 状态是否应允许"立即发布"，还是只允许"重试"（走 remixing 流程）。

---

### 🟡 P2 中：流水线"日志"按钮 data-logs 用 `publishJobId || id`，但日志接口按 `taskId` 查询

- **位置**：`app.js:8167`（按钮）、`app.js:8212-8213`（查询）
- **现象**：日志按钮 `data-logs="${t.publishJobId || t.id}"`（第8167行）——优先用 publishJobId，没有则用 task id。但 `togglePipelineLogs` 内 `const taskId = btn.dataset.logs; request(/api/auto-publish/logs?taskId=${taskId})`（第8212-8213行）始终以 `taskId` 作为查询参数名传给后端。
  - 当 `publishJobId` 存在时，实际传的是 publishJobId 值但放在 `taskId` 参数里。后端 `/api/auto-publish/logs?taskId=` 接口若严格按 taskId 字段查，则传 publishJobId 会查不到日志。
  - 当 `publishJobId` 不存在时退化为 task id，正常。
- **影响**：对于有 publishJobId 的任务，点"日志"可能查不到日志（取决于后端是否容忍 taskId 参数传 publishJobId 值）。需核对后端日志查询接口的字段语义。
- **修复方向**：明确日志查询键——若后端按 taskId 查，按钮应统一用 `t.id`；若支持 publishJobId，应传不同参数名。

---

### 🟡 P2 中：历史项"重新发布"按钮复用 `retryPipelineTask(taskId)`，但历史接口返回的 `taskId` 字段含义需确认

- **位置**：`app.js:8366`、`app.js:8054-8066`
- **现象**：`renderPublishHistory` 中"重新发布"按钮 `data-hist-retry="${h.taskId}"`（第8366行），点击调用 `retryPipelineTask(taskId)`（第8376行），即 `POST /api/auto-publish/pipeline/retry/${taskId}`。依赖发布历史接口返回的 `h.taskId` 是有效的流水线任务 id。若历史接口返回的 `taskId` 为 null 或指向已不存在的任务，重试会失败（后端 404）。
- **影响**：边界情况下"重新发布"报错，但已有 `canRetry && h.taskId` 防御（第8366行），不会显示无 taskId 的按钮。风险可控。
- **建议**：确认发布历史接口的 `taskId` 字段始终对应可重试的流水线任务。

---

### 🟢 P3 低：`renderAccountTree` 点击矩阵名仅折叠账号列表，点击矩阵名不加载该矩阵历史

- **位置**：`app.js:8294-8300`
- **现象**：矩阵名 `.ap-tree-matrix-name` 的 click 只做折叠/展开（第8296-8299行），不触发任何数据加载。审查要求第5点"点击矩阵/账号是否正确触发加载该账号的发布历史"——**账号点击正确**（第8287-8292行调用 `fetchPublishHistory`），矩阵点击无加载行为属于设计选择（矩阵下有多个账号，无法只看一个矩阵的历史），不构成 Bug。
- **状态**：符合预期，无需修复。仅记录确认。

---

### 🟢 P3 低：`fetchPublishHistory` 未做防抖/竞态保护

- **位置**：`app.js:8304-8327`
- **现象**：用户快速连续点击不同账号时，多个 `fetchPublishHistory` 并发，后发的请求可能先返回，导致 `renderPublishHistory` 渲染的是旧账号数据（竞态）。`this.publishHistory` 会被覆盖，`this.selectedAccountId` 虽然是最新值但渲染内容可能错位。
- **影响**：快速点击场景下右侧历史列表与左侧高亮账号短暂不一致。低概率、易复现但影响小。
- **修复方向**：引入请求序号（自增 token），渲染前校验 token 是否为最新。

---

### 🟢 P3 低：`_startPolling` 轮询矩阵数据后调用 `fetchMonitorData`，后者会再次请求 matrices 接口

- **位置**：`app.js:7974-7988`、`app.js:8030-8051`
- **现象**：轮询回调里 `request('/api/auto-publish/matrices')` 成功后赋值 `this.matrices` 并调用 `this.fetchMonitorData()`（第7985行）。`fetchMonitorData` 第8032行检查 `if (this.matrices && this.matrices.length)` 命中后直接复用内存数据，不会重复请求——**此处实际正确**。但若 matrices 为空数组（length=0），会走到第8040行再次 `request('/api/auto-publish/matrices')`，造成同一接口在一次轮询周期内请求两次。
- **影响**：仅当所有矩阵都没有 autoPublishConfig 或矩阵列表为空时多一次冗余请求。性能影响极小。
- **修复方向**：`fetchMonitorData` 的空数组判断改为 `if (this.matrices)` 而非 `this.matrices.length`。

---

## 三、审查要求逐项核对

| # | 审查项 | 结论 | 说明 |
|---|--------|------|------|
| 1 | `init()` 不再调 `fetchMatrices`，改 `fetchPipelineTasks`+`fetchPublishHistory` | ⚠️ 部分符合 | `init()`（7913-7929）调了 `fetchPipelineTasks`✅，但**未直接调 `fetchPublishHistory`**（该方法是按账号触发的，需用户点账号），调的是 `fetchMatrices().then(renderAccountTree)`。语义上合理（发布历史需先选账号），但与审查描述"改为 fetchPipelineTasks+fetchPublishHistory"有出入——实际是 fetchPipelineTasks + fetchMatrices+renderAccountTree。**非 Bug，描述偏差。** |
| 2 | `_startPolling` 轮询 `fetchPipelineTasks`+`fetchPublishHistory`，不再轮询 `fetchMatrices` | ❌ 不符合 | 轮询（7974-7988）调的是 `fetchPipelineTasks` + `fetchPublishLogs` + 内联 `request('/api/auto-publish/matrices')`（即仍在轮询 matrices，只是没走 `fetchMatrices` 方法名）。**未轮询 `fetchPublishHistory`**。发布历史按账号加载，不轮询属合理设计，但与审查描述不符。 |
| 3 | `renderPublishHistory` 字段匹配、空数据处理 | ✅ 正确 | 字段：materialTitle/status/viewsCount/likesCount/commentsCount/sharesCount/executedAt/createdAt/failReason/publishedVideoUrl/taskId（8337-8369）均有兜底。空数据显示"该账号暂无发布历史"（8334）。✅ |
| 4 | `renderAccountTree` 渲染矩阵→账号树形选择器 | ✅ 正确 | 矩阵分组、账号高亮、空账号提示、折叠功能完整（8259-8301）。✅ |
| 5 | 点击矩阵/账号触发加载发布历史 | ✅ 账号正确 | 账号点击→`fetchPublishHistory`✅；矩阵点击仅折叠（设计合理）。见 P3 记录。 |
| 6 | matrix tab 配置保存：选中矩阵 GET、保存 PUT、payload 完整 | ✅ 正确 | `fetchMatrixAutoPublishConfig` GET（5794-5805）✅；`saveMatrixAutoPublishConfig` PUT（5871-5912）✅；payload 含 enabled/presetId/dailyLimit/monitorIntervalHours/publishTimeSlots/cdpInstanceId✅。 |
| 7 | 自动发布开关 change 事件触发保存 | ❌ 不符合 | 见 P1 严重问题。开关无 change 绑定，仅靠保存按钮。 |
| 8 | 删除的方法是否还有引用 | ✅ 无残留 | 全局 `fetchMatrices`/`renderMatrices`（矩阵 tab 用，5527/5535）是 matrix 模块的独立函数，与 autoPublish 内的 `this.fetchMatrices`（7992）是不同作用域，非旧方法残留。`openMatrixAccountsModal` 未找到任何引用✅。 |
| 9 | 旧 DOM ID 引用 | ✅ 无残留 | `ap-matrices-list`、`ap-matrix-modal`、`ap-accounts-modal` 在 app.js 中**零引用**✅。HTML 中也无这些 ID。 |
| 10 | 流水线看板重试/立即发布/日志按钮 | ⚠️ 基本正常 | 重试✅、立即发布✅（见 P2 中业务策略疑问）、日志⚠️（见 P2 中 publishJobId/taskId 参数问题）。 |
| 11 | 播放数据弹窗 | ✅ 正常 | `openAnalyticsModal`（5973-6102）完整：创建 overlay、请求数据、汇总统计、直方图、折线图、错误兜底✅。绑定在 `renderMatrixAccounts` 的 `.mx-analytics-btn`（5660-5662）✅。 |

---

## 四、其他观察（非阻断）

1. **`autoPublish.init()` 可重入安全**：用 `_initialized` 标志（7914-7917）保证事件只绑定一次，多次切 tab 不会重复绑定✅。
2. **轮询可见性判断**：`_startPolling` 用 `!document.querySelector('.auto-publish-tab')?.classList.contains('hidden')` 判断 tab 可见才轮询（7978），避免后台轮询✅。
3. **`renderMatrixAutoPublishConfig` 引用 `autoPublish.presets`/`autoPublish.cdpInstances`**（5811/5817）：依赖 autoPublish 已初始化。若用户先访问 matrix tab 再访问 auto-publish tab，autoPublish 可能尚未 `fetchPresets`/`fetchCdpInstances`，导致配置区下拉为空。`typeof autoPublish !== 'undefined'` 防御了未定义，但未防御"已定义但 presets 为空"。建议 matrix tab 选中矩阵时也触发 autoPublish 的 presets/cdp 预加载。
4. **HTML 与 JS 的 ID 对齐**：逐一核对 `ap-pipeline-tbody`、`ap-monitor-list`、`ap-filter-matrix`、`ap-filter-account`、`ap-filter-status`、`ap-refresh-pipeline`、`ap-refresh-monitor`、`ap-refresh-tree`、`ap-account-tree`、`ap-history-title`、`ap-account-history-list`、`ap-refresh-logs`、`ap-log-filter`、`ap-logs-list`、`mx-autopublish-config`、`mx-save-autopublish` ——全部在 HTML 中存在且 JS 引用正确✅。

---

## 五、修复优先级建议

| 优先级 | 问题 | 建议动作 |
|--------|------|----------|
| P0 | TDZ 崩溃（7860 vs 7865） | 必须修复，否则页面不可用 |
| P1 | 开关 change 未绑定保存 | 确认产品意图后补充绑定 |
| P1 | 日志筛选器 change 不重新 fetch | 改为调用 fetchPublishLogs |
| P2 | 日志按钮 taskId/publishJobId 参数 | 核对后端接口字段 |
| P2 | retry 状态允许立即发布 | 确认业务策略 |
| P3 | 发布历史竞态、轮询冗余请求 | 可后续优化 |

---

*审查完成。以上结论均基于 `app.js`（8437 行）与 `index.html`（1893 行）的实际代码核对，未做任何代码修改。*
