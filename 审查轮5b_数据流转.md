# 第五轮（B）审查报告：端到端数据流转和边界情况验证

**审查日期**: 2026-09-19
**审查范围**: `public/app.js` + `src/server.js` + `src/database.js`
**审查重点**: 端到端数据流转、API 字段匹配、边界情况、残留死代码、CSS 类名、路由一致性
**项目**: D:/WILLLUXE/yix-repo/feedaccount (Node.js ESM)

---

## 问题汇总

| 严重程度 | 数量 |
|---------|------|
| 高危    | 1    |
| 中危    | 4    |
| 低危    | 5    |

---

## 高危问题

### H1. publish-history 接口在 `accountId` 存在但账号已被删除时静默回退为「返回全部任务」

**文件**: `src/server.js` 第 4270-4288 行；`src/database.js` 第 2089-2103 行（`listPipelineTasks` 同样模式）

**具体表现**:
`/api/auto-publish/publish-history` 的 `accountId` 分支：
```js
if (accountId) {
  let acc = null;
  if (matrixId) acc = store.listMatrixAccounts(matrixId).find(a => a.id === accountId);
  if (!acc) {
    const matrices = store.listMatrices();
    for (const m of matrices) {
      const found = store.listMatrixAccounts(m.id).find(a => a.id === accountId);
      if (found) { acc = found; break; }
    }
  }
  if (acc) { accountName = acc.accountName; ... }   // ← acc 为 null 时无 else 分支
  if (accountName) { where.push("(ma.account_name = ? OR j.account_id = ?)"); ... }
}
```

当用户点击树形选择器中某个账号，但该账号随后被删除（或在 `this.matrices` 缓存中存在但 DB 已不存在），`acc` 为 `null`，`accountName` 保持 `null`，**不会添加任何账号过滤条件**，SQL 返回该矩阵（或全表）的所有发布历史。

`listPipelineTasks`（`database.js` 第 2089-2103 行）也是完全相同的模式——`acc` 为 null 时不加 WHERE，返回全部任务。

**影响**:
- 用户在前端选了账号 A，但 API 实际返回的是账号 B/C/D… 的发布历史（全部任务），前端无法察觉，导致用户看到错误账号的数据，可能基于错误数据做决策（如重试、删除）。
- 树形选择器高亮账号 A，但右侧列表显示的是所有账号的内容——视觉与数据严重不一致。

**触发场景**:
1. 账号被另一端删除，但前端 `this.matrices` 缓存未刷新（10 秒轮询窗口内）。
2. 账号 ID 被手动篡改（URL 参数注入）。
3. 数据库迁移导致 `matrix_accounts` 表 ID 变化。

**修复建议**:
`accountId` 存在但 `acc` 为 null 时，应返回空数组 `{ result: [] }`（前端会正确显示「该账号暂无发布历史」），而不是静默放行全部数据。

---

## 中危问题

### M1. publish-history 返回 `status: 'success'`，但前端 `statusClass` 映射无 `'success'` 键，显示为灰色 pending 徽章

**文件**: `src/server.js` 第 4360 行；`public/app.js` 第 7897-7908 行、第 8355-8356 行

**具体表现**:
后端 `publish-history` 返回：
```js
status: r.job_status || r.status,   // job_status 来自 tk_publish_jobs.status
```

`tiktok-publish-manager.js` 第 94 行在发布成功后将 `tk_publish_jobs.status` 设为 `"success"`。而 pipeline 的 `p.status` 用的是 `"published"`。

前端 `statusClass` 映射表：
```js
const map = {
  pending, remixing, remixed, scheduled, published, failed, retry
  // ← 没有 'success'
};
return map[status] || 'ap-status-pending';   // 'success' 回退为 pending 样式
```

**影响**:
- 发布历史列表中，发布成功的任务显示灰色「success」文字徽章（用了 `ap-status-pending` 的灰色样式），视觉上像是「待处理」，与实际「已发布成功」状态不符。
- `renderPublishHistory` 中 `canRetry = status === 'failed' || status === 'retry'`——'success' 不触发重试按钮，这一点是对的。但视觉误导会降低用户信任。

**修复建议**:
方案 A：后端统一——`publish-history` 的 `status` 字段在 `job_status === 'success'` 时映射为 `'published'`。
方案 B：前端 `statusClass` 增加 `success: 'ap-status-published'` 映射，并将显示文字也统一为「published」。

### M2. `renderPipeline` 的 `viewsCount` 字段后端不返回，播放量列永远显示 `—`

**文件**: `public/app.js` 第 8164-8165 行、第 8177 行；`src/database.js` 第 2082-2152 行

**具体表现**:
`renderPipeline` 第 8165 行：
```js
const views = t.viewsCount != null ? t.viewsCount : '—';
```

但 `listPipelineTasks`（`database.js` 第 2127-2151 行）返回的对象**不包含 `viewsCount` 字段**——它只返回 `materialTitle, materialFilePath, jobExecutedAt, jobStatus, publishedVideoId, publishedVideoUrl` 等字段，没有做 `tk_video_analytics` 的聚合查询。

前端注释（第 8164 行）自己也承认：「播放量：需要从历史接口获取，这里先显示占位（pipeline 接口暂不含 analytics）」。

**影响**:
- 流水线看板的「播放量」列对用户毫无意义，永远是 `—`，是 UI 上的死列。
- 用户可能误以为数据缺失或功能未实现。

**说明**: 这属于**已知的设计缺口**而非 bug，但长期保留死列会误导用户。建议要么后端 `listPipelineTasks` 增加 `viewsCount` 聚合子查询（参考 `publish-history` 的写法），要么前端移除该列。

### M3. `_startPolling` 每 10 秒重建矩阵列表 DOM，滚动位置丢失

**文件**: `public/app.js` 第 7990-8004 行

**具体表现**:
```js
this._pollTimer = setInterval(() => {
  if (!document.querySelector('.auto-publish-tab')?.classList.contains('hidden')) {
    this.fetchPipelineTasks({ quiet: true });
    this.fetchPublishLogs({ quiet: true });
    request('/api/auto-publish/matrices').then(data => {
      this.matrices = Array.isArray(data) ? data : [];
      this._updateFilterOptions();     // 重建筛选器 <select> 选项
      this.fetchMonitorData();          // 重建监控列表 DOM
    }).catch(() => {});
  }
}, 10000);
```

`_updateFilterOptions()` 会重置 `<select id="ap-filter-matrix">` 和 `<select id="ap-filter-account">` 的 `innerHTML`，如果用户正在展开筛选下拉框，下拉框会被重置。
`fetchMonitorData()` → `renderMonitor()` 用 `container.innerHTML = ...` 重建监控列表，滚动位置丢失。

**影响**:
- 监控列表较长时，用户滚动到某处查看，10 秒后被重置回顶部。
- 树形选择器（`renderAccountTree`）虽未在轮询中调用，但 `_updateFilterOptions` 会重建筛选下拉——用户若正在选择矩阵/账号，选择状态会闪烁。

**说明**: 与上一轮 M5 同源问题，但本轮轮询路径已从 `fetchMatrices().then(renderMatrices)` 改为静默 `request().then(_updateFilterOptions)`，不再重建整个矩阵卡片。监控列表的 DOM 重建仍存在。

**修复建议**: 轮询时仅更新数据，用 diff 更新 DOM，或仅更新有变化的条目，而非整块 `innerHTML` 重建。

### M4. matrix-config PUT 的 `ratio`/`hashtagsJson`/`privacyLevel` 永远无法通过前端修改

**文件**: `public/app.js` 第 5879-5901 行；`src/server.js` 第 4225-4234 行

**具体表现**:
前端 `saveMatrixAutoPublishConfig` 的 payload：
```js
const payload = {
  enabled, presetId, dailyLimit, monitorIntervalHours,
  publishTimeSlots, cdpInstanceId（回填）
  // ← 没有 ratio, hashtagsJson, privacyLevel
};
```

后端 PUT 读取 `body.ratio` 等，但 body 中没有这些字段 → `undefined` → `upsertMatrixAutoPublishConfig` 的 `!== undefined` 检查跳过更新。

**影响**:
- `hashtagsJson` 无法配置，导致自动发布时 hashtags 为空（`tiktok-publish-manager` 读取 `job.materialHashtags`，而 material 的 hashtags 来自素材表，不来自矩阵配置）。
- `ratio` 固定为 `"9:16"`，`privacyLevel` 固定为 `"public"`。

**说明**: 这是上一轮 L3 的遗留，本轮确认仍未修复。如果业务上不需要在矩阵配置层修改这些字段（它们由素材层决定），则可降级为「设计决策」；但 `hashtagsJson` 字段在 DB 和 upsert 中都存在，说明原设计是支持矩阵级 hashtags 的，目前前端缺失对应 UI。

**修复建议**: 若需支持，在 `renderMatrixAutoPublishConfig` 增加 hashtags 输入框、ratio 下拉、privacy 下拉，并在 payload 中包含。

---

## 低危问题

### L1. `pipeline-log-row`、`col-account`、`col-views`、`col-title` CSS 类未定义

**文件**: `public/app.js` 第 8170、8171、8172、8177、8186-8188 行；`public/styles.css`

**具体表现**:
`renderPipeline` 生成的 HTML 使用了这些类：
- `<tr class="pipeline-log-row hidden">` （第 8186 行）
- `<td class="col-account">` （第 8170 行）
- `<td class="col-source">` （第 8171 行，已定义）
- `<td class="col-title">` （第 8172 行）
- `<td class="col-views">` （第 8177 行）

在 `styles.css` 中搜索：
- `.pipeline-log-row` → **未定义**
- `.col-account` → **未定义**
- `.col-title` → **未定义**
- `.col-views` → **未定义**
- `.col-source` → 已定义（第 4816 行）
- `.col-creator` → 已定义（第 4815 行）
- `.col-attempts` → 已定义（第 4817 行）
- `.col-time` → 已定义（第 4818 行）
- `.col-action` → 已定义（第 4819 行）

**影响**:
- `pipeline-log-row` 没有 CSS，但内联了 `style="padding:8px 12px;background:#f8fafc;"`，且 `.hidden` 类提供了 `display:none !important`——所以功能正常，只是缺少专属样式（如边框、圆角）。
- `col-account`、`col-title`、`col-views` 缺失样式，这些列没有特殊排版（如 `white-space:nowrap`、`max-width`、`text-overflow:ellipsis`），长标题/长账号名会撑宽表格，但 `.ap-table-wrap` 有 `overflow-x:auto` 兜底，不会破坏布局。

**修复建议**: 为 `col-title` 增加 `max-width` + `text-overflow:ellipsis`，为 `col-views` 增加 `text-align:right`，为 `pipeline-log-row` 增加边框样式。

### L2. `ap-monitor-count`、`ap-monitor-count.has-new` CSS 已定义但未被使用

**文件**: `public/styles.css` 第 4755-4767 行；`public/app.js` 第 8257-8267 行

**具体表现**:
`renderMonitor` 生成的 HTML：
```html
<div class="ap-monitor-item">
  <span class="ap-monitor-name">…</span>
  <span class="ap-monitor-time">…</span>
  <button data-monitor-trigger="…">立即监控</button>
</div>
```

CSS 中定义了 `.ap-monitor-count`（紫色徽章）和 `.ap-monitor-count.has-new`（绿色徽章），但 `renderMonitor` 从未输出这两个类。

**影响**: 死 CSS，无功能影响，但表明原设计的「新视频计数徽章」功能未实现。

**修复建议**: 若要显示新视频数，后端 `monitorSingleMatrix` 应返回 `{ newCount }`，前端在监控项右侧渲染 `<span class="ap-monitor-count ${newCount ? 'has-new' : ''}">${newCount}</span>`。若不需要，删除 CSS。

### L3. `auto-publish-tab` 类仅作 visibility toggle 钩子，无专属样式

**文件**: `public/index.html` 第 1558 行；`public/styles.css`

**具体表现**:
`<section class="auto-publish-panel auto-publish-tab">`，`auto-publish-tab` 在 `styles.css` 中无定义，仅靠 JS `classList.toggle('hidden', ...)` 控制显隐。

**影响**: 无 bug，`.hidden` 提供了 `display:none !important`，功能正常。属设计上可接受——标记为提示性。

### L4. `fetchMonitorData` 在 `init()` 中可能因 `fetchMatrices` 竞态而走 fallback 重复请求

**文件**: `public/app.js` 第 7936-7941 行、第 8046-8067 行

**具体表现**:
`init()`：
```js
this.fetchMatrices().then(() => {   // 异步
  this.fetchMonitorData();           // 在 then 内，确保 matrices 已就绪
  this.renderAccountTree();
  this._updateFilterOptions();
});
```

本轮已改为 `fetchMatrices().then(...)` 串行，`fetchMonitorData` 执行时 `this.matrices` 已填充，走 `if (this.matrices && this.matrices.length)` 分支，不会 fallback 重复请求。**本轮已修复上一轮 M1**。

但 `fetchMonitorData` 的 fallback 分支（第 8056-8066 行）仍保留——当 `this.matrices` 为空数组（`[]`，length 为 0）时仍会请求 `/api/auto-publish/matrices`。这只在矩阵确实为空时不会触发（因为 `length` 为 0，条件不满足走 fallback，但 fallback 请求返回空数组后 `monitorData` 也为空，`renderMonitor` 显示空状态）——逻辑正确，只是多一次请求。

**影响**: 无 bug，仅是冗余请求（矩阵为空时）。可忽略。

### L5. `_showHistoryLog` 使用 `alert()` 显示日志，与行内日志体验不一致

**文件**: `public/app.js` 第 8397-8413 行

**具体表现**:
发布历史项的「查看日志」按钮调用 `_showHistoryLog`，用 `alert(text)` 弹窗显示纯文本日志。而流水线表格的「日志」按钮调用 `togglePipelineLogs`，在行内展开渲染 HTML 格式日志（带颜色、时间）。

**影响**:
- 体验不一致：历史页用原生 `alert`（丑陋、不可滚动、长日志被截断），流水线页用行内 HTML。
- `alert` 会阻塞主线程，长日志（200 条）可能导致浏览器卡顿。

**修复建议**: 统一为 modal 或行内展开，移除 `alert`。

---

## 数据流转链路验证（逐项核对审查要求）

### 1. auto-publish tab 默认显示 ✅

- `index.html` 第 83 行：`<button class="platform-tab active" data-platform="auto-publish">`——tab 按钮默认 `active`。
- `index.html` 第 1558 行：`<section class="auto-publish-panel auto-publish-tab">`——无 `hidden` 类，默认可见。
- `app.js` 第 7871-7873 行：`setTimeout(() => { document.querySelector('.platform-tab[data-platform="auto-publish"]')?.click(); }, 0);`——页面加载后自动点击，触发 `autoPublish.init()`（第 2647 行）。

**结论**: ✅ 页面加载后 auto-publish tab 可见，`init()` 被调用。

### 2. init() 调用链 ✅

`init()`（第 7926-7942 行）：
```js
this._bindEvents();
this.fetchPresets();        → GET /api/ai-presets ✅
this.fetchProfiles();      → GET /api/profiles ✅
this.fetchCdpInstances();  → GET /api/cdp/instances ✅
this.fetchPipelineTasks(); → GET /api/auto-publish/pipeline?limit=100 ✅
this.fetchPublishLogs();   → GET /api/auto-publish/logs?limit=50 ✅
this.fetchMatrices().then(() => {
  this.fetchMonitorData();      → 复用 matrices 数据 ✅
  this.renderAccountTree();     → 渲染树 ✅
  this._updateFilterOptions();  → 更新筛选器 ✅
});
this._startPolling();      → 启动 10s 轮询 ✅
```

**结论**: ✅ 所有调用链正确执行，API 路径与后端路由匹配。

### 3. _startPolling 轮询内容 ✅（有 M3 体验问题）

```js
this.fetchPipelineTasks({ quiet: true });    → GET /api/auto-publish/pipeline ✅
this.fetchPublishLogs({ quiet: true });       → GET /api/auto-publish/logs ✅
request('/api/auto-publish/matrices')...      → GET /api/auto-publish/matrices ✅
  → _updateFilterOptions() + fetchMonitorData()
```

**结论**: ✅ 未轮询已删除的方法。轮询的 3 个接口均存在。但 `_updateFilterOptions` + `fetchMonitorData` 会重建 DOM（见 M3）。

### 4. publish-history API 字段匹配 ✅

后端返回（`server.js` 第 4348-4374 行）：
```
jobId, taskId, matrixId, matrixName, accountId, accountName, platform,
creatorId, sourceVideoId, materialTitle, materialFilePath, status, pipelineStatus,
viewsCount, likesCount, commentsCount, sharesCount, executedAt, scheduledAt,
failReason, attemptCount, publishedVideoId, publishedVideoUrl, createdAt, updatedAt
```

前端 `renderPublishHistory`（第 8353-8386 行）使用：
- `h.materialTitle` ✅
- `h.status` ✅（有 M1 样式问题）
- `h.viewsCount` ✅
- `h.likesCount` ✅
- `h.commentsCount` ✅
- `h.sharesCount` ✅
- `h.executedAt` ✅
- `h.createdAt` ✅
- `h.failReason` ✅
- `h.taskId` ✅
- `h.publishedVideoUrl` ✅

**结论**: ✅ 字段完全匹配。

### 5. pipeline API 字段匹配 ✅（有 M2 缺失字段）

后端 `listPipelineTasks` 返回（`database.js` 第 2127-2151 行）：
```
id, creatorId, creatorName, matrixId, matrixName, platform, accountName,
sourceVideoId, sourceUrl, remixTaskId, profileId, publishJobId, status,
failReason, attemptCount, createdAt, updatedAt, materialTitle, materialFilePath,
jobExecutedAt, jobStatus, publishedVideoId, publishedVideoUrl
```

前端 `renderPipeline`（第 8148-8194 行）使用：
- `t.matrixName` ✅
- `t.accountName` ✅
- `t.platform` ✅
- `t.status` ✅
- `t.attemptCount` ✅
- `t.failReason` ✅
- `t.createdAt` ✅
- `t.publishJobId` ✅
- `t.sourceVideoId` ✅
- `t.id` ✅
- `t.materialTitle` ✅
- `t.jobExecutedAt` ✅
- `t.viewsCount` ❌（后端不返回，见 M2）

**结论**: ✅ 除 `viewsCount` 外字段完全匹配。

### 6. 空数据边界 ✅

- `publish-history` 返回空数组 `[]`：
  - `fetchPublishHistory`：`this.publishHistory = []` → `renderPublishHistory` 显示「该账号暂无发布历史」✅
- `pipeline` 返回空数组：
  - `fetchPipelineTasks`：`this.pipelineTasks = []` → `renderPipeline` 显示「暂无流水线任务」✅
- `matrices` 返回空数组：
  - `fetchMatrices`：`this.matrices = []` → `renderAccountTree` 显示「暂无矩阵」✅
  - `fetchMonitorData`：`monitorData = []` → `renderMonitor` 显示「暂无监控数据」✅
- `analytics` 返回 `{ jobs: [] }`：
  - `openAnalyticsModal`：`if (!data.jobs || !data.jobs.length)` 显示「暂无播放数据」✅
- `logs` 返回空数组：
  - `togglePipelineLogs`：显示「该任务暂无日志」✅
  - `renderPublishLogs`：显示「暂无日志」✅

**结论**: ✅ 所有空数据边界正确处理。

### 7. 矩阵×账号树形选择器 ✅

- 数据来源：`fetchMatrices()` → `GET /api/auto-publish/matrices` → 返回 `[{ id, name, accounts: [{ id, platform, accountName }] }]`
- 点击账号：`el.dataset.accId` → `fetchPublishHistory(accId)` → `GET /api/auto-publish/publish-history?accountId=...&limit=100`
- 参数：`accountId` ✅，`limit=100` ✅

**结论**: ✅ 数据来源正确，点击调用的 API 和参数正确。（但有 H1 边界问题）

### 8. matrix tab 配置字段匹配 ✅

后端 `GET /api/auto-publish/matrix-config/:matrixId` 返回（`database.js` 第 1875-1889 行）：
```
matrixId, enabled, presetId, dailyLimit, monitorIntervalHours, lastMonitorAt,
publishTimeSlots, ratio, hashtagsJson, privacyLevel, cdpInstanceId, createdAt, updatedAt
```

前端 `renderMatrixAutoPublishConfig`（第 5807-5868 行）渲染：
- `cfg.enabled` ✅
- `cfg.presetId` ✅
- `cfg.dailyLimit` ✅
- `cfg.monitorIntervalHours` ✅
- `cfg.publishTimeSlots` ✅（JSON parse 兼容）
- `cfg.cdpInstanceId` ✅（只读显示）
- `cfg.ratio` ❌（不渲染，见 M4）
- `cfg.hashtagsJson` ❌（不渲染，见 M4）
- `cfg.privacyLevel` ❌（不渲染，见 M4）

**结论**: ✅ 已渲染字段完全匹配。未渲染的 3 个字段属 M4 设计缺口。

### 9. 保存配置 payload 与后端 upsert 参数 ✅（有 M4 遗漏）

前端 `saveMatrixAutoPublishConfig` payload（第 5879-5901 行）：
```
enabled, presetId, dailyLimit, monitorIntervalHours, publishTimeSlots, cdpInstanceId
```

后端 PUT 读取（`server.js` 第 4225-4234 行）：
```
body.enabled, body.presetId, body.dailyLimit, body.monitorIntervalHours,
body.publishTimeSlots, body.ratio, body.hashtagsJson, body.privacyLevel, body.cdpInstanceId
```

`upsertMatrixAutoPublishConfig`（`database.js` 第 1892-1944 行）参数：
```
enabled, presetId, dailyLimit, monitorIntervalHours, lastMonitorAt,
publishTimeSlots, ratio, hashtagsJson, privacyLevel, cdpInstanceId
```

**对应关系**:
| payload 字段 | 后端读取 | upsert 参数 | 结果 |
|---|---|---|---|
| enabled | ✅ | ✅ | 更新 |
| presetId | ✅ | ✅ | 更新 |
| dailyLimit | ✅ | ✅ | 更新 |
| monitorIntervalHours | ✅ | ✅ | 更新 |
| publishTimeSlots | ✅ | ✅ | 更新 |
| cdpInstanceId | ✅ | ✅ | 更新（回填） |
| ratio | ❌（未传） | undefined→跳过 | 不更新 |
| hashtagsJson | ❌（未传） | undefined→跳过 | 不更新 |
| privacyLevel | ❌（未传） | undefined→跳过 | 不更新 |

**结论**: ✅ 已传字段完全对应。未传字段因 `!== undefined` 保护不会误清空（传 `null` 才会清空）。属 M4 设计缺口。

### 10. 播放数据弹窗 ✅

- 触发：矩阵账号列表的「播放数据」按钮（`app.js` 第 5660-5662 行）→ `openAnalyticsModal(matrixId, accountName)`
- API：`GET /api/auto-publish/analytics/:matrixId`
- 后端：matrixId 以 `mx_` 开头 → 查 `matrix_profiles` 获取 `profileId` → 查 `tk_publish_jobs` + `tk_video_analytics`
- 返回字段：`{ profileId, jobCount, analyticsCount, jobs: [{ jobId, executedAt, status, publishedVideoId, recordCount, firstRecorded, lastRecorded, totalViews, maxViews, minViews, avgViews, records: [{ views, likes, comments, shares, recordedAt }] }] }`
- 前端使用：
  - `data.jobs` ✅
  - `job.records` ✅（后端保证 `records.length > 0`）
  - `job.totalViews` ✅
  - `job.jobId` ✅
  - `job.executedAt` ✅
  - `job.recordCount` ✅
  - `job.firstRecorded` / `job.lastRecorded` ✅
  - `r.views` ✅
- 空数组防护：`allViews.length ? Math.max(...) : 0` ✅（第 6024-6026 行）、`jobViews.length ? Math.max(...) : 0` ✅（第 6075-6077 行）

**结论**: ✅ 弹窗正常工作，字段匹配，边界已防护。上一轮 H1 已修复。

### 11. 重试/立即发布/日志按钮 ✅

- **重试**（`renderPipeline` 第 8181 行 `data-retry`）：
  - 点击 → `retryPipelineTask(t.id)` → `POST /api/auto-publish/pipeline/retry/:id`
  - 后端路由 `apRetryMatch`（第 4381 行）→ `store.updatePipelineTask(taskId, {status:'retry',...})` ✅
  - 显示条件：`canRetry = status === 'failed' || status === 'retry'` ✅
- **立即发布**（`renderPipeline` 第 8182 行 `data-publish`）：
  - 点击 → `triggerPublishTask(t.id)` → 查找 `task.publishJobId` → `POST /api/tiktok/publish/jobs/:jobId/execute`
  - 后端路由 `tkPublishExecuteMatch`（第 2220 行）→ `tiktokPublisherManager.executeJob(jobId)` ✅
  - 显示条件：`canPublish = (status === 'scheduled' || status === 'retry') && t.publishJobId` ✅
- **日志**（`renderPipeline` 第 8183 行 `data-logs`）：
  - 点击 → `togglePipelineLogs(taskId, btn)` → `GET /api/auto-publish/logs?taskId=...&limit=200`
  - 行内展开 ✅
- **历史页重试**（`renderPublishHistory` 第 8382 行 `data-hist-retry`）：
  - 点击 → `retryPipelineTask(h.taskId)` → 同上 ✅
- **历史页日志**（`renderPublishHistory` 第 8381 行 `data-hist-logs`）：
  - 点击 → `_showHistoryLog(h.taskId)` → `GET /api/auto-publish/logs?taskId=...` → `alert`（见 L5）✅

**结论**: ✅ 所有按钮正常工作。

### 12. 删除的旧方法引用 ✅

- `auto-publish-scheduler.js` 中搜索 `getCreatorAutoPublishConfig`/`updateCreatorAutoPublishConfig`/`listCreatorAutoPublishConfigs`/`listEnabledCreatorConfigs` → **0 匹配**，无残留。
- `server.js` 第 4480 行注释：「旧达人级自动发布 API 已删除（迁移到矩阵级 /api/auto-publish/*）」
- 前端 `autoPublish` 对象方法全部存在：`init, _bindEvents, _startPolling, fetchMatrices, fetchPipelineTasks, fetchProfiles, fetchMonitorData, fetchCdpInstances, fetchPresets, fetchPublishHistory, fetchPublishLogs, retryPipelineTask, triggerPublishTask, triggerMonitor, togglePipelineLogs, _showHistoryLog, _updateFilterOptions, _updateAccountFilterOptions, renderPipeline, renderMonitor, renderAccountTree, renderPublishHistory, renderPublishLogs, statusClass`——全部有定义，无调用未定义方法。

**结论**: ✅ 无残留死方法引用。

### 13. CSS 类名 ✅（有 L1/L2 缺失/冗余）

前端使用的 auto-publish 相关类 → CSS 定义状态：
| 类名 | 状态 |
|---|---|
| `ap-status-badge` + 7 个状态变体 | ✅ 全部定义 |
| `ap-monitor-item` / `ap-monitor-name` / `ap-monitor-time` | ✅ |
| `ap-tree-matrix` / `ap-tree-matrix-name` / `ap-tree-accounts` / `ap-tree-account` / `ap-tree-account.active` / `ap-tree-account-platform` | ✅ |
| `ap-history-item` + 4 个子类 | ✅ |
| `empty-state` / `empty-state.compact` | ✅ |
| `failed-row` | ✅ |
| `ap-fail-reason` | ✅ |
| `ap-monitor-list` / `ap-tree-list` / `ap-history-list` | ✅ |
| `ap-filter-select` / `ap-table` / `ap-table-wrap` | ✅ |
| `col-creator` / `col-source` / `col-attempts` / `col-time` / `col-action` | ✅ |
| `mx-ap-row` / `mx-ap-label` / `mx-ap-value` / `mx-ap-hint` / `mx-ap-switch` / `mx-ap-switch-slider` | ✅ |
| `matrix-autopublish-area` / `mx-autopublish-config-body` | ✅ |
| `auto-publish-panel` / `ap-main-col` / `ap-section-head` / `ap-monitor-section` / `ap-pipeline-section` / `ap-history-split` / `ap-tree-col` / `ap-history-detail-col` | ✅ |
| `link-btn` | ✅ |
| `hidden` | ✅ |
| `muted-activity` | ✅ |
| `platform-tiktok` | ✅ |
| `pipeline-log-row` | ❌ 未定义（见 L1） |
| `col-account` | ❌ 未定义（见 L1） |
| `col-title` | ❌ 未定义（见 L1） |
| `col-views` | ❌ 未定义（见 L1） |
| `ap-monitor-count` / `ap-monitor-count.has-new` | ⚠️ 已定义但未使用（见 L2） |

**结论**: ✅ 关键类全部定义。4 个非关键类缺失（L1），2 个类冗余（L2），不影响功能。

### 14. 前端 API 路径与后端路由一致性 ✅

| 前端调用 | 后端路由 | 一致性 |
|---|---|---|
| `GET /api/auto-publish/matrices` | `server.js` 4184 | ✅ |
| `GET /api/auto-publish/matrix-config/:matrixId` | `server.js` 4211 (`apMatrixCfgMatch`) | ✅ |
| `PUT /api/auto-publish/matrix-config/:matrixId` | `server.js` 4211 (`apMatrixCfgMatch`) | ✅ |
| `GET /api/auto-publish/pipeline?matrixId&status&accountId&limit` | `server.js` 4242 | ✅ |
| `GET /api/auto-publish/publish-history?matrixId&accountId&limit` | `server.js` 4256 | ✅ |
| `POST /api/auto-publish/pipeline/retry/:id` | `server.js` 4381 (`apRetryMatch`) | ✅ |
| `POST /api/auto-publish/monitor/:matrixId` | `server.js` 4390 (`apMonitorMatch`) | ✅ |
| `GET /api/auto-publish/logs?taskId&level&limit` | `server.js` 4403 | ✅ |
| `GET /api/auto-publish/analytics/:id` | `server.js` 4435 (`apAnalyticsMatch`) | ✅ |
| `POST /api/tiktok/publish/jobs/:jobId/execute` | `server.js` 2220 (`tkPublishExecuteMatch`) | ✅ |
| `GET /api/ai-presets` | `server.js` 4767 | ✅ |
| `GET /api/profiles` | `server.js` 1532 | ✅ |
| `GET /api/cdp/instances` | `server.js` 1584 | ✅ |

**结论**: ✅ 所有前端调用的 API 路径与后端路由完全一致，参数名（query/path/body）全部匹配。

---

## 建议修复优先级

1. **H1**（accountId 静默回退返回全部数据）—— 立即修复，可能导致数据泄露给错误用户视图
2. **M1**（status='success' 显示为 pending 样式）—— 尽快修复，视觉误导
3. **M2**（pipeline 播放量列永远为 —）—— 决策：补数据 or 移除列
4. **M4**（hashtags/ratio/privacy 无法配置）—— 若需功能则增加 UI
5. **M3**（轮询重建 DOM 滚动丢失）—— 优化渲染策略
6. **L5**（alert 显示日志）—— 统一为 modal
7. **L1**（缺失 CSS 类）—— 补充样式
8. **L2**（冗余 CSS）—— 删除或实现新视频计数功能

---

## 与上一轮审查的对比

| 上一轮问题 | 本轮状态 |
|---|---|
| H1（空数组 Math.max 崩溃） | ✅ 已修复（第 6024-6026、6075-6077 行增加 `length` 防护） |
| H2（并发限制绕过） | ⚠️ 未在本轮范围内（tiktok-publish-manager.js）|
| M1（init 重复请求 matrices） | ✅ 已修复（改为 `fetchMatrices().then(...)` 串行） |
| M5（轮询闪烁） | ⚠️ 部分修复（不再重建矩阵卡片，但监控列表仍重建——见 M3） |
| L3（hashtags 无法配置） | ❌ 仍未修复（见 M4） |
