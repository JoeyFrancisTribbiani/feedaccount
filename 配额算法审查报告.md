# 混剪配额算法审查报告

**审查文件**: `src/auto-publish-scheduler.js` — `triggerRemixTasks` (L366-460) + `_doRemix` (L462-554)
**审查日期**: 2026-09-19
**审查范围**: 库存计算 / SQL 查询 / 配额边界 / 混剪触发 / 死锁

---

## 问题汇总

| # | 严重程度 | 问题 | 位置 |
|---|---------|------|------|
| 1 | **高危** | pending 计入 stockCount 导致永久死锁 | L427-430 |
| 2 | **高危** | published "只算今天" SQL 逻辑错误，导致库存虚高 | L404-420 |
| 3 | **中危** | _doRemix 中 matrix_id 补全在 cdpInstanceId 检查之后，旧数据必失败 | L474-506 |
| 4 | **中危** | _doRemix 缺少视频存在性检查（与 retryFailedTasks 不一致） | L465 |
| 5 | **低危** | candidates 三元表达式冗余（else 分支为死代码） | L438-440 |
| 6 | **低危** | COALESCE WHERE 子查询为死代码 | L388-391 |
| 7 | **低危** | todayPublished 变量赋值后未使用（浪费 DB 查询） | L423 |
| 8 | **低危** | dailyLimit 配置加载用 `\|\|` 而非 `??`，dailyLimit=0 不可能 | database.js L1879/1954 |
| 9 | **低危** | published 检查存在 N+1 查询性能问题 | L404-420 |
| 10 | **低危** | return 在 for 循环中只触发一个混剪（性能瓶颈） | L458 |

---

## 1. 【高危】pending 计入 stockCount 导致永久死锁

**位置**: L427-430

```js
const pendingForMatrix = allPending.filter(p =>
  p.matrix_id === cfg.matrixId || (p.matrix_id === null && profileId && p.profile_id === profileId)
);
stockCount += pendingForMatrix.length;  // ← BUG
```

**问题**: `pending` 状态的 pipeline（已下载但未混剪）被计入库存。pending 不是成品，它还需要经过混剪才能变成可发布的 `remixed`/`scheduled`/`published`。但 stockCount 把它当作已有库存，导致：

- **场景**: `dailyLimit=3`，`targetStock=4`。矩阵有 4 个 pending pipeline，0 个 remixed/scheduled/published。
- `stockCount = 0 + 0 + 0 + 0 + 4(pending) = 4 >= 4` → `continue`（跳过混剪）
- pending 永远不会被混剪（因为 `_doRemix` 从不被调用），pending 计数永不下降
- **永久死锁**: pending → 不混剪 → pending 不变 → 不混剪 → ...

**更极端的 case**: `dailyLimit=0` 时 `targetStock=1`，哪怕只有 1 个 pending 就触发死锁。

**根因**: pending 的语义是"待处理原料"，不是"可用库存"。它代表"有视频可混剪"而非"有成品可发"。把原料当库存，阻止了唯一的转化路径（pending → remixing）。

**影响**: 一旦某矩阵积累了 ≥ `targetStock` 个 pending pipeline，该矩阵的所有待混剪视频永远无法被处理。

---

## 2. 【高危】published "只算今天" SQL 逻辑错误，库存虚高

**位置**: L404-420

```js
for (const p of matrixPipelinesAll) {
  if (["published", "scheduled", "remixed", "remixing"].includes(p.status)) {
    if (p.status === "published") {
      // 检查是否今天发布的
      const jobRow = this.store.db.prepare(
        `SELECT j.executed_at FROM tk_publish_jobs j
         JOIN auto_remix_publish_pipeline p ON p.publish_job_id = j.id
         WHERE p.status = 'published' AND (p.matrix_id = ? OR (p.matrix_id IS NULL AND p.profile_id = ?))
         AND j.executed_at LIKE ?`
      ).get(cfg.matrixId, profileId, `${todayStr}%`);  // ← BUG
      if (jobRow) stockCount++;
    } else {
      stockCount++;
    }
  }
}
```

**问题**: 该 SQL 查询在 `for` 循环内执行，每次迭代检查的是"该矩阵是否有**任意一条** published pipeline 今天发布过"，而不是"**当前这条** pipeline 是否今天发布"。

SQL 的 WHERE 子句没有过滤当前迭代的 `p.id`，只按 `matrix_id`/`profile_id` + `executed_at LIKE today` 过滤。`.get()` 返回第一条匹配行。

**后果**: 如果矩阵有 3 条 published pipeline（2 条昨天发布，1 条今天发布），循环 3 次：
- 每次都查到"有今天发布的"→ `jobRow` 非空 → `stockCount++`
- 3 条全部被计入 → `stockCount` 虚高 +2

**验证**: 模拟 3 published (2 yesterday + 1 today) + 1 scheduled + 1 remixed：
- 错误 stockCount = 5（3 published 全算 + 1 scheduled + 1 remixed）
- 正确 stockCount = 3（1 published today + 1 scheduled + 1 remixed）
- 虚高 +2

**影响**: 库存虚高导致提前停止混剪，配额用不满。矩阵的 dailyLimit 越高、历史 published 越多，误差越大。

---

## 3. 【中危】_doRemix 中 matrix_id 补全在 cdpInstanceId 检查之后

**位置**: L474-506

```js
const effectiveMatrixId = pipeline.matrix_id;           // L474 — 可能为 null
let cdpInstanceId = effectiveCfg?.cdpInstanceId || null; // L475
if (!cdpInstanceId && effectiveMatrixId) {              // L476 — effectiveMatrixId 为 null 时不查
  const mp = ...WHERE matrix_id = ?...get(effectiveMatrixId);
  cdpInstanceId = mp?.profile_id || null;
}
if (!cdpInstanceId) {                                    // L485 — 为 null 直接标记失败并 return
  this._updatePipeline(pipeline.id, { status: "failed", ... });
  return;                                                // ← 在这里退出
}
// 尝试从旧表补全 matrix_id                              // L495 — 永远到不了
if (!pipeline.matrix_id) {
  const mac = ...WHERE mac.creator_id = ?...
  if (mac?.matrix_id) {
    ...UPDATE... SET matrix_id = ?...
    pipeline.matrix_id = mac.matrix_id;
  }
}
```

**问题**: 执行顺序错误。`matrix_id` 补全（L495-506）在 `cdpInstanceId` 空值检查（L485-492）之后。当 `pipeline.matrix_id` 为 null 且 `effectiveCfg.cdpInstanceId` 也为 null 时：
1. `effectiveMatrixId = null`
2. `cdpInstanceId = null`（无法用 null 查 `matrix_profiles`）
3. L485: `!cdpInstanceId` 为 true → 标记 failed → **return**
4. L495 的 `matrix_id` 补全**永远执行不到**

**触发条件**: 旧数据 pipeline（`matrix_id IS NULL`）通过 `profile_id` 匹配进入 `triggerRemixTasks` 的 `pendingForMatrix`，但矩阵配置没有显式设置 `cdp_instance_id`（依赖 `matrix_profiles` 查询）。此时 `_doRemix` 会直接失败，而不会尝试补全 `matrix_id` 后重新查 `matrix_profiles`。

**对比**: `_scheduleOnePipeline`（L636-648）和 `retryFailedTasks`（L933-956）都在配置查找之前做 `matrix_id` 补全，顺序正确。唯独 `_doRemix` 顺序反了。

---

## 4. 【中危】_doRemix 缺少视频存在性检查

**位置**: L465

```js
if (!pipeline.source_video_id) {   // ← 只检查是否为 falsy
  this._updatePipeline(pipeline.id, { status: "failed", failReason: "Pipeline 缺少 source_video_id" });
  return;
}
```

**问题**: `_doRemix` 只检查 `source_video_id` 是否为 falsy，不检查视频是否真实存在。

**对比** `retryFailedTasks`（L959-967）:
```js
const video = this.store.getRemixVideo(pipeline.source_video_id);
if (!video) {
  this._updatePipeline(pipeline.id, { status: "failed", failReason: "重试失败: 视频不存在" });
  return;
}
```

**后果**: `_downloadAndCreatePipelineForMatrix`（L275）在下载失败时会写入 `source_video_id = "pending_download"`。这个字符串是 truthy，通过 L465 检查，随后被发送给 AI 混剪 API：
```js
videoIds: [pipeline.source_video_id],  // → ["pending_download"]
```
API 会因为找不到视频而失败，pipeline 被标记 failed。虽然最终结果是 failed（不会无限重试），但浪费了一次 API 调用 + 错误日志，且 `failReason` 不够明确。

---

## 5. 【低危】candidates 三元表达式冗余

**位置**: L438-440

```js
const candidates = pendingForMatrix.length ? pendingForMatrix : allPending.filter(p =>
  p.matrix_id === cfg.matrixId || (p.matrix_id === null && profileId && p.profile_id === profileId)
);
```

**问题**: `pendingForMatrix`（L427-429）已经用完全相同的 filter 逻辑从 `allPending` 过滤过了。当 `pendingForMatrix.length > 0` 时，返回 `pendingForMatrix`；当 `pendingForMatrix.length === 0` 时，else 分支用**同样的 filter** 再过滤一次 `allPending`，结果必然也是空数组。

**验证**: `pendingForMatrix === allPending.filter(相同条件)` 恒为 true。else 分支是死代码，永远返回 `[]`。

**影响**: 无功能影响，但代码可读性差，暗示作者可能想表达不同的逻辑（比如 fallback 到全量 pending）。

---

## 6. 【低危】COALESCE WHERE 子查询为死代码

**位置**: L388-391

```js
const matrixPipelines = this.store.db.prepare(
  `SELECT status, profile_id FROM auto_remix_publish_pipeline
   WHERE matrix_id = COALESCE(?, (SELECT mp.matrix_id FROM matrix_profiles mp WHERE mp.profile_id = profile_id))`
).all(cfg.matrixId);
```

**问题**: `COALESCE(?, subquery)` 中，`?` 绑定的是 `cfg.matrixId`（非 NULL 字符串）。`COALESCE` 返回第一个非 NULL 参数，所以**永远返回 `cfg.matrixId`**，子查询**永远不执行**。

等价于: `WHERE matrix_id = cfg.matrixId`。COALESCE + 子查询是误导性的死代码，不提供任何旧数据兼容（旧数据兼容实际由 L396-401 的 `matrixPipelinesAll` 处理）。

**注意**: 当 `profileId` 存在时（常见 case），`matrixPipelines` 根本不被使用（L396-401 用 `matrixPipelinesAll` 替代）。`matrixPipelines` 仅在 `profileId` 为 null 时作为 fallback，此时它只匹配 `matrix_id = cfg.matrixId`，无法匹配 `matrix_id IS NULL` 的旧数据。

---

## 7. 【低危】todayPublished 变量赋值后未使用

**位置**: L423

```js
const todayPublished = profileId ? this._countTodayPublishedByProfile(profileId) : 0;
```

**问题**: `todayPublished` 被赋值但在 `triggerRemixTasks` 中**从未被使用**。`stockCount` 已经通过 L404-420 的循环包含了 published（今天的）。

**影响**: 每次调用 `triggerRemixTasks` 遍历每个矩阵时，都会执行一次 `_countTodayPublishedByProfile`（内部调用 `listTkPublishJobs` 查最多 500 条 job），纯属浪费 DB I/O。

---

## 8. 【低危】dailyLimit 配置加载用 `||` 而非 `??`

**位置**: `database.js` L1879, L1954

```js
// getMatrixAutoPublishConfig (L1879)
dailyLimit: Number(row.daily_limit || 3),

// listEnabledMatrixConfigs (L1954)
dailyLimit: Number(r.daily_limit || 3),
```

**问题**: 使用 `||` 运算符，当 `daily_limit = 0` 时（用户想禁用发布），`0 || 3` = 3，dailyLimit 被强制变成 3。

虽然 `triggerRemixTasks`（L384）用 `??` 正确处理了 0：
```js
const dailyLimit = cfg.dailyLimit ?? 3;
```
但配置加载层已经把 0 转成了 3，`??` 永远看不到 0。`_scheduleOnePipeline`（L663）也用 `||`：
```js
const dailyLimit = effectiveCfg?.dailyLimit || 3;
```

**影响**: `dailyLimit=0`（禁用自动发布）不可能实现。如果产品不需要这个功能，则无影响。

---

## 9. 【低危】published 检查 N+1 查询性能问题

**位置**: L404-420

每次循环到一条 `published` pipeline 时，都执行一次 SQL 查询（L409-414）。如果矩阵有 N 条 published pipeline，就执行 N 次相同的查询（返回相同的结果）。

**影响**: published pipeline 多时，`triggerRemixTasks` 每次调用会执行大量重复 SQL。10 个矩阵各 10 条 published = 100 次重复查询。

**正确做法**: 在循环外用一条 `COUNT(*)` 查询统计今天 published 的数量，或用单条 JOIN 查询带出每条 pipeline 的 `executed_at`。

---

## 10. 【低危】return 在 for 循环中只触发一个混剪

**位置**: L458

```js
await this._doRemix(pipeline, cfg);
return; // 每次只触发一个混剪任务
```

`return` 退出整个 `for (const cfg of configs)` 循环，每次 `triggerRemixTasks` 调用只触发 1 个混剪。结合 `REMIX_INTERVAL_MS = 5 分钟` 和主循环 60 秒间隔：

- 最多每 5 分钟触发 1 次混剪
- 10 个矩阵各需 4 次混剪 = 40 次 = 最少 200 分钟（3.3 小时）

**影响**: 纯性能瓶颈，不影响正确性。当矩阵多、配额高时，库存补充速度可能跟不上发布消耗速度。

---

## 11. 与排期发布的交互分析

### 11.1 dailyLimit 检查口径不一致（低危）

- `triggerRemixTasks` 的 stockCount 中，published 按**矩阵**统计（`matrix_id`/`profile_id` 过滤 pipeline）
- `_scheduleOnePipeline` 的 `todayPublished` 用 `_countTodayPublishedByProfile(profileId)`，按 **profile** 统计所有 `tk_publish_jobs`（含手动创建的、非 pipeline 的 job）

**场景**: 用户手动通过其他方式发布了 3 条视频（创建了 `tk_publish_jobs` 但无 pipeline），`_countTodayPublishedByProfile` 会计入这 3 条。但 `triggerRemixTasks` 的 stockCount 不含这 3 条（因为没有对应的 pipeline）。

**后果**: `triggerRemixTasks` 认为库存不足，继续混剪；但 `_scheduleOnePipeline` 发现 `todayPublished >= dailyLimit`，拒绝排期。混剪成品滞留在 `remixed` 状态，直到次日 `todayPublished` 重置。非永久死锁，但造成 1 天的延迟 + 1 次浪费的混剪。

### 11.2 targetStock = dailyLimit + 1 导致轻微超产（低危）

`targetStock = dailyLimit + 1` 的设计意图是"多混 1 条备用"。但当 `dailyLimit` 已达上限时：
- `stockCount` 含 `scheduled`（已排期但未执行）
- 假设 3 条已排期 + 1 条 remixed = stockCount 4 = targetStock → 停止混剪
- 但如果 3 条排期全部执行成功（todayPublished=3），`_scheduleOnePipeline` 不会再排期第 4 条
- 第 4 条 remixed 滞留到次日

这是**设计意图**（备用来日首条发布），不是 bug。但若次日 monitor 又有新视频，库存计算会正确复用这条 remixed。

### 11.3 无永久死锁（排除 pending 问题后）

排除问题 #1（pending 计入 stockCount）后，配额算法与排期之间**不存在永久死锁**：
- remixed → scheduled（次日 dailyLimit 重置后可排期）
- scheduled → published（job 执行后）
- published → 不再计入 stockCount（次日，只算今天的）

状态流转是单向的，不会回退到阻塞状态。

---

## 修复建议（仅建议，不改代码）

1. **#1 高危**: 从 stockCount 中移除 pending，或改用 `stockCount = published(今天) + scheduled + remixed + remixing`。pending 是"待混剪原料"，不应阻止混剪触发。
2. **#2 高危**: 将 published 的"只算今天"检查改为按当前 pipeline 的 `publish_job_id` 查 `executed_at`，或在循环外用单条 SQL `COUNT(*)` 统计今天 published 数量。
3. **#3 中危**: 将 `_doRemix` 中 `matrix_id` 补全逻辑（L495-506）移到 `cdpInstanceId` 查找（L474-481）之前。
4. **#4 中危**: 在 `_doRemix` L465 后增加 `this.store.getRemixVideo(pipeline.source_video_id)` 存在性检查。
5. **#5-10 低危**: 清理死代码、统一 `||`→`??`、移除未使用变量、优化 N+1 查询。

---

*报告结束*
