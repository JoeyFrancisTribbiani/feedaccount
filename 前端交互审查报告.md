# 第三轮审查报告：前端交互代码正确性

**项目**: D:/WILLLUXE/yix-repo/feedaccount
**审查范围**: `public/app.js`（autoPublish 对象，第 7735–8382 行 + 播放数据弹窗，第 5843–5972 行）、`public/index.html`（自动发布 Tab，第 1546–1645 行）、`public/styles.css`（自动发布样式，第 4528–4900 行）
**审查日期**: 2026-09-18
**审查重点**: DOM ID 匹配 / 事件绑定 / 弹窗流程 / 表单提交 / 异常处理 / 变量作用域 / CSS 类名

---

## 审查结论

整体代码质量较高，DOM ID 全部匹配，API 路径与后端完全一致，事件绑定逻辑正确，弹窗流程完整。发现 **1 个高危问题**（状态筛选器失效）、**3 个中危问题**、**4 个低危问题**。

---

## 一、DOM 元素 ID 匹配检查 ✅ 全部通过

| app.js 中引用的 ID | index.html 中是否存在 | 备注 |
|---|---|---|
| `#ap-matrices-list` | ✅ 第 1555 行 | |
| `#ap-pipeline-tbody` | ✅ 第 1607 行 | |
| `#ap-monitor-list` | ✅ 第 1568 行 | |
| `#ap-filter-matrix` | ✅ 第 1578 行 | |
| `#ap-filter-status` | ✅ 第 1581 行 | |
| `#ap-refresh-matrices` | ✅ 第 1553 行 | |
| `#ap-refresh-pipeline` | ✅ 第 1591 行 | |
| `#ap-refresh-monitor` | ✅ 第 1566 行 | |
| `#ap-matrix-modal` | ✅ 第 1636 行 | |
| `#ap-modal-title` | ✅ 第 1639 行 | |
| `#ap-modal-body` | ✅ 第 1642 行 | |
| `#ap-refresh-logs` | ✅ 第 1625 行 | |
| `#ap-log-filter` | ✅ 第 1619 行 | |
| `#ap-logs-list` | ✅ 第 1628 行 | |
| `#analytics-overlay` | ⚠️ 动态创建 | 第 5854 行 JS 运行时创建，非 HTML 静态元素，**正常** |
| `#analytics-body` | ⚠️ 动态创建 | 同上，第 5863 行，**正常** |

**结论**: 所有静态 DOM ID 均在 index.html 中存在，动态创建的元素也正确。

---

## 二、事件绑定检查 ✅ 全部正确

| 元素 | 事件 | 回调 | 绑定位置 | 正确性 |
|---|---|---|---|---|
| `#ap-refresh-matrices` | click | `fetchMatrices()` | 7815 行 | ✅ |
| `#ap-refresh-pipeline` | click | `fetchPipelineTasks()` | 7816 行 | ✅ |
| `#ap-refresh-monitor` | click | `fetchMonitorData()` | 7817 行 | ✅ |
| `#ap-filter-matrix` | change | 设置 filterMatrix + 重新拉取 | 7818–7821 行 | ✅ |
| `#ap-filter-status` | change | 设置 filterStatus + `renderPipeline()` | 7822–7825 行 | ⚠️ **见高危问题 #1** |
| `#ap-matrix-modal` | click（遮罩） | `_closeMatrixModal()` | 7827–7831 行 | ✅ |
| `#ap-matrix-modal .modal-close` | click | `_closeMatrixModal()` | 7832 行 | ✅ |
| `#ap-refresh-logs` | click | `fetchPublishLogs()` | 7834 行 | ✅ |
| `#ap-log-filter` | change | `renderPublishLogs()` | 7835 行 | ✅ |
| 矩阵卡片 header `[data-matrix-toggle]` | click | `openMatrixAccountsModal(id)` | 8027–8033 行 | ✅ |
| 矩阵开关 `[data-matrix-enabled]` | change | `toggleMatrixAutoPublish(id, checked)` | 8036–8041 行 | ✅ |
| 配置按钮 `[data-config-btn]` | click | `renderConfigPage(id)` | 8058–8060 行 | ✅ |
| 监控按钮 `[data-monitor-trigger]` | click | `triggerMonitor(id)` | 8062–8064 行 | ✅ |
| 保存配置 `[data-config-save]` | click | 收集 payload + `saveMatrixConfig()` | 8142–8175 行 | ✅ |
| 返回 `[data-config-back]` | click | `openMatrixAccountsModal(id)` | 8177–8179 行 | ✅ |
| 时间段保存 `[data-config-slots-save]` | click | 校验 + `saveMatrixConfig()` | 8181–8198 行 | ✅ |
| 混剪方案 `[data-config-preset]` | change | 单字段保存 `{ presetId }` | 8200–8202 行 | ✅ |
| 每日发布数 `[data-config-daily]` | change | 单字段保存 `{ dailyLimit }` | 8203–8205 行 | ✅ |
| 监控间隔 `[data-config-interval]` | change | 单字段保存 `{ monitorIntervalHours }` | 8206–8208 行 | ✅ |
| CDP实例 `[data-config-cdp]` | change | 单字段保存 `{ cdpInstanceId }` | 8209–8211 行 | ✅ |
| 重试按钮 `[data-retry]` | click | `retryPipelineTask(id)` | 8270–8272 行 | ✅ |
| 立即发布 `[data-publish]` | click | `triggerPublishTask(id)` | 8275–8277 行 | ✅ |
| 日志按钮 `[data-logs]` | click | `togglePipelineLogs(jobId, btn)` | 8280–8282 行 | ✅ |
| 播放数据按钮 `.mx-analytics-btn` | click | `openAnalyticsModal(mxId, accName)` | 5655–5657 行 | ✅ |

---

## 三、弹窗交互流程检查 ✅ 正确

**矩阵卡片点击 → 弹窗显示账号列表 → 点配置 → 配置页 → 返回 → 关闭**

1. **矩阵卡片点击** → `openMatrixAccountsModal(matrixId)`（8045 行）→ 设置标题、渲染账号列表 HTML、`modal.style.display = 'flex'` ✅
2. **点配置按钮** → `renderConfigPage(matrixId)`（8102 行）→ 拉取最新配置、渲染配置表单、绑定事件 ✅
3. **返回账号列表** → `openMatrixAccountsModal(matrixId)`（8178 行）→ 重新渲染账号列表 ✅
4. **关闭弹窗** → 点击遮罩（7827–7831 行）或关闭按钮（7832 行）→ `_closeMatrixModal()` → `modal.style.display = 'none'` ✅

**弹窗显示/隐藏方式**: 使用 `style.display = 'flex'/'none'`，与 HTML 初始 `style="display:none;"` 一致，不依赖 `.hidden` 类。✅

---

## 四、表单提交检查 ✅ 正确（含 falsy 值处理）

**保存配置 payload 构建**（8142–8174 行）：

```javascript
const payload = {
  presetId: presetSel?.value || null,        // 空字符串 → null ✅
  dailyLimit: parseInt(dailyInp?.value, 10) || 0,  // 空值/NaN → 0 ✅
  monitorIntervalHours: parseInt(intervalInp?.value, 10) || 0,  // 同上 ✅
  cdpInstanceId: cdpSel?.value || null,      // 空字符串 → null ✅
};
// publishTimeSlots: 有值 → JSON.stringify(slots)，无值 → null ✅
// enabled: 保留当前状态 ✅
```

**PUT 请求 URL**: `/api/auto-publish/matrix-config/${encodeURIComponent(matrixId)}` ✅ 与后端路由 `^/api/auto-publish/matrix-config/([^/]+)$` 匹配。

**后端 falsy 值处理**（database.js 1905–1915 行）: 使用 `!== undefined` 判断，`0` 和 `null` 均可正确写入数据库。✅

**单字段自动保存**: preset/dailyLimit/interval/cdpInstance 的 change 事件各自发送 `{ 字段名: 值 }`，后端仅更新传入字段，不影响其他字段。✅

---

## 五、流水线列表渲染 ✅ 正确

`renderPipeline()`（8223–8283 行）:
- 空列表显示 `colspan="7"` 提示 ✅（表头 7 列匹配）
- 每行渲染数据行 + 隐藏的日志行（`pipeline-log-row hidden`）
- **重试按钮**: `canRetry = status === 'failed' || status === 'retry'` ✅
- **立即发布按钮**: `canPublish = (status === 'scheduled' || status === 'retry') && t.publishJobId` ✅
- **日志按钮**: 始终显示，`data-logs="${t.publishJobId || t.id}"` ✅
- 三个按钮均正确绑定 click 事件 ✅

---

## 六、监控列表渲染 ✅ 正确

`renderMonitor()`（8325–8342 行）:
- 正确从 `m.autoPublishConfig?.lastMonitorAt`（嵌套字段）读取上次监控时间 ✅
- 使用 `formatDateTime()` 格式化 ✅
- 空列表显示提示 ✅

**立即监控按钮**: 在矩阵账号列表弹窗中（8084 行），不在监控列表中。点击后调用 `triggerMonitor(matrixId)` → `POST /api/auto-publish/monitor/${matrixId}` ✅

---

## 七、日志展开/折叠 ✅ 正确

`togglePipelineLogs(jobId, btn)`（8286–8322 行）:
- `btn.closest('tr').nextElementSibling` 获取日志行 ✅（数据行与日志行相邻）
- 已展开则隐藏（`classList.add('hidden')`），已隐藏则展开（`classList.remove('hidden')`）✅
- 加载日志后渲染到 `[data-log-content]` 容器 ✅
- 异常处理显示错误信息 ✅

---

## 八、播放数据弹窗 ✅ 正确

`openAnalyticsModal(matrixId, accountName)`（5844–5972 行）:
- **加载**: `GET /api/auto-publish/analytics/${matrixId}` ✅ 与后端路由匹配
- **空数据处理**: `if (!data.jobs || !data.jobs.length)` 提前返回 ✅
- **汇总统计**: 总播放/最高/最低/平均 ✅
- **直方图**: 按区间分桶渲染 ✅
- **折线图**: 每个发布任务的 SVG polyline ✅
- **数据字段匹配**: 前端 `data.jobs[].records.map(r => r.views)` ↔ 后端 `records: records.map(r => ({ views: r.views_count }))` ✅
- **异常处理**: catch 块显示错误信息 ✅

---

## 九、状态切换 ✅ 正确

- **Tab 切换**（2630–2648 行）: 切换到 auto-publish 时调用 `autoPublish.init()` ✅
- **矩阵筛选器**: change → 设置 `filterMatrix` → `fetchPipelineTasks()`（重新拉取）✅
- **状态筛选器**: ⚠️ **见高危问题 #1**
- **日志级别筛选器**: `fetchPublishLogs()` 已传 level 到后端 + `renderPublishLogs()` 客户端二次过滤 ✅

---

## 十、刷新按钮 ✅ 全部正确

| 按钮 | 绑定 | 正确性 |
|---|---|---|
| 矩阵刷新 `#ap-refresh-matrices` | `fetchMatrices()` | ✅ |
| 流水线刷新 `#ap-refresh-pipeline` | `fetchPipelineTasks()` | ✅ |
| 监控刷新 `#ap-refresh-monitor` | `fetchMonitorData()` | ✅ |
| 日志刷新 `#ap-refresh-logs` | `fetchPublishLogs()` | ✅ |

---

## 十一、异常处理 ✅ 基本完善

| 场景 | 处理方式 | 正确性 |
|---|---|---|
| `fetchMatrices` 失败 | 显示错误信息到列表 ✅ | 7856–7862 行 |
| `fetchPipelineTasks` 失败（非 quiet）| 清空+重新渲染 | ✅ 但**无错误提示** ⚠️ 见低危 #4 |
| `fetchPipelineTasks` 失败（quiet）| 静默忽略 | ✅ 轮询场景合理 |
| `fetchProfiles` 失败 | `this.profiles = []` | ✅ 静默降级合理（非关键数据）|
| `fetchMonitorData` 失败 | `this.monitorData = []` + 重新渲染 | ✅ 但**无错误提示** ⚠️ 见低危 #4 |
| `fetchPresets` 失败 | `this.presets = []` | ✅ 静默降级合理 |
| `fetchCdpInstances` 失败 | `this.cdpInstances = []` | ✅ 静默降级合理 |
| `toggleMatrixAutoPublish` 失败 | showToast + 回滚开关 | ✅ 7920–7928 行 |
| `saveMatrixConfig` 失败 | showToast | ✅ 7944–7946 行 |
| `retryPipelineTask` 失败 | showToast | ✅ 7957–7959 行 |
| `triggerPublishTask` 失败 | showToast | ✅ 7975–7977 行 |
| `triggerMonitor` 失败 | showToast | ✅ 7989–7991 行 |
| `togglePipelineLogs` 失败 | 显示错误到日志区域 | ✅ 8319–8321 行 |
| `fetchPublishLogs` 失败（非 quiet）| showToast | ✅ 8357–8359 行 |
| `openAnalyticsModal` 失败 | 显示错误到弹窗 | ✅ 5969–5971 行 |
| `renderConfigPage` 拉取配置失败 | 使用默认配置继续 | ✅ 8110–8112 行 |

---

## 十二、变量作用域 ✅ 无问题

- `autoPublish` 对象所有方法使用 `this` 引用自身属性，`init()` 中通过 `_bindEvents()` 绑定的事件使用箭头函数保留 `this` 上下文 ✅
- `renderMatrices()`/`renderPipeline()` 等渲染方法中使用 `querySelectorAll().forEach()` 的回调均为箭头函数，`this` 正确指向 `autoPublish` ✅
- 全局函数 `request`、`escapeHtml`、`showToast`、`formatDateTime` 均在文件顶部定义（158–216 行）✅
- `CSS.escape` 用于转义 matrixId 作为 CSS 选择器值，所有现代浏览器支持 ✅
- 未发现引用未定义变量的情况 ✅

---

## 十三、CSS 类名检查 ✅ 全部存在

| app.js 中使用的类名 | styles.css 中是否存在 |
|---|---|
| `ap-creator-card` | ✅ 4578 行 |
| `ap-creator-header` | ✅ 4583 行 |
| `ap-creator-toggle` | ✅ 4597 行 |
| `ap-creator-info` | ✅ 4606 行 |
| `ap-creator-platform` | ✅ 4618 行 |
| `ap-creator-enabled` | ✅ 4622 行 |
| `ap-switch` | ✅ 4856 行 |
| `ap-switch-slider` | ✅ 4868 行 |
| `ap-creator-body` | ✅ 4627 行 |
| `ap-config-row` | ✅ 4634 行 |
| `ap-config-label` | ✅ 4640 行 |
| `ap-config-value` | ✅ 4646 行 |
| `ap-binding-item` | ✅ 4693 行 |
| `ap-binding-seq` | ✅ 4703 行 |
| `ap-binding-name` | ✅ 4709 行 |
| `ap-binding-limit` | ✅ 4717 行 |
| `ap-monitor-item` | ✅ 4744 行 |
| `ap-monitor-name` | ✅ 4753 行 |
| `ap-monitor-time` | ✅ 4757 行 |
| `ap-status-badge` | ✅ 4828 行 |
| `ap-status-pending` | ✅ 4837 行 |
| `ap-status-remixing` | ✅ 4838 行 |
| `ap-status-remixed` | ✅ 4839 行 |
| `ap-status-scheduled` | ✅ 4840 行 |
| `ap-status-published` | ✅ 4841 行 |
| `ap-status-failed` | ✅ 4842 行 |
| `ap-status-retry` | ✅ 4843 行 |
| `ap-fail-reason` | ✅ 4845 行 |
| `failed-row` | ✅ 4815 行 |
| `pipeline-log-row` | ⚠️ **CSS 中无定义** — 仅用 JS 的 `hidden` 类控制显隐，无独立样式，**功能不受影响** |
| `hidden` | ✅ 全局通用隐藏类 |
| `empty-state compact` | ✅ 全局通用类 |
| `muted-activity` | ✅ 全局通用类 |
| `button button-primary` | ✅ 1131 行 |
| `button button-secondary` | ✅ 1143 行 |
| `link-btn` | ✅ 769 行 |
| `modal-overlay` | ✅ 4032 行 |
| `modal-content`/`modal-header`/`modal-close`/`modal-body` | ✅ 全局弹窗类 |

---

## 十四、API 路径与后端一致性 ✅ 全部匹配

| 前端调用 | 方法 | 后端路由 | 一致性 |
|---|---|---|---|
| `/api/auto-publish/matrices` | GET | ✅ server.js 4184 行 | ✅ |
| `/api/auto-publish/matrix-config/:matrixId` | GET | ✅ server.js 4211–4222 行 | ✅ |
| `/api/auto-publish/matrix-config/:matrixId` | PUT | ✅ server.js 4223–4238 行 | ✅ |
| `/api/auto-publish/pipeline?matrixId=&status=&limit=` | GET | ✅ server.js 4242–4248 行 | ✅ |
| `/api/auto-publish/pipeline/retry/:id` | POST | ✅ server.js 4252–4258 行 | ✅ |
| `/api/auto-publish/monitor/:matrixId` | POST | ✅ server.js 4261–4267 行 | ✅ |
| `/api/auto-publish/logs?level=&limit=` | GET | ✅ server.js 4270–4291 行 | ✅ |
| `/api/auto-publish/analytics/:id` | GET | ✅ server.js 4294–4337 行 | ✅ |
| `/api/tiktok/publish/jobs/:jobId/execute` | POST | ✅ server.js 2220–2226 行 | ✅ |
| `/api/cdp/instances` | GET | ✅ server.js 1584 行 | ✅ |
| `/api/ai-presets` | GET | ✅ server.js 4626–4629 行 | ✅ |
| `/api/profiles` | GET | ✅ server.js 1532 行 | ✅ |

**PUT payload 字段**: 前端发送 `{ enabled, presetId, dailyLimit, monitorIntervalHours, publishTimeSlots, cdpInstanceId }` → 后端接收并透传给 `upsertMatrixAutoPublishConfig()`（4225–4235 行）。后端额外接受 `ratio`、`hashtagsJson`、`privacyLevel` 但前端未发送（使用默认值），不影响功能。✅

---

## 发现的问题

### 🔴 高危 #1：状态筛选器（`#ap-filter-status`）失效

**位置**: `app.js` 第 7822–7825 行

**问题**: 当用户切换状态筛选器时，`change` 事件回调仅执行：
```javascript
this.filterStatus = e.target.value;
this.renderPipeline();  // ← 仅重新渲染，未过滤！
```

而 `renderPipeline()`（8223–8283 行）直接遍历 `this.pipelineTasks.map(...)`，**完全没有按 `this.filterStatus` 过滤**。这意味着：
- 用户选择"仅显示 failed 状态"后，列表中仍显示所有状态的任务
- 状态筛选器形同虚设

**对比**: 矩阵筛选器（`#ap-filter-matrix`）的 change 事件调用 `fetchPipelineTasks()`（7820 行），会重新从服务器拉取过滤后的数据，功能正常。

**根因**: `fetchPipelineTasks()` 在构建请求参数时**已经**把 `this.filterStatus` 传给了后端（7870 行 `params.set('status', this.filterStatus)`），但 `filterStatus` 的 change 事件没有调用 `fetchPipelineTasks()`，只调用了 `renderPipeline()`。

**影响**: 用户无法按状态筛选流水线任务，影响排查失败任务。

**建议修复**: 将第 7824 行的 `this.renderPipeline()` 改为 `this.fetchPipelineTasks()`，与矩阵筛选器保持一致。或者，如果希望纯客户端过滤，在 `renderPipeline()` 中添加 `this.pipelineTasks.filter(t => !this.filterStatus || t.status === this.filterStatus)`。

---

### 🟡 中危 #2：`triggerMonitor` 传递的 matrixId 被后端忽略

**位置**: `app.js` 第 7981–7992 行 + `server.js` 第 4261–4267 行

**问题**: 前端 `triggerMonitor(matrixId)` 调用 `POST /api/auto-publish/monitor/${matrixId}`，但后端处理逻辑为：
```javascript
const apMonitorMatch = pathname.match(/^\/api\/auto-publish\/monitor\/([^/]+)$/);
if (apMonitorMatch && request.method === "POST") {
  autoScheduler?.monitorMatrixVideos?.().catch(() => {});  // ← 忽略 matrixId
  sendJson(response, 200, { ok: true, message: "监控已触发" });
}
```

后端**完全忽略** URL 中的 `matrixId`，而是调用 `monitorMatrixVideos()` 对所有已启用的矩阵执行监控。

**影响**: 用户在矩阵 A 的弹窗中点击"立即监控"，实际上会触发所有矩阵的监控，而非仅矩阵 A。用户预期与实际行为不符。此外，`monitorMatrixVideos()` 内部有间隔检查（108 行 `if (now - lastMonitorAt < intervalMs) continue`），如果刚监控过，点击"立即监控"可能不会执行任何操作，但前端仍提示"监控已触发"，造成误导。

**建议**: 后端应根据 matrixId 仅触发指定矩阵的监控，或前端提示文案改为"已触发全局监控"。

---

### 🟡 中危 #3：流水线日志过滤使用不可靠的子字符串匹配

**位置**: `app.js` 第 8286–8322 行（`togglePipelineLogs`）

**问题**: 点击日志按钮时，前端调用 `GET /api/auto-publish/logs?limit=20` 获取最近 20 条日志，然后在客户端用子字符串匹配过滤：
```javascript
const taskId = btn.dataset.logs;  // 实际是 publishJobId 或 task.id
const logs = data.filter((l) =>
  (l.message || '').includes(taskId) || (l.taskId && l.taskId.includes(taskId))
);
```

存在多个问题：
1. **只取最近 20 条日志**：如果该任务的日志不在最近 20 条内，则永远看不到
2. **子字符串匹配不可靠**：`taskId` 可能是其他 ID 的子串（如 `task_1` 会匹配到 `task_10`、`task_11` 等），导致显示不相关日志
3. **后端日志查询条件固定**：后端 `WHERE message LIKE '%发布任务%'`（4275 行），只返回包含"发布任务"的日志，可能与特定任务无关
4. **`l.taskId` 字段**：后端返回 `taskId: l.task_id`（4288 行），但日志的 `task_id` 是否准确填充取决于日志写入逻辑

**影响**: 用户可能看到错误的日志或看不到日志，影响问题排查。

**建议**: 后端应提供按 `taskId`/`publishJobId` 精确查询的日志接口，如 `GET /api/auto-publish/logs?taskId=xxx`。

---

### 🟡 中危 #4：`fetchMonitorData` 重复调用 `/api/auto-publish/matrices`

**位置**: `app.js` 第 7892–7905 行

**问题**: `fetchMonitorData()` 调用 `GET /api/auto-publish/matrices`（与 `fetchMatrices()` 完全相同的接口），然后在客户端过滤 `enabled === 1` 的矩阵。`init()` 方法（7786–7791 行）同时调用了 `fetchMatrices()` 和 `fetchMonitorData()`，导致同一接口被调用两次。

**影响**: 每次进入自动发布 Tab 或刷新时，`/api/auto-publish/matrices` 被请求两次，浪费网络资源。该接口查询所有矩阵 + 账号 + 配置 + 实例 + 达人数（4185–4205 行），数据量可能较大。

**建议**: `fetchMonitorData()` 可复用 `this.matrices` 数据（`fetchMatrices()` 已拉取），或合并为一次请求后在客户端分发。

---

### 🟢 低危 #5：`.ap-creator-toggle.expanded` 和 `.ap-creator-header.expanded` 为死代码

**位置**: `styles.css` 第 4594–4605 行

**问题**: CSS 中定义了 `.ap-creator-header.expanded` 和 `.ap-creator-toggle.expanded` 样式（展开时变色 + 箭头旋转 90°），但 app.js 中**从未**添加 `expanded` 类。矩阵卡片点击后打开弹窗而非内联展开，所以这些样式永远不会生效。

**影响**: 无功能影响，仅为死代码/样式不一致。

---

### 🟢 低危 #6：播放数据弹窗中部分字段未 HTML 转义

**位置**: `app.js` 第 5951 行

**问题**:
```javascript
<span style="...">${job.executedAt?.substring(0, 16) || '—'} · ${job.recordCount}条记录</span>
```
`job.executedAt` 和 `job.recordCount` 直接插入 HTML，未使用 `escapeHtml()`。虽然 `executedAt` 来自数据库的 `executed_at`（日期字符串）、`recordCount` 是数字，XSS 风险极低，但不符合安全编码规范。

**影响**: 理论上的 XSS 风险（如果数据库被注入恶意数据）。

---

### 🟢 低危 #7：轮询未包含监控数据刷新

**位置**: `app.js` 第 7838–7847 行

**问题**: `_startPolling()` 每 10 秒轮询 `fetchPipelineTasks()` 和 `fetchPublishLogs()`，但**不包含** `fetchMonitorData()`。监控状态列表（上次监控时间）不会自动更新，用户需要手动点击刷新按钮。

**影响**: 监控时间显示可能过时，需手动刷新。

---

### 🟢 低危 #8：播放数据弹窗关闭按钮使用内联 `onclick`

**位置**: `app.js` 第 5861 行

**问题**:
```javascript
<button class="modal-close" type="button" onclick="document.querySelector('#analytics-overlay').remove()">×</button>
```
使用内联 `onclick` 而非 `addEventListener`。虽然功能正常，但不利于 CSP（Content Security Policy）策略，且与项目其他弹窗的关闭方式不一致（其他弹窗用 `addEventListener` 绑定 `.modal-close`）。

**影响**: 功能正常，但代码风格不一致，CSP 不友好。

---

## 汇总表

| # | 严重程度 | 问题 | 位置 |
|---|---|---|---|
| 1 | 🔴 高危 | 状态筛选器 `#ap-filter-status` 失效，切换无效果 | app.js:7822–7825 |
| 2 | 🟡 中危 | `triggerMonitor` 的 matrixId 被后端忽略，触发全局监控 | app.js:7981 + server.js:4261 |
| 3 | 🟡 中危 | 流水线日志过滤用子字符串匹配，不可靠且只取 20 条 | app.js:8286–8322 |
| 4 | 🟡 中危 | `fetchMonitorData` 重复请求 `/api/auto-publish/matrices` | app.js:7892–7905 |
| 5 | 🟢 低危 | `.ap-creator-toggle.expanded` / `.ap-creator-header.expanded` 死代码 | styles.css:4594–4605 |
| 6 | 🟢 低危 | 播放数据弹窗 `job.executedAt`/`recordCount` 未转义 | app.js:5951 |
| 7 | 🟢 低危 | 轮询未包含监控数据刷新 | app.js:7838–7847 |
| 8 | 🟢 低危 | 播放数据弹窗关闭按钮使用内联 `onclick` | app.js:5861 |

---

## 通过项汇总

| 检查项 | 结果 |
|---|---|
| DOM 元素 ID 匹配 | ✅ 全部通过（15 个静态 ID + 2 个动态创建） |
| 事件绑定 | ✅ 24 个事件绑定全部正确 |
| 弹窗流程 | ✅ 矩阵卡片→账号列表→配置页→返回→关闭 完整正确 |
| 表单提交 | ✅ payload 完整含 falsy 值，PUT URL 正确 |
| 单字段自动保存 | ✅ preset/dailyLimit/interval/cdpInstance 均正确 |
| 流水线列表渲染 | ✅ 重试/立即发布/日志按钮正确绑定 |
| 监控列表渲染 | ✅ 正确读取嵌套字段 |
| 日志展开/折叠 | ✅ 正确切换 hidden 类 |
| 播放数据弹窗 | ✅ 正确加载和渲染折线图 |
| 状态切换 | ✅ Tab 切换触发 init（状态筛选器除外） |
| 刷新按钮 | ✅ 4 个刷新按钮全部正确 |
| 异常处理 | ✅ 16 个场景均有处理（2 个静默降级可接受） |
| 变量作用域 | ✅ 无未定义变量引用 |
| CSS 类名 | ✅ 全部存在（`pipeline-log-row` 无独立样式但功能不受影响） |
| API 路径一致性 | ✅ 12 个 API 路径与后端完全匹配 |
